# 掲載前チェックリスト（転写検証実験・下見）

- 作成: 2026-08-20 丸山。対象は下見（16人）の掲載。本番の掲載でも同じ手順を使う。
- 掲載文: `project/掲載文_下見.md` ／ 計画: `project/実験計画書_転写検証.md`（第2稿）
- **上から順に消し込む。1つでも残っていたら掲載しない。**
- ここに書いてある作業は**すべて人手**（GASのデプロイ、スプレッドシートの用意、募集サイトへの
  掲載）。コード側の準備（刺激・設定・ページ）は済んでいる。

---

## A. 掲載してよいかの前提

- [ ] **A-1 倫理審査の承認が下りている**。承認番号と承認日を控える。
      承認前に有償で参加者を募集してはいけない。書類は `倫理審査` の手順で作る。
- [ ] **A-2 指導教員（栗原先生）の承認**。計画書 第2稿と、この掲載文・掲載条件（人数16人・
      報酬200円・所要7分）を見せて了解を取る。計画書10章の「この順序とWISS 8/31目標の承認」が
      未了のままなら、先にそこを片づける。
- [ ] **A-3 予算の裏づけ**。16人 × 200円 ＝ 3,200円 に、募集サイトの手数料を足した額。
- [ ] **A-4 ターゲット8字が確定版であること**を確認する。
      `experiment/transfer_config.js` の `targets` が **あ・か・が・ぱ・し・つ・ま・ら**
      であること（2026-08-20に確定。以後変更禁止）。

```bash
node -e 'global.window={};require("./experiment/transfer_config.js");
         console.log(window.TRANSFER_CONFIG.targets.join("・"))'
# → あ・か・が・ぱ・し・つ・ま・ら
```

## B. サーバ側（Google Apps Script）の用意

いま `gas/code.gs` は**まだ転写検証実験に対応していない**。`gas/transfer_patch.md` の
差分を当てる必要がある。既存の実験（乙課題・frac課題）の処理には触らない差分になっている。

**下見ぶんは 2026-08-20 に自動化して済ませた**（B-1〜B-5 は済み・B-6 は一部）。
以下は「何が済んでいて、何が残っているか」の記録として読む。本番の掲載では、
新しいスプレッドシートに対して同じ手順を繰り返す（G-1）。

| 項目 | 状態 |
|---|---|
| B-1 記録用スプレッドシート（下見専用・新規） | ✓ 済 `1gsJ6_Rucv5uoKsgrs_m-Y41sxh5B0qcPKWyHkxveKMs` |
| B-2/B-3 コードの用意（`gas/code.gs` に `gas/transfer_patch.md` の3差分を当てたもの） | ✓ 済 → `gas_transfer/code.gs` としてリポジトリに置いた |
| B-4 シートIDの設定 | ✓ 済（スクリプトプロパティではなく `gas_transfer/code.gs` の定数 `SPREADSHEET_ID` に直書き。clasp で入れ直せるようにするため） |
| B-5 デプロイ（ウェブアプリ・全員アクセス・実行は自分） | ✓ 済（`clasp push -f` → `clasp deploy`） |
| B-6 `/exec` URL をページ側に入れる | △ `transfer_config.js` の `roster.status_url` と `logging.submit_url` は入れた。**`transfer.js` が `submit_url` へ送る処理はまだ入っていない**（B-8） |
| B-7 `roster.require_server` | ✓ 済（`true`）|

デプロイ済みの `/exec` URL（下見用）:

```
https://script.google.com/macros/s/AKfycbw1bnn7H3HCYuD3fDUIi1djGcnea0cVqnA15XiN5ipgwlrsCNkQx4q16-bdN0wbGQRP/exec
```

コードを直したときの入れ直しは、リポジトリの `gas_transfer/` で:

```bash
clasp push -f
clasp deploy -i AKfycbw1bnn7H3HCYuD3fDUIi1djGcnea0cVqnA15XiN5ipgwlrsCNkQx4q16-bdN0wbGQRP -d "説明"
```

> デプロイIDを変えなければ `/exec` URL は変わらない。`clasp push` だけでは**公開版に
> 反映されない**（`@HEAD` が変わるだけ）。直したつもりで古い版が動き続ける事故が起きやすい。

### まだ残っている2つ（どちらも人手が要る）

- [ ] **B-8 ウェブアプリの利用許可を1回だけ与える（本人しかできない）**。
      いま `/exec` を開くと **`Authorization needed`（承認が必要です）** の画面が出て、
      HTTP 403 になる。clasp から作ったスクリプトは、持ち主が一度も許可を与えていないため、
      スプレッドシートを読み書きする許可が下りていない。
      1. ブラウザで上の `/exec` URL を開く（Google アカウント `g19922ma@gm.tsuda.ac.jp` で）
      2. **「REVIEW PERMISSIONS（権限を確認）」→ アカウントを選ぶ**
      3. 「このアプリは確認されていません」と出たら **「詳細」→「(安全でないページ)に移動」**
         （自分が作ったスクリプトなのでこの表示になる）
      4. **「許可」** を押す
      → そのあと `{"status":"ok","message":"iFont experiment endpoint"}` が返れば完了。
      **これを済ませるまで、C節の疎通確認は全部 403 で失敗する。**
- [ ] **B-9 回答をこの窓口へ送る処理を `transfer.js` に入れる**。
      いまページは**集団の振り分け（`roster.status_url`）だけ**サーバを使っていて、
      **1問ごとの回答はどこにも送っていない**。`prod_common.js` の `SUBMIT_URL` は実験1
      （乙課題・frac課題）と共用なので、そこを埋めると既存の実験まで巻き込む。
      そこで `transfer.js` 側に「`CFG.logging.submit_url` へ POST する」処理を足す必要がある。
      送る中身は `prod_common.js` の `post()` が組む封筒と同じ形にそろえること
      （`participant_id` / `worker_id` / `completion_code` / `ts` / `audio_device` /
      `resume_count` / `resume_gap_s` / `ua` / `dpr` / `screen` / `touch` / `refresh_hz`）。
      うち後ろの3種（再生機器の申告・再開回数・中断秒）は `prod_common.js` が外に
      見せていないので、同意画面のラジオボタンと `PROD.loadState()` の戻り値から作り直す。

## C. デプロイ後の疎通確認（**B-8 を済ませてから**）

`$EXEC` は上のデプロイ済みURL。まとめて流すなら:

```bash
EXEC="https://script.google.com/macros/s/AKfycbw1bnn7H3HCYuD3fDUIi1djGcnea0cVqnA15XiN5ipgwlrsCNkQx4q16-bdN0wbGQRP/exec"
```

- [ ] **C-1 生きているか**

```bash
curl -sL "$EXEC"
# → {"status":"ok","message":"iFont experiment endpoint"}
```

- [ ] **C-2 集団の振り分けが返るか**（試し打ち用のIDで2回叩く）

```bash
curl -sL "$EXEC?action=transfer_status&phase=calib&participant_id=curltest-001"
curl -sL "$EXEC?action=transfer_status&phase=calib&participant_id=curltest-002"
# → 1本目 {"status":"ok","group":"acal","assign_index":0}
#   2本目 {"status":"ok","group":"aprime","assign_index":0}
#   （交互に配られる ＝ 音声と視覚が半々になる）
```

> 参加者IDの頭を **`curltest-`** にすること。この接頭辞の行は `is_test` 列に `true` が立ち、
> あとで C-7 のひとコマンドでまとめて消せる。

- [ ] **C-3 同じ人が戻ってきたら同じ集団が返るか**

```bash
curl -sL "$EXEC?action=transfer_status&phase=calib&participant_id=curltest-001"
# → {"status":"ok","group":"acal","assign_index":0,"returning":true}
```

- [ ] **C-4 前半に出た人が後半を断られるか**

```bash
curl -sL "$EXEC?action=transfer_status&phase=test&participant_id=curltest-001"
# → {"status":"ok","blocked":true,"reason":"already_in_calib"}
```

- [ ] **C-5 送った回答が本当にシートに入るか**（POST してから件数を見る）

```bash
# 1問ぶんの回答を送る（正解「あ」に「あ」と答えた形）
curl -sL --post301 --post302 --post303 \
  -H 'Content-Type: text/plain;charset=utf-8' \
  -d '{"participant_id":"curltest-001","worker_id":"curltest","completion_code":"CURLTEST0001",
       "modality":"transfer_audio","phase":"calib","group":"acal","assign_index":0,
       "assign_source":"server","trial_index":1,"stimulus_id":"acal|あ|audio||60",
       "target_char":"あ","response_char":"あ","gate_ms":60,"is_filler":false,
       "n_choices":68,"rt_ms":1234,"version":"3.0","config_version":"prov-2026-08-20b"}' \
  "$EXEC"
# → {"status":"ok","correct":true}

# 見え心地の回答も1件
curl -sL --post301 --post302 --post303 \
  -H 'Content-Type: text/plain;charset=utf-8' \
  -d '{"participant_id":"curltest-001","modality":"transfer_wellbeing","phase":"calib",
       "group":"b","choice":"fade","wellbeing_json":"{}","version":"3.0"}' \
  "$EXEC"
# → {"status":"ok"}

# 行数を数える（シートを開かずに、入ったことを確かめる）
curl -sL "$EXEC?action=transfer_health"
# → {"status":"ok","spreadsheet_id":"1gsJ...","rows":{"transfer_trials":1,
#     "transfer_wellbeing":1,"transfer_roster":2},"test_rows":{...同じ数...}}
```

> `rows` が増えていれば、**POST した行が実際にシートに入った**ということ。
> `test_rows` が `rows` と同じなら、まだ本物の参加者は1人も来ていない。

- [ ] **C-6 スプレッドシートを一度は目で見る**。`transfer_trials` `transfer_wellbeing`
      `transfer_roster` の3枚ができ、列名が揃っていること（自動生成なので普通は合っている）。
- [ ] **C-7 試し打ちの行を消す**（本番の人数に混ざらないように）

```bash
curl -sL "$EXEC?action=transfer_purge_test"
# → {"status":"ok","removed":{"transfer_trials":1,"transfer_wellbeing":1,"transfer_roster":2}}
curl -sL "$EXEC?action=transfer_health"
# → rows がすべて 0 に戻っていること
```

> `transfer_purge_test` が消せるのは、参加者IDが `curltest-` で始まる行**だけ**。
> クラウドソーシングの作業者IDがこの形になることはないので、本物のデータには手が届かない。

## D. 刺激とページ（機械で確かめる）

- [ ] **D-1 聴覚刺激の一式検査**。次のコマンドが「合格」で終わること。
      入口HTMLの参照、設定と索引の一致、実験ページが引きうる全組合せ、
      WAV 544本の実測長さを、まとめて見る。

```bash
node experiment/tools/check_transfer_stimuli.js
# → 合格: 聴覚刺激は本番の構成で揃っている。
```

- [ ] **D-2 刺激を作り直したなら**、索引も一緒に作り直したか。作り直しは次のコマンド。
      録音の生WAV（`recordings_raw/`）はリポジトリに入れない（`.gitignore` 済み）。
      配信する打ち切り済みWAV（`experiment/transfer_stimuli/`）はコミットする。

```bash
python3 experiment/tools/build_transfer_gates.py \
    --src recordings_raw/adopted --onsets recordings_raw/adopted_onsets.json
```

- [ ] **D-3 代用モードが無効であること**。`transfer_config.js` の `audio.fallback.enabled`
      が `false`。true のままだと、索引が読めなかったときに**別の音（合成音）で静かに
      走り続け、気づかずに使えないデータを取ってしまう**。D-1 が一緒に見ている。

## E. 公開ページ（GitHub Pages）の確認

**実物のURLで確かめる。手元のファイルが動いていることの証拠にはならない**
（未コミットの変更は公開されていない）。

- [ ] **E-1 コミットして push する**。push 先は `origin maruyama/font-exploration` と
      `fork main` のみ。**`origin main`（栗原先生のmain）には push しない**。

```bash
git add experiment/transfer_config.js experiment/transfer.js \
        experiment/transfer_stimuli experiment/transfer_audio_manifest.json \
        experiment/tools/build_transfer_gates.py experiment/tools/check_transfer_stimuli.js \
        gas_transfer project/掲載文_下見.md project/掲載前チェックリスト.md
git commit -m "転写検証: 本番の聴覚刺激を組み込み、下見の掲載準備をした"
git push fork HEAD:main
git push origin HEAD:maruyama/font-exploration
```

- [ ] **E-2 公開されたページとファイルが実際に取れるか**（Pages の反映に数分かかる）

```bash
B=https://g19922ma.github.io/iFont/experiment
curl -s -o /dev/null -w "%{http_code} " $B/transfer_calib.html
curl -s -o /dev/null -w "%{http_code} " $B/transfer.js
curl -s -o /dev/null -w "%{http_code} " $B/transfer_config.js
curl -s -o /dev/null -w "%{http_code} " $B/transfer_audio_manifest.json
curl -s -o /dev/null -w "%{http_code}\n" "$B/transfer_stimuli/%E3%81%B1_g0060.wav"
# → 200 200 200 200 200   （最後はファイル名にかなが入るWAV。ここが404なら配信の設定を疑う）
```

- [ ] **E-3 公開版が手元と同じか**。`transfer_config.js` の設定版が
      `prov-2026-08-20b` になっていること。

```bash
curl -s https://g19922ma.github.io/iFont/experiment/transfer_config.js | grep config_version
```

- [ ] **E-4 検索避けが効いているか**。`transfer_calib.html` に
      `<meta name="robots" content="noindex,nofollow">` が入っていること。

## F. 掲載直前の最終確認（自分で1回通す）

`?prod=1` を付けた**本番と同じURL**で、自分が1人目の参加者になったつもりで最後まで通す。

- [ ] **F-1 音声の課題を通す**（`...?prod=1&wid=selftest-audio&group=acal` で強制できるのは
      研究者モードだけなので、本番モードでは名簿の順番どおりに割り当てられる。
      1本目に開けば音声になる）。
      - [ ] 同意画面が出て、再生機器の申告欄がある
      - [ ] 音量の確認で「あ・い・う・え・お」が**録音した肉声**で鳴る（合成音ではない）
      - [ ] 練習が動き、五十音の表から答えを選べる
      - [ ] 画面下に「**音声は代用モード**」が**出ていない**
      - [ ] 最後に12桁の完了コードが出る
- [ ] **F-2 視覚の課題を通す**（別のブラウザ／シークレットウィンドウで、別の `wid` で開く。
      2本目なので視覚になる）。
      - [ ] 文字が徐々に現れ、途中で消える
      - [ ] 最後に完了コードが出る
- [ ] **F-3 スプレッドシートに行が入っているか**を目で見る。
      - `transfer_trials` に1問1行。`target_char` `response_char` `correct` `gate_ms`
        `config_version` が埋まっている
      - `config_version` 列が `prov-2026-08-20b`
- [ ] **F-4 所要時間を測る**。F-1 と F-2 の**開始から完了コードまで**をストップウォッチで測る。
      掲載文のタイトルは「約7分」。**実測が8分を超えたら、タイトルの分数と報酬を直すか、
      1人あたりの問題数（`design.max_target_trials`）を減らす。**
      現在の設定では1人あたり90問ほどになるので、ここは実測でずれる可能性が高い。
- [ ] **F-5 確認に使った自分の行を消す**（`transfer_trials` と `transfer_roster` の両方）。
- [ ] **F-6 掲載文の作業URLに `?prod=1` が入っているか**を、貼り付けた文面の実物で見る。
      落ちていると、参加者は完了コードにたどり着けずデータも残らない。

## G. 下見が終わったあと（本番の掲載に移る前）

- [ ] **G-1 本番は「別のスプレッドシート＋別のデプロイ」でやり直す**。
      **いまデプロイしてあるものは下見専用**である。名簿（`transfer_roster`）に残った
      下見の参加者は、同じ窓口のまま本番の較正フェーズに来ると「戻ってきた人」と
      見なされて同じ集団に入ってしまい、下見と本番のデータが混ざる。
      本番に移るときは、次を上から順にやり直す。
      1. 新しいスプレッドシートを作り、シートIDを控える
      2. `gas_transfer/` を新しいシートのスクリプトとして作り直す
         （`clasp create --type sheets --parentId <新しいシートID>`。
         `.clasp.json` の `scriptId` / `parentId` が新しいものに変わる）
      3. `gas_transfer/code.gs` の定数 `SPREADSHEET_ID` を新しいシートIDに書き換える
      4. `clasp push -f` → `clasp deploy` → 新しい `/exec` URL を控える
      5. **B-8 の利用許可を新しいデプロイでもう一度与える**（デプロイごとに要る）
      6. `experiment/transfer_config.js` の `roster.status_url` と `logging.submit_url` を
         新しいURLに差し替え、`config_version` を上げる
      7. C節の疎通確認をやり直す
      - 手順2〜4をやらずに同じシートを使ってしまった場合の応急処置: 本番の前に
        `transfer_trials` `transfer_wellbeing` `transfer_roster` を
        `pilot_` を頭に付けた名前に**変えて**空から始める。下見の参加者の作業者IDは
        控えておき、本番の募集で除外する。
- [ ] **G-2 下見の結果を反映する**（`project/掲載文_下見.md` §5 の1〜5）。
      打ち切りなしの正答率・所要時間・基準アニメの長さを出し、シミュレーション（A2）で
      打ち切り時刻・まぎれ字の量・必要人数を確定する。
- [ ] **G-3 数値を確定したら `config_version` を上げる**（`prov-` を外した版にする）。
      どの数値で取ったデータかが記録から分かるようにするため。
- [ ] **G-4 生成の凍結**。本番の検証フェーズ（群Atest・群B）を掲載する前に、
      生成のコード・設定・8字ぶんの進み方を**コミットで固定**し、そのコミット番号を
      `transfer_config.js` の `visual.warp.frozen_commit` に書く（計画書 Q3）。
