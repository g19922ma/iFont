// =========================================================================
// 転写検証実験 設定ファイル (transfer.html / transfer.js が読む)
//   計画書: project/実験計画書_転写検証.md (第2稿・4章と10章)
//
//   この実験の数値は「まだ決まっていない」ものが多い(計画書11章の A2 で確定する)。
//   そこで、あとで数値だけ差し替えられるように、実験パラメータを全部この1ファイルに
//   集めてある。transfer.js には数値を直接書かない。
//
//   Python 側(experiment/tools/build_transfer_gates.py)もこのファイルを
//   node 経由で読む(同じ表を2か所に書かないため)。したがって
//   **このファイルは「window.TRANSFER_CONFIG = { ... };」の代入1つだけ**にすること
//   (関数・分岐・他ファイルへの依存を入れない)。
//
// ── A2 の結果が出たら差し替える項目(ここだけ触ればよい) ───────────────
//   1. targets                     … ターゲット8字 (2026-08-20 に確定済み。**もう触らない**)
//   2. audio.gates_ms              … 聴覚の打ち切り時刻(音の実体の開始からの絶対ms)
//      （下見だけ足す時点は audio.pilot_extra_gates。本番前に切る）
//   3. visual.progress_pct_levels  … 視覚較正(群A′)の打ち切り水準 s%
//      （参加者ごとのずらしは visual.progress_pct_shift。2026-08-24 に追加）
//   4. visual.gates_ms             … 群Bの打ち切り時刻ms(聴覚と同じ時点で比べる)
//   5. visual.base_anim_ms         … 基準アニメの長さ(Q4: 下見で決める)
//   6. visual.families.*           … ぼかし半径の上限・ワイプの向きなど描画の定数
//   7. design.* / decoys.*         … 1人あたりの問題数・偽のターゲットの数・確認問題の割合
//   8. assignment.*                … 条件×字の割付表(参加者の連番から決める)
//   9. warp（転写の進み方 s(t)）   … 生成工程の出力。transfer_warp.json に置き、
//                                     凍結時のコミット番号を warp.frozen_commit に書く
//  10. wellbeing.chars             … 見え心地評価に使う代表字
//   (デプロイのときに入れるもの: roster.status_url と logging.submit_url =
//    GAS ウェブアプリの /exec URL。下見用は 2026-08-20 に入れ済み。本番は別の
//    スプレッドシートで作り直して差し替える)
// =========================================================================
window.TRANSFER_CONFIG = {
  // 設定の版。全レコードに config_version として保存される(どの数値で取ったデータか分かる)。
  // prov = provisional(暫定)。数値がまだ A2 で確定していないという印。
  //   prov-2026-08-20  … 最初の実装(ターゲット8字は仮置き・音声は合成音の代用)
  //   prov-2026-08-20b … ターゲット8字を確定値に差し替え、聴覚刺激を録音した
  //                      自然音声の打ち切り済みWAVに切り替えた
  //   prov-2026-08-21  … 聴覚の打ち切り時点を**字ごとの配置**に変えた
  //                      (Furui 1986 の知見＋下見の結果。audio.gates_ms の注記を見る)。
  //                      あわせて「全長」を各字の1点として足し、下見だけの10ms追加は切った。
  //                      見え心地(RQ3)は群Bの末尾から独立の実験(群C)へ切り出した。
  //   prov-2026-08-23  … 聴覚刺激を自然音声からあみたろ(COEIROINK・切出版)へ切り替えた。
  //                      VOICEVOX・AivisSpeech・COEIROINKの10話者を比較し、清濁のVOTが
  //                      最も明瞭(か-が=48ms)で、息っぽさ・音程のばらつきも小さい話者として
  //                      採用(project/合成音声_話者候補.md 11章)。単独の1モーラを直接
  //                      合成すると発話全体が間延びする問題があったため、「字+ん」で合成した
  //                      あとに対象字だけを自動検出で切り出す方式（キャリアフレーズ法）を
  //                      採った。切り出し境界は高域エネルギー比率の低下(鼻音「ん」の特徴)を
  //                      手がかりに自動検出しており、全数の目視確認はまだ行っていない。
  //   prov-2026-08-24  … 視覚の設計を2点変えた（先生のパイロット kurihara-pilot2 の結果）。
  //                      ① 視覚較正の打ち切り水準を上寄りの [6,13,22,34,50,70,100]（＋下見の3%）
  //                         から **薄い側6水準を1〜10%の帯に置き、20%と100%を固定**する
  //                         [1, 2.5, 4, 5.5, 7, 8.5, 20, 100] に置き換えた。
  //                         さらに**参加者ごとに薄い側6水準を0.5%ずつずらす**ようにした
  //                         （progress_pct_shift）。13%以上の6水準が全問正解で、
  //                         曲線の形を決める情報を持っていなかったため。
  //                      ② 基準アニメの長さを 600ms → **300ms**。群Bの打ち切り最大が220msで、
  //                         600msだと等速の対照が進み具合37%までしか進まず不利だったため。
  //                         RQ4の速さ2水準も比率1.67倍を保って [600,1000]→[300,500] に。
  //                      下見だけの仕掛けだった pilot_extra_levels は役目を終えたので削除した。
  //   prov-2026-08-24b … **検証用の音声集団（群Atest）を廃止し、集団を4つにした**
  //                      （丸山決定・2026-08-24。外部の専門家2名への相談の結論）。
  //                      検証フェーズ（transfer_test.html）は群Bだけになり、振り分けが
  //                      起きなくなる。検証の比較相手は群Acal の曲線（＝生成に与えた
  //                      目標プロファイル）に一本化する。
  //                      理由: 群Bの参加者は生成に一切関与していないので、
  //                      「群Acalの曲線 vs 群Bの曲線」の比較そのものが、生成に関わって
  //                      いない集団での検証（ホールドアウト検証）として成立する。
  //                      各25名程度の小標本では、AcalとAtestを分けるとかえって
  //                      両群間の標本誤差でノイズが増え、「一致しない」ときに手法の限界か
  //                      標本のばらつきかを切り分けにくくなる。詳細は
  //                      project/相談_Atestは必要か.md と計画書 4.1・Q1・Q15。
  //                      あわせて視覚較正（群A′）の設計を2つ変えた。
  //                      ② **基準アニメの速さ2水準（RQ4）を4方式すべてに広げた**
  //                         （以前はフェードだけ）。1人あたりの問題数を増やさないため、
  //                         1人が担当する字を「4方式 × 2字」から
  //                         **「4方式 × 1字（8字のうち4字）× 2速度」**に変えた。
  //                         そのぶん1人がカバーする組合せは半分になるので、必要人数は倍になる。
  //                      ③ **進み具合の水準を8→5に減らした**（薄い側3水準＋20%＋100%）。
  //                         1人あたりのターゲット問題数は 80問 → **40問**、
  //                         1人あたりの合計は 134問 → **68問**（所要およそ8分→4〜5分）。
  //                         個人で多くの点を取るより、少ない点×多人数のほうが個人差が
  //                         平均化されて曲線がなめらかになる、という判断による。
  //   prov-2026-08-24c … **出題の設計を「出題範囲の学習」対策で全面的に作り直した**
  //                      （丸山決定・2026-08-24。外部の専門家への相談 →
  //                        project/相談_出題範囲の学習をどう防ぐか.md）。
  //
  //                      **なぜ変えたか。** 対象が8字しかないので、1人が多くの問題を解くうちに
  //                      「出てくるのは同じ8字だ」と気づく。これまでは「気づいても、当て推量で
  //                      答えるしかない床の付近しか動かない」と考えていたが、これは**誤り**である。
  //                      音声知覚の研究では、答えの候補集合（response set）の大きさそのものが
  //                      識別成績を変えることが知られており、8字だと知っていれば
  //                      「k系の音なら『か』しかない」という消去法が働いて、
  //                      **曲線の中間の正答率まで持ち上がる**。当て推量率の補正では直せない。
  //
  //                      ① **1人あたりの測定点を減らし、参加者間で分担する**（不完全ブロック配置）。
  //                         1人が担当するのは、本命1字につき**8点のうち3点**だけにした。
  //                         必要なのは個人ごとの曲線ではなく集団の識別率曲線なので、
  //                         「字 × 時点」のセルを参加者間で分ければよい。
  //                         どの3点を担当するかは**参加者の連番から決定的に決める**ので、
  //                         途中で閉じて再開しても同じ割付に戻る（assignment.point_subsets）。
  //                      ② **偽のターゲット（decoy）12字を、本命8字とまったく同じ回数
  //                         （各3回）出す**。まぎれ字の比率を上げるだけでは足りない。
  //                         本命8字が各3回出るのに、まぎれ字が60字から広く選ばれて
  //                         各0〜1回しか出ないと、**出現頻度の差**で
  //                         「この8字だけ何度も出る」と見抜かれる
  //                         （人は明示的に数えなくても出現確率を拾う）。
  //                         **よく出る20字がどれも同じ3回**なので、
  //                         **出現頻度から本命8字を見分けることが原理的にできない**。
  //                         完全に見抜かれても絞れるのは 1/20（5%）までである
  //                         （本命8字だけが繰り返される設計なら 1/8＝12.5%）。
  //                         本命だけ4回にすると「4回出る字が本命」と分かるので、
  //                         **本命も decoy も3回でそろえる**ことが要である。
  //                      ③ **decoy 12字は参加者ごとに変える**（「ん」を除く59字から選ぶ）。
  //                         個人から見れば「20字が繰り返し出る」構造に見えるが、
  //                         集団を横断して見ると多くの字が出るので、データを集めても
  //                         本命がどれかを読み取りにくい。参加者どうしが情報を交換しても、
  //                         見えている頻出集合が人によって違う。
  //                      ④ **同じ字を近接させない**（design.min_same_char_gap）。
  //                      ⑤ **学習の検出を強める**。回答が本命8字／頻出20字に入る割合を
  //                         試行順に追えるよう resp_in_target_set・resp_in_frequent_set 列を
  //                         足し、最後に awareness の質問（繰り返しに気づいたか・
  //                         どの字だと思ったか）を2問置いた。
  //                      1人あたりの問題数は3集団とも **70問**
  //                      （本命24＋decoy36＋確認問題10）。聴覚 108問→70問・
  //                      視覚較正 68問→70問・視覚検証 94問→70問。
  //                      **3集団とも同じ出題構造**になったことが重要である
  //                      （計画書 4.3「測定文脈をそろえる副次効果」）。
  //   prov-2026-08-24d … **聴覚刺激の切り出しの不具合を直し、打ち切り時点を実測の
  //                      モーラ長に合わせ直した**（2026-08-24）。
  //                      ① キャリアフレーズ「字＋ん」からの切り出しが 68字中62字で失敗し、
  //                         onset＋260ミリ秒で機械的に切っていたため、**末尾に「ん」が
  //                         残ったまま**だった（長い打ち切り条件と全長条件で「か」が
  //                         「かん」に聞こえる）。探索の開始を「onset＋260ミリ秒以降」から
  //                         **母音の音量ピークの直後**に変え、判定を鼻音そのものの性質
  //                         （0〜500Hz が優勢・1.5〜5kHz がほぼ消える）に変えて作り直した。
  //                         スクリプト: experiment/tools/crop_carrier_nasal.py
  //                         音源: experiment/tts_candidates_carrier/あみたろ_cut
  //                      ② その結果 1モーラは onset から 116〜260ミリ秒しかないと分かったので、
  //                         打ち切り時点をその中に収まるよう詰め直した（26字）。
  //                         点の数は全字8点（7点＋全長）のままで、頻度差ゼロの設計は不変。
  //                      ③ どの字も最短の点を10ミリ秒にした（S字曲線の床を取るため）。
  //                      ⚠ **この版は失敗している。** 人が耳で聞いて「り」「い」に「ん」が
  //                        残っていると分かり、調べ直すと 68字中34字で残っていた（下の 24e）。
  //   prov-2026-08-24e … **キャリアフレーズの後続音を「ん」から「ぱ」に変えた**（2026-08-24）。
  //                      ① 後続が「ん」（鼻音）だと母音から地続きに移るので境目が無く、
  //                         切り出しに2回続けて失敗した。**68字中34字で《ん》が残っており**、
  //                         とくに さ・し・す・せ・そ は1ミリ秒も落ちていなかった。
  //                         母音の種類ごとにしきい値を変える必要があったことが破綻の元である
  //                         （経緯と実測値は project/刺激作成_キャリアフレーズ.md）。
  //                      ② 後続を無声破裂音「ぱ」にすると、母音のあとに**閉鎖区間**
  //                         （唇を閉じてほぼ無音になる区間）が来る。音量が落ちるだけの
  //                         現象なので、**母音の種類によらない全字共通のしきい値**で境目が
  //                         決まる。68字すべてで閉鎖を検出できた（深さ 29〜46dB）。
  //                         スクリプト: experiment/tools/build_carrier_takes.py
  //                                     experiment/tools/crop_carrier_stop.py
  //                                     experiment/tools/normalize_takes.py
  //                                     experiment/tools/build_gate_ladders.py
  //                         音源: experiment/tts_candidates_carrier2/norm
  //                      ③ モーラ長は 80〜167ミリ秒（中央100）。「ん」版（116〜260）より
  //                         短いのは、「あん」が1つの重い音節になって母音が伸びていたためで、
  //                         「あぱ」の側が普通の話し言葉に近い。打ち切り点はこの長さに
  //                         収まるよう字ごとに引き直した。点の数は全字8点（7点＋全長）の
  //                         ままで、頻度差ゼロの設計は不変。
  //                      ④ **し・す・ち・つ の4字は母音が無声化する**（無声子音＋狭母音＋
  //                         無声子音の並びで起きる、日本語として自然な現象）。話速を落としても
  //                         アクセント核を置いても止まらなかった。理由と判断は記録文書にある。
  //   prov-2026-08-25a … **キャリアフレーズの後続音を「ぱ」から「さ」に変え、刺激544本を
  //                      作り直した**（2026-08-25）。
  //                      ① 後続を さ／ぱ／た／あ の4通りで68字ぶん作って実測した
  //                         （experiment/tools/compare_followers.html）。「さ」が全項目で最良:
  //                           境目の検出 68/68・母音の無声化 2字・**末尾に後続の残り 0字**
  //                         （「ぱ」は残り31字、「た」は16字、「あ」は検出 0/68）。
  //                         しかも本命8字に「さ」が入っていないので同音の重複もない。
  //                         ⭐ 論文に書ける根拠: 「さ」版（高域ノイズの立ち上がりで検出）と
  //                           「ぱ」版（無音区間で検出）は**まったく別の原理**で境目を測ったのに、
  //                           モーラ長が中央7msしかずれず、40ms以上ずれる字は0字だった。
  //                         スクリプト: experiment/tools/crop_carrier_fricative.py
  //                         音源: experiment/tts_candidates_carrier2/norm_さ
  //                      ② **対象モーラのアクセント核は「あり」（accent=1）で確定。**
  //                         「りさ」が「いさ」に聞こえる件で核を外す案（accent=0）を試したが、
  //                         核を外すと第1モーラが短くなり（中央104ms→78ms）、**68字中34字が
  //                         切り出しの下限に張り付いて21字で「さ」の摩擦が末尾に残った**。
  //                         その中に**本命の「か」「つ」が入っていた**（末尾の高域割合 0.72／0.97）。
  //                         「り」「ろ」は本命8字ではないので、この取引は成り立たない。
  //                         判定は丸山さんが耳で行った。
  //                         検証ページ: experiment/tools/compare_target_accent.html
  //                                     experiment/tools/check_sa_acc1.html
  //                      ③ **偽ターゲット12字の選び方を「参加者ごとに独立な純ランダム」に変えた**
  //                         （丸山判断）。それまでは5人で候補を一巡する系統的な配り方だった。
  //                         12字×3回で頻度をそろえる設計そのものは不変で、頻度差ゼロは保たれる。
  //                         ⚠ 実装では Math.random() を使わない（途中再開で12字が変わるため）。
  //                      ④ 打ち切り時点を「さ」版の実測モーラ長で引き直した（77〜164ms・中央104）。
  //                         点の数は全字8点（7点＋全長）のままで、頻度差ゼロの設計は不変。
  //                      ⑤ **「り」「ろ」の2字だけ、対象モーラの核を外した**（accent=0・丸山判断）。
  //                         あみたろの「り」は「い」に、「ろ」は「も」に聞こえる。弾き音が
  //                         母音に埋もれるためで、実測でも子音部は「ろ」6ms と全話者中で
  //                         最短の部類（つくよみちゃん15ms・人の録音11ms）。
  //                         この2字は**核を外しても切り出しがきれい**なことを確かめたうえで外した
  //                         （末尾の高域割合 0.014／0.008・しきい値0.15。68字全体でも漏れ0字）。
  //                         モーラ長は り104→94ms・ろ120→97ms に変わり、打ち切り点も引き直した。
  //                         ⚠ 字によって合成条件が違うことになるが、**り・ろ はどちらも本命8字では
  //                           なく**（偽ターゲットの候補で、回答は解析に使わない）、核の有無は
  //                           その字の中の声の高さの動きを変えるだけなので、測定値には影響しない。
  //                         設定は build_carrier_takes.py の TARGET_ACCENT_BY_KANA。
  //                      ⑥ **「び」だけ「あ＋び＋さ」の3モーラで合成し、前後の「あ」と「さ」を削った**
  //                         （丸山判断）。「び」も「い」に聞こえていた。語頭に破裂音が来ると
  //                         **その手前に音が無いので閉鎖（唇を閉じている区間）が現れようがなく**、
  //                         閉鎖が無ければ破裂も目立たない。前に母音を置くと閉鎖が
  //                         「前の母音からの音量の落ち込み」として現れる。子音部は 8ms → 31ms。
  //                         核を外す案は 8ms → 1ms と逆に悪化するので採らない。
  //                         設定は build_carrier_takes.py の LEAD_VOWEL_BY_KANA。
  //                         ⚠ **この字だけ onset の決め方が違う**（他は −58dBFS の立ち上がり、
  //                           び は「あ」との音量の谷＝閉鎖の始まり）。頭に無音を60ms足して、
  //                           前置き50msは全字でそろえてある。び は偽ターゲットの候補で
  //                           回答は解析に使わないので測定値には影響しない。
  //                         ⚠ **途中で見つけて直した失敗**: はじめは谷の10ms手前から切っていたが、
  //                           そうすると**最短の打ち切り条件(10ms)が母音「あ」の断片**になり、
  //                           び だけ −19.8dB と最大クラスの音量になっていた（ば −28.0・ら −39.4）。
  //                           最短条件で「あ」と答えられて曲線の床が壊れる。谷ちょうどで切るよう
  //                           直し、−27.7dB（68字中45番目）に収まった。
  //                           **刺激を変えたら、いちばん長い条件だけでなく最短条件も確かめること。**
  config_version: "prov-2026-08-25a",

  // =======================================================================
  // ⚠⚠ 掲載前フラグ ⚠⚠  **募集サイトへ掲載申請する直前に false にする**
  // =======================================================================
  // true のあいだは、本番モード（URL に ?prod=1 が付いた状態）で動かしても、
  // **名簿・回答・見え心地・完走レコードのすべてに is_test = true が付く**。
  // あわせて、名簿の連番（何人目か）は本番とは別のカウンタから配る
  //   本番のカウンタ  transfer_counters/calib
  //   テストのカウンタ transfer_counters/calib__test   ← true のあいだはこちら
  // ので、動作確認で本番の連番を食いつぶさない（＝聴覚と視覚の交互の振り分けが
  // ずれない）。
  //
  // なぜ要るのか。2026-08-21 の動作確認で、URL から ?group= が落ちて本番モード扱いに
  // なり、名簿（transfer_roster）に本物の行が1件混ざったうえ、本番の連番を1つ
  // 消費してしまった（transfer_roster/calib_anon-eiq54sm9）。回答には is_test 列が
  // あったが名簿には目印が無く、あとから見分けられなかった。参加者IDの頭が
  // curltest- / uitest- かどうかだけを目印にしていると、こういう「うっかり」を拾えない。
  //
  // ⚠ **false にし忘れると、本物の参加者のデータが全部テスト扱いになる。**
  //   解析の既定（export_transfer_firestore.py / analyze_transfer.py）は
  //   is_test の行を外すので、集計が 0 件になって初めて気づくことになる。
  //   掲載前チェックリストの最初の項目（0-1）で、**配信中のファイル**を見て
  //   false であることを確かめること。手元のファイルではなく実物を見る:
  //     curl -s https://kana-task.web.app/transfer_config.js | grep -n "pre_launch"
  //     # → pre_launch: false,  でなければ掲載しない
  //
  // 逆に、掲載が終わって追加の動作確認をするときは true に戻してよい
  // （本物のデータに混ざらない）。戻したら ?v= を上げて配信し直すこと。
  //
  // ■ 2026-08-24: **false にした（掲載可）。** 外す前に、それまでの試し打ちデータを
  //   一掃してある——Firestore の全データを
  //   project/pilot_data/firestore_before_launch/ に CSV で控えたうえで、
  //   purge_transfer_firestore.py --yes --reset-counters で366行を削除し、
  //   採番カウンタ6本（本番3フェーズ＋試し打ち3フェーズ）を0に戻した。
  //   印の付いていなかった1行（transfer_roster/calib_anon-eiq54sm9。2026-08-21 の事故）は
  //   --mark-test で印を付けてから消した。
  //
  // ■ 2026-08-24（同日の2度目）: **true に戻した。** 配信先を Firebase Hosting
  //   （https://kana-task.web.app）へ移す作業のためで、掲載はまだしていない。
  //   聴覚刺激の「ん」が残る問題の作り直し待ちでもある。
  //   新しい配信先が誰でも開ける状態になるので、false のままだと、
  //   URL を踏んだ人の記録が本物として保存され、本番の連番も進んでしまう。
  //
  //   ⚠ **掲載するときは false に戻す。** 戻し忘れると本物の参加者のデータが
  //     全部テスト扱いになる。忘れないよう、配信用のファイルを作る
  //     experiment/tools/build_hosting.sh は、true のままだと**その場で止まる**
  //     ようにしてある（動作確認のときだけ ALLOW_PRELAUNCH=1 を付けて迂回する）。
  //     false に戻したら ?v= を上げて配信し直すこと。
  pre_launch: true,

  // ---- 入口のページと集団の振り分け --------------------------------------
  // 参加者の集団は4つ(acal・aprime・b・c)で、入口URLは3つある。
  // 較正フェーズだけは2集団が同時期に走るので、1つの入口にまとめ、
  // 参加者IDごとにどちらの集団かをサーバが決める(クラウドソーシングでは、
  // 別々の掲載どうしで「片方にしか参加できない」を強制できないため)。
  //   transfer_calib.html   → phase "calib"   → acal(聴覚) か aprime(視覚)
  //                            並び順は「この人が何番目か」を2で割った余りで選ぶので、
  //                            2集団の人数が釣り合う。
  //   transfer_test.html    → phase "test"    → b(視覚)。集団は1つだけ
  //   transfer_comfort.html → phase "comfort" → c(見え心地)。集団は1つだけ
  //
  // ■ 2026-08-24: 検証フェーズから**検証用の音声集団(atest)を外した**。
  //   これで test は集団が1つになり、振り分けは起きない(連番だけ配る)。
  //   外した理由は config_version の prov-2026-08-24b の項に書いた。
  //
  // ■ 2026-08-24: 較正フェーズは**掲載を2本に分けた**ので、実際には自動振り分けを使わない。
  //   群Acal（聴覚・約11.5分・250円）と群A′（視覚・約4〜5分・130円）で謝礼が違うのに、
  //   募集サイトの掲載は1本につき金額が1つしか付けられないためである。
  //   入口ページ側で集団を決め打ちする（transfer.js の FORCED_GROUP）。
  //     transfer_calib_audio.html  … force_group: "acal"
  //     transfer_calib_visual.html … force_group: "aprime"
  //   **人数の比（50人 : 100人）は掲載の募集人数で決める。**
  //
  //   下の並びは (a) 振り分け版 transfer_calib.html（研究者の動作確認用）で使うのと、
  //   (b)「このフェーズにありうる集団の一覧」として決め打ちの妥当性を検査するのに使う。
  //   決め打ちページは、ここに名前が無い集団を指定しても効かない
  //   （check_transfer_stimuli.js が機械で見ている）。
  phases: {
    calib: ["acal", "aprime"],
    // 検証フェーズは群Bだけ。集団が1つなので振り分けは起きず、連番だけ配る。
    test: ["b"],
    // 群C(見え心地)も集団が1つしかないので、振り分けは起きない。連番だけ配る。
    comfort: ["c"],
  },

  // ---- 重複参加を断る規則 --------------------------------------------------
  // 「このフェーズに来た人が、**過去にどのフェーズに出ていたら**断るか」の表。
  // 4つの集団(acal・aprime・b・c)は互いに独立でなければならないので、
  // 同じ人が2つに出ると比較が濁る。
  //
  // 3フェーズ(calib / test / comfort)の走る順は次のとおりで、規則もそれに沿う。
  //   1. 較正フェーズ(calib) … 一番先に単独で走る。**断る相手がいないので空**
  //   2. 検証フェーズ(test) と 群C(comfort) … 較正が終わってから**同時期に並行**で走る
  // したがって test と comfort は、較正フェーズ経験者を断り、さらに
  // **同時期に走る互いを、どちら向きにも塞ぐ**(掲載前チェックリスト H-1)。
  //
  // reason はお断り画面の文言の出し分けに使う。GAS 版と同じ文字列にそろえてある
  // (transfer.js / transfer_comfort.js の blockedScreen が見ている)。
  phase_blocks: {
    calib: [],
    test: [
      { phase: "calib", reason: "already_in_calib" },
      { phase: "comfort", reason: "already_in_comfort" },
    ],
    comfort: [
      { phase: "calib", reason: "already_in_other_phase" },
      { phase: "test", reason: "already_in_other_phase" },
    ],
  },

  // ---- 保存基盤(Firestore / GAS)の選択 ------------------------------------
  // 名簿と記録の置き場所を切り替える。**どちらか一方が落ちても続けられる**ように、
  // 主で失敗したらもう一方を試す作りにしてある(fallback)。
  //
  // なぜ Firestore を主にするのか。GAS は応答の本体を script.googleusercontent.com へ
  // リダイレクトして返す作りで、この中継が一過性で失敗し、JSON ではなく HTML の
  // エラーページを返すことがある(2026-08-21 に実機で発生。サーバ自体は正常だった)。
  // Firestore は REST を直接叩くので中継が無く、書き込みの成否もその場で分かる。
  //
  // **切り戻し方**: backend.roster と backend.logging を "gas" にするだけでよい。
  // GAS 側のコードもデプロイもそのまま残してあるので、この2行で 2026-08-20 の構成に戻る。
  backend: {
    // 名簿(集団の振り分け・連番・重複チェック)をどちらに聞くか。"firestore" | "gas"
    roster: "firestore",
    // 回答と見え心地の記録をどちらへ送るか。"firestore" | "gas"
    logging: "firestore",
    // 主が(作り直しも含めて)全部だめだったとき、もう一方を試すか。
    // 名簿は「両方だめならお断り画面」、記録は「両方だめならその1問が失われる」。
    fallback: true,
    // 記録だけ、成功しても**もう一方にも同じものを送る**(二重書き込み)。
    // 移行の見極め期間だけ true にして、両方に同じ件数が入ることを確かめるのに使う。
    // 常用はしない(GAS の同時実行30本の上限に当たるため)。
    dual_write_logging: false,
  },

  // Firebase(Firestore)の接続先。
  // プロジェクトは 2026-08-21 に firebase CLI で作った(ifont-transfer / 東京)。
  //
  // ⚠ api_key を**リポジトリに入れてよい理由**: Firebase のウェブAPIキーは、
  //   設計上ブラウザに埋め込んで公開するものである。これは「秘密の合言葉」ではなく
  //   「どのプロジェクト宛てか」を示す宛名にすぎない。守るのは
  //   firebase/firestore.transfer.rules のほうで、いまのルールは
  //   「作れるだけ・回答は読めない・書き換えられない・消せない」に絞ってある。
  //   (GAS の DUMP_TOKEN のような、本来秘密であるべき値とは性質が違う。)
  firestore: {
    enabled: true,
    project_id: "ifont-transfer",
    api_key: "AIzaSyB87BFzuwRHtCRJVhjilYjjJwM0KfYRgio",
    // 1回あたりの待ち上限ms。Firestore は東京(asia-northeast1)に置いたので、
    // GAS の実測約6秒に比べてはるかに速い(実測 0.1〜0.3 秒)。
    // それでも回線が細い端末を考えて10秒とってある。
    timeout_ms: 10000,
  },

  // 参加者名簿(サーバ)。集団の割り当てと、フェーズをまたいだ重複参加の照合に使う。
  // status_url は GAS ウェブアプリの /exec URL。
  // **backend.roster が "firestore" のときは使わない**(切り戻し用に残してある)。
  // 空にすると、参加者IDのハッシュから決める代用の割り当てになる(重複の照合はできない)。
  // サーバ側のコードは gas_transfer/(gas/code.gs に gas/transfer_patch.md を当てたもの)。
  roster: {
    // 2026-08-20 にデプロイした**下見用**の窓口。本番は別のスプレッドシートで
    // 作り直して差し替える(掲載前チェックリスト G-1)。
    status_url: "https://script.google.com/macros/s/AKfycbw1bnn7H3HCYuD3fDUIi1djGcnea0cVqnA15XiN5ipgwlrsCNkQx4q16-bdN0wbGQRP/exec",
    // 1回あたりの待ち上限ms。**GAS の応答は実測でおよそ6秒かかる**
    // (2026-08-21 に本番URLから計測)。8秒だと少しの揺らぎで間に合わず、
    // サーバが生きているのに「時間切れ」として失敗にしていた。余裕を持たせる。
    timeout_ms: 15000,
    // 問い合わせの作り直し。GAS はときどき、一過性の理由で JSON ではなく
    // HTML のエラーページを返したり、応答しなかったりする。1回で諦めると、
    // サーバ自体は動いているのに参加者をお断りしてしまう(2026-08-21 に実機で発生)。
    // そこで間隔を空けて何度か試し、**全部だめだったときだけ**お断りにする。
    //   delays_ms … 何ミリ秒待ってから次を試すか。attempts より短ければ最後の値を使い回す
    //   jitter_ms … 待ち時間に足す散らし幅の上限。皆が同時に再挑戦して混むのを防ぐ
    retry: {
      attempts: 3,
      delays_ms: [800, 2000, 4000],
      jitter_ms: 400,
    },
    // true にすると、名簿サーバに繋がらないときは課題を始めさせない(お断り画面)。
    // false なら、割り当てだけ手元で決めて続行し、重複はあとからデータで除外する。
    // 2026-08-20 に true へ。false のままだと、サーバが落ちているあいだに来た人が
    // 参加者IDのハッシュで勝手に集団を決めて進んでしまい、音声と視覚の人数の釣り合いが
    // 崩れるうえ、フェーズをまたいだ重複参加も止められないため。
    require_server: true,
  },

  // ---- ターゲット字（本命8字） --------------------------------------------
  // **確定・変更禁止**(2026-08-20 丸山。計画書 4.3 の決定)。
  // 日本語モーラの主な子音の出し方を1つずつ入れた層化構成:
  //   あ=母音のみ / か=無声の破裂 / が=有声の破裂(かとの清濁の対) /
  //   ぱ=両唇の無声破裂(かとの調音位置の差) / し=こすれる音(摩擦) /
  //   つ=破裂+摩擦(破擦) / ま=鼻から抜ける音(鼻音) / ら=舌ではじく音。
  // 選定は「音の種類 + 録音の品質チェック」だけで決めており、曲線や
  // 「1次変換で合わせきれない度合い」を見てから差し替えることはしない(選択の偏りを防ぐため)。
  targets: ["あ", "か", "が", "ぱ", "し", "つ", "ま", "ら"],

  // ---- decoy（偽のターゲット） ---------------------------------------------
  // **2026-08-24 に、それまでの「まぎれ字を毎回ちがう字から選ぶ」方式を捨てて置き換えた。**
  //
  // ■ なぜ「まぎれ字を増やす」だけでは足りないのか
  //   まぎれ字を60字から毎回ちがう字で選ぶと、本命8字が各3回出るのに、まぎれ字は
  //   1字あたり0〜1回しか出ない。人は明示的に数えなくても**出現頻度の差**を拾うので、
  //   「この8字だけ何度も出てくる」という構造がそのまま見えてしまう。
  //   全68字を等しい頻度にするには非ターゲットが88%必要で、試行数として成り立たない。
  //
  // ■ decoy 方式（1人あたり12字・各3回）
  //   偽のターゲットを12字選び、**本命8字とまったく同じ回数（各3回）**出す。
  //   参加者から見える「よく出る字」は 8＋12＝**20字**で、頻度はその20字で等しい。
  //   → **出現頻度から本命8字を見分けることが原理的にできない。**
  //     （本命だけ4回にすると「4回出る字が本命」と分かってしまうので、回数は必ずそろえる。）
  //   出題範囲を完全に見抜かれても、絞り込めるのは 1/20（5%）までである
  //   （本命8字だけが繰り返される設計なら 1/8＝12.5%）。
  //   decoy の回答は**解析には使わない**（記録の is_decoy 列で外す）。
  //
  // ■ **12字は参加者ごとに変える**（丸山の発案・2026-08-24）
  //   全参加者で同じ12字に固定せず、候補59字から**参加者の連番ごとに違う12字**を配る。
  //   ・個人から見れば「20字が繰り返し出る」構造に見える
  //   ・集団を横断して見ると多くの字が出るので、データを集めても本命が浮き上がらない
  //   ・参加者どうしが情報を交換しても、見えている頻出集合が人によって違う
  //   選び方は transfer.js の decoyChars()。
  //   **2026-08-25 に「参加者ごとに独立な純ランダムの12字」に変えた**（丸山判断）。
  //   それまでは「候補を巡ごとに並べ替えて12字ずつ配り、5人で候補をひととおり使い切る」
  //   という系統的な配り方だった。集団全体で均等に使われる利点はあったが、
  //   **decoy の回答はそもそも解析に使わない**ので均等である必要がなく、
  //   純ランダムのほうが論文で説明しやすい。
  //   ⚠ ただし実装では Math.random() を使わず、**連番を種にした並べ替え**で選ぶ。
  //     この関数はセッション中に5か所から呼ばれ、かつ**途中再開に対応する**必要が
  //     あるため、同じ人には何度呼んでも同じ12字が返らなければならない。
  //     （理由の詳細は transfer.js の decoyChars() の注記）
  //
  // ■ 候補の作り方（**混同しやすい字を外す設計は採らない**・2026-08-24 丸山決定）
  //   候補 = 全68かな − 本命8字 − exclude（「ん」だけ）＝ **59字**。あとは完全にランダム。
  //   1. **混同が起きること自体が測定の対象である。** 「ら」を「な」と答えるのは、
  //      その時点で届いている情報が「鼻音か弾き音か」の区別までしかないことを示していて、
  //      識別率曲線の一部として意味を持つ。似た字を候補から外すと、これが測れなくなる。
  //   2. **偽の字を「本命と似ていないもの」に限ると、簡単に分かる字ばかりが偽物になり、
  //      かえって本命が浮き上がる。** ランダムのほうが構造を読み取れない。
  //   3. 層化も除外もしない単純な設計なので、論文でも説明しやすい。
  //   「ん」だけ外すのは、単独で提示したときの性質が他の字と大きく違うためである。
  //
  // ■ 打ち切り時点は本命字とそろえる
  //   decoy になりうる59字すべてに、音の種類が同じ本命字と**同じ打ち切り時点の並び**を
  //   割り当ててある（下の audio.gates_ms）。「偽物だけ長くて簡単」という偏りを作らない。
  //
  // ⚠ ここを変えたら、聴覚の打ち切り済みWAVを作り直すこと
  //   （build_transfer_gates.py → 掲載前チェックリスト D-2）。
  decoys: {
    // 1人あたり何字を偽のターゲットにするか。
    per_participant: 12,
    // 1字あたり何回出すか。**design.target_points_per_char と同じ値にすること**
    // （本命と回数がずれると、頻度で見分けられてしまう）。
    // 実際の回数は「その字の測定点のうち何点を担当するか」で決まるので、
    // ここは覚え書きの意味合いが強い。
    reps: 3,
    // 候補から外す字。
    exclude: ["ん"],
    // 候補を直接指定したいとき（空なら「全かな − 本命 − exclude」を自動で使う）。
    pool: [],
    // 巡ごとの並べ替えの種。変えると誰にどの字が当たるかが変わる。
    seed_prefix: "decoy-pool:",
  },

  // 回答に使うかな表。聴覚・視覚で同じ表を使う(計画書4.3)。
  // 空欄 "" は表の穴(五十音表の並びを保つための余白)。
  answer_grid: [
    ["あ", "い", "う", "え", "お"], ["か", "き", "く", "け", "こ"], ["さ", "し", "す", "せ", "そ"],
    ["た", "ち", "つ", "て", "と"], ["な", "に", "ぬ", "ね", "の"], ["は", "ひ", "ふ", "へ", "ほ"],
    ["ま", "み", "む", "め", "も"], ["や", "", "ゆ", "", "よ"], ["ら", "り", "る", "れ", "ろ"],
    ["わ", "", "", "", "ん"],
    ["が", "ぎ", "ぐ", "げ", "ご"], ["ざ", "じ", "ず", "ぜ", "ぞ"], ["だ", "", "", "で", "ど"],
    ["ば", "び", "ぶ", "べ", "ぼ"], ["ぱ", "ぴ", "ぷ", "ぺ", "ぽ"],
  ],
  // 表を2段に折り返す位置(上の段に何行入れるか)。見た目だけの設定。
  answer_grid_split_at: 10,
  // 同じ音になる字は1つのボタンに併記する(聴覚で区別できないため)。
  homophone_label: { "お": "お／を", "じ": "じ／ぢ", "ず": "ず／づ" },

  // ---- 聴覚課題(群Acal のみ) ----------------------------------------------
  // 2026-08-24 に検証用の音声集団(群Atest)を廃止したので、この設定を使うのは
  // 較正フェーズの群Acal だけになった。
  audio: {
    // 使用許諾のクレジット表記(COEIROINK本体の規約で必須。→653行目付近の注記)。
    // **2026-08-25 追加。** この値は掲載文・実験ページ双方のクレジット表示が読む「正」の場所にする。
    // 同じ文字列が次の2か所にも手書きで存在するので、変えるときは3か所とも揃えること
    // (2026-08-25時点でどちらも "COEIROINK:あみたろ" で一致していることを確認済み):
    //   - experiment/tts_candidates_carrier2/synth_meta.json の "credit"
    //   - experiment/tools/build_carrier_takes.py の CREDIT 定数
    credit: "COEIROINK:あみたろ",
    // 打ち切り時刻。**音の実体の開始(acoustic onset)を0msとした絶対時間**。
    //
    // ■ 2026-08-21 の変更: 全字で同じ7時点を使うのをやめ、**字ごとに時点を置く**
    //   ようにした（丸山判断。計画書 4.3・10章A2②）。
    //
    //   理由。Furui(1986) の日本語CV音節の打ち切り実験では、識別に必要な区間の長さが
    //   子音の種類でかなり違うと報告されている——破裂音・鼻音は 50〜70ms（破裂の
    //   約10ms前から）、無声摩擦音は平均 120ms（声の始まりの100ms前から）。
    //   下見（project/パイロット分析_20260821.md）もこれと同じ向きで、
    //   「あ」「が」は 20ms で、破裂系は 40ms で、「つ」は 60ms で、「ら」は 110ms で
    //   すでに天井に張り付いていた。全字に同じ並びを当てると、早く天井に着く字では
    //   上のほうの時点が、遅い字では下のほうの時点が、どちらも情報を持たない点になる。
    //
    //   **絶対時間ms である点は変えない。** 字をまたいだ比較（曲線どうしの距離E）も、
    //   生成の逆引き（音の時刻 → アニメの進み具合）も、どちらも絶対時間の上で行うので、
    //   「その字の全長の何%」のような相対の置き方にすると両方が成り立たなくなる。
    //
    //   ここに書いてあるのは**暫定の配置**である。下見で全域を粗く測り、
    //   **A2 で各字の配置を確定する**。確定したらこの表を書き替えて、
    //   打ち切りWAVを作り直すこと（掲載前チェックリスト D-2）。
    //
    // ■ 書き方
    //   _default … 表に名前の無い字に使う共通の並び（いまは全68かなを書き出したので出番は無い）。
    //   "<かな>" … その字だけの並び（共通の並びを上書きする）。
    //   ※ 字ごとの上書きは **この gates_ms の中に書く**。`gates_ms_by_char` のような
    //     別のキーは作らない（同じ表が2か所に分かれると必ず食い違うため）。
    //   ※ 「打ち切りなし(全長)」は下の include_full_gate で足す。ここには書かない
    //     （このファイルは Python 側も読む。数と文字列が混ざると生成が止まる）。
    gates_ms: {
      // 表に名前の無い字に使う共通の並び。**2026-08-24 からは全68かなを下に
      // 書き出したので、この _default は実際には使われない**（安全側の保険として残す）。
      _default: [20, 40, 60, 80, 110, 150, 220],

      // ---- 全68かなの並び（2026-08-25 に、後続音「さ」＋アクセント核あり の音源で引き直した） ----
      // 形は音の種類ごとに3通り（はやい／まさつ／なかま）。その形のまま、
      // **その字のモーラ長に収まるよう比例で配ってある**。最短は必ず10ms（床）、
      // 最長はモーラの終わりから15ms内側。作り直すときは
      //   python3 experiment/tools/build_gate_ladders.py --src experiment/tts_candidates_carrier2/norm_さ
      // の出力をここへ貼る（手で数字をいじらないこと）。
      "あ": [10, 20, 30, 35, 50, 65, 90],   // はやい・モーラ104ms
      "い": [10, 15, 20, 25, 35, 45, 60],   // はやい・モーラ77ms
      "う": [10, 15, 20, 25, 35, 45, 60],   // はやい・モーラ77ms
      "え": [10, 20, 25, 35, 45, 60, 85],   // はやい・モーラ100ms
      "お": [10, 15, 20, 25, 35, 45, 60],   // はやい・モーラ77ms
      "か": [10, 20, 25, 35, 45, 60, 85],   // はやい・モーラ100ms
      "き": [10, 15, 25, 30, 40, 50, 70],   // はやい・モーラ87ms
      "く": [10, 15, 20, 25, 35, 45, 60],   // はやい・モーラ77ms
      "け": [10, 20, 25, 35, 45, 55, 80],   // はやい・モーラ94ms
      "こ": [10, 20, 25, 35, 45, 60, 85],   // はやい・モーラ100ms
      "さ": [10, 40, 60, 75, 95, 120, 150],   // まさつ・モーラ164ms
      "し": [10, 20, 30, 35, 40, 50, 60],   // まさつ・モーラ77ms
      "す": [10, 30, 40, 50, 60, 75, 90],   // まさつ・モーラ107ms
      "せ": [10, 40, 55, 70, 85, 110, 135],   // まさつ・モーラ152ms
      "そ": [10, 40, 60, 75, 95, 120, 150],   // まさつ・モーラ164ms
      "た": [10, 20, 30, 40, 50, 65, 95],   // はやい・モーラ110ms
      "て": [10, 20, 25, 35, 45, 60, 80],   // はやい・モーラ97ms
      "と": [10, 20, 25, 35, 45, 60, 80],   // はやい・モーラ97ms
      "ち": [10, 25, 35, 45, 55, 75, 100],   // なかま・モーラ114ms
      "つ": [10, 20, 30, 35, 50, 65, 85],   // なかま・モーラ100ms
      "な": [10, 25, 35, 50, 70, 90, 125],   // はやい・モーラ142ms
      "に": [10, 25, 35, 50, 70, 90, 125],   // はやい・モーラ142ms
      "ぬ": [10, 20, 35, 45, 60, 80, 115],   // はやい・モーラ130ms
      "ね": [10, 25, 35, 50, 70, 90, 130],   // はやい・モーラ144ms
      "の": [10, 20, 35, 45, 65, 80, 115],   // はやい・モーラ132ms
      "は": [10, 30, 40, 50, 60, 75, 95],   // まさつ・モーラ110ms
      "ひ": [10, 25, 40, 50, 60, 70, 90],   // まさつ・モーラ104ms
      "ふ": [10, 25, 35, 45, 50, 65, 80],   // まさつ・モーラ95ms
      "へ": [10, 30, 40, 50, 60, 75, 90],   // まさつ・モーラ107ms
      "ほ": [10, 30, 40, 50, 60, 75, 90],   // まさつ・モーラ107ms
      "ま": [10, 25, 35, 50, 65, 85, 125],   // はやい・モーラ139ms
      "み": [10, 20, 35, 45, 60, 80, 115],   // はやい・モーラ130ms
      "む": [10, 20, 35, 45, 60, 80, 115],   // はやい・モーラ130ms
      "め": [10, 20, 35, 45, 65, 85, 120],   // はやい・モーラ137ms
      "も": [10, 25, 35, 50, 65, 85, 125],   // はやい・モーラ139ms
      "や": [10, 30, 40, 55, 75, 100, 130],   // なかま・モーラ144ms
      "ゆ": [10, 25, 30, 40, 55, 70, 90],   // なかま・モーラ107ms
      "よ": [10, 30, 40, 55, 70, 95, 125],   // なかま・モーラ142ms
      "わ": [10, 25, 40, 50, 65, 90, 115],   // なかま・モーラ132ms
      "ら": [10, 25, 35, 50, 65, 85, 110],   // なかま・モーラ127ms
      "り": [10, 20, 30, 35, 45, 60, 80],   // なかま・モーラ94ms
      "る": [10, 25, 35, 45, 60, 85, 110],   // なかま・モーラ124ms
      "れ": [10, 25, 30, 40, 55, 70, 95],   // なかま・モーラ110ms
      "ろ": [10, 20, 30, 35, 50, 65, 80],   // なかま・モーラ97ms
      "ん": [10, 15, 25, 30, 45, 55, 75],   // はやい・モーラ92ms
      "が": [10, 20, 25, 35, 45, 60, 85],   // はやい・モーラ100ms
      "ぎ": [10, 20, 25, 35, 45, 60, 80],   // はやい・モーラ97ms
      "ぐ": [10, 20, 25, 35, 50, 60, 85],   // はやい・モーラ102ms
      "げ": [10, 20, 25, 35, 50, 60, 85],   // はやい・モーラ102ms
      "ご": [10, 20, 30, 35, 50, 65, 90],   // はやい・モーラ104ms
      "ざ": [10, 25, 35, 45, 60, 80, 105],   // なかま・モーラ122ms
      "じ": [10, 25, 30, 40, 50, 70, 90],   // なかま・モーラ104ms
      "ず": [10, 25, 35, 45, 55, 75, 100],   // なかま・モーラ114ms
      "ぜ": [10, 25, 30, 40, 55, 70, 90],   // なかま・モーラ107ms
      "ぞ": [10, 25, 35, 45, 60, 80, 100],   // なかま・モーラ117ms
      "だ": [10, 20, 30, 40, 50, 65, 95],   // はやい・モーラ110ms
      "で": [10, 20, 25, 35, 45, 60, 80],   // はやい・モーラ97ms
      "ど": [10, 15, 25, 30, 45, 55, 75],   // はやい・モーラ92ms
      "ば": [10, 15, 25, 30, 45, 55, 75],   // はやい・モーラ92ms
      "び": [10, 20, 25, 35, 50, 60, 85],   // はやい・モーラ102ms
      "ぶ": [10, 15, 20, 25, 35, 45, 60],   // はやい・モーラ77ms
      "べ": [10, 15, 25, 30, 45, 55, 75],   // はやい・モーラ92ms
      "ぼ": [10, 20, 25, 35, 45, 55, 80],   // はやい・モーラ94ms
      "ぱ": [10, 15, 25, 30, 40, 50, 70],   // はやい・モーラ87ms
      "ぴ": [10, 15, 25, 30, 40, 50, 70],   // はやい・モーラ85ms
      "ぷ": [10, 15, 25, 30, 40, 50, 65],   // はやい・モーラ82ms
      "ぺ": [10, 15, 25, 30, 45, 55, 75],   // はやい・モーラ92ms
      "ぽ": [10, 20, 30, 35, 50, 65, 90],   // はやい・モーラ107ms
    },

    // 各字の並びの最後に「打ち切りなし(全長)」を1点足すか。
    // 曲線の天井（その字を最後まで聞けば何%当たるか）を字ごとに押さえるためのもので、
    // 字ごとに時点を置くようにしたぶん、天井が測れているかを字ごとに確かめる必要がある。
    // ※ 確認問題A（全部聞かせの問題を数%混ぜる仕掛け）と同じ刺激を使うが、
    //   記録の check_kind 列で見分けられる。両方を残すか片方に寄せるかは A2 で決める。
    include_full_gate: true,
    // ---- 下見だけ: いちばん早い側に打ち切り時点をもう1つ足す --------------------
    // ねらい: **曲線の立ち上がりが 20ms より手前にあるか**を見る。下見では
    // 「あ」「が」が最も早い20msですでに全問正解で、早い側の解像度が足りていない
    // 疑いがある(project/パイロット分析_20260821.md の RQ1)。
    //
    // ⚠ **10ms の刺激は、実際に鳴っている音がごく短い。** 打ち切りの終端には
    // 5ms の余弦フェードが掛かる決まりなので、10ms の刺激では後半5msが
    // フェードで減衰していく区間になり、**そのままの大きさで鳴るのは頭の約5msだけ**である。
    // したがってこの水準は「**床がどこにあるかを見る**」ためのものであり、
    // **生成(warp)の材料には使わない**。詳しくは計画書 7.1。
    //
    // enabled を false にすると gates_ms だけになり、この仕掛けが無かった頃と
    // 完全に同じ挙動に戻る。
    //
    // ■ 2026-08-21 に false へ落とした。
    //   この仕掛けは「全字が同じ7時点」だった頃に、早い側の解像度を足すための
    //   応急処置だった。上の gates_ms を字ごとの配置に変えたことで、10ms が要る字
    //   （あ・か・が・ぱ・ま）にはその字の並びの中に 10ms が入っている。
    //   ここを true のままにすると、し（30msから始めたい字）にまで 10ms が足され、
    //   ほぼ何も聞こえない点に回答を1つ使ってしまう。
    pilot_extra_gates: {
      enabled: false,
      // 足す打ち切り時刻ms。既存の gates_ms と重ならない値にすること。
      gate_ms: [10],
    },

    // 事前生成した「打ち切り済みWAV」の置き場所と索引。
    // 生成は experiment/tools/build_transfer_gates.py。
    //
    // ⚠ 音源の切り替えは、この2行をまとめて書き換えるだけで行う
    //   （このファイルは「代入1つだけ」の制約があり、関数や条件分岐は使えないため。
    //    切り替えのたびに config_version を上げ、check_transfer_stimuli.js を通し直すこと）。
    //   候補:
    //     あみたろ（COEIROINK・切出版・2026-08-23採用・現行）:
    //       "transfer_audio_manifest_amitaro.json" / "transfer_stimuli_amitaro"
    //       クレジット表記必須: "COEIROINK:あみたろ"（あみたろ提供元は研究利用を歓迎する規約だが、
    //       COEIROINK自体の規約でクレジットは必須。生成音声を「誰でも二次利用可」として
    //       公開することは禁止 — 詳しくは project/合成音声_話者候補.md）
    //     花音（AivisSpeech・単独発話版・2026-08-23生成・不採用）:
    //       "transfer_audio_manifest_kanon.json" / "transfer_stimuli_kanon"
    //       息っぽさ・字ごとの音程のばらつき(225Hz)が大きく見送った。クレジットは任意(ACML 1.0)
    //     自然音声（丸山の肉声・2026-08-23まで使用）: "transfer_audio_manifest.json" / "transfer_stimuli"
    manifest_url: "transfer_audio_manifest_amitaro.json",
    stimuli_dir: "transfer_stimuli_amitaro",
    // 本番刺激(自然音声)が揃うまでの代用。manifest_url が読めないときだけ使う。
    // 既存の合成音プールを、ブラウザ側で同じ規則(onset起点・終端5msフェード)で
    // 切って鳴らす。**研究者の動作確認専用**で、本番データ取得には使わない。
    // 2026-08-20: 録音した自然音声の打ち切り済みWAVを experiment/transfer_stimuli/ に
    // 置いたので **false に落とした**。索引が読めないときは代用の合成音で静かに
    // 走り続けるより、その場で止まったほうが安全(気づかずに別の音でデータを
    // 取ってしまう事故を防ぐ)。合成音で試したいときだけ一時的に true に戻す。
    fallback: {
      enabled: false,
      dir: "audio_base",              // <かな>.mp3 が入っているディレクトリ
      ext: ".mp3",
      onsets_url: "audio1char_onsets.json",   // かな→acoustic_onset_ms
      default_onset_ms: 50,
    },
    // 打ち切り端のフェードアウト(計画書4.4-5)。t を終端とする 5ms の余弦ランプ。
    fade_out_ms: 5,
    // 合図音(無線イヤホンの頭欠け対策)。既存の1文字課題と同じ方式。
    beep_hz: 880,
    beep_ms: 80,
    beep_lead_ms: 600,     // 合図音 → 刺激再生 までの間
    end_gap_ms: 500,       // 刺激の終わり → 終了合図音
    end_beep_gap_ms: 140,

    // ---- 聞き取り確認（課題に入る前の関門）----------------------------------
    // **2026-08-25 に追加（丸山判断）。**
    //
    // ■ なぜ要るのか
    //   それまでの「音量の確認」は、**サンプルを鳴らすボタンを押しただけ**で
    //   先へ進めた。押しても**実際に聞こえたかを確かめていない**ので、
    //   iPhone の着信スイッチ（マナーモード）で音が消えている人がそのまま通過し、
    //   70問を無音のまま答えてしまう。**iOS は既定で Web Audio もサイレント
    //   スイッチで消す**ので、これは珍しい事故ではない。
    //   （その人は確認問題Aで除外されるが、**5分ぶんの時間と謝礼が無駄になる**。
    //     早く気づけるほうが参加者にとっても良い。）
    //
    // ■ なにをするか
    //   はっきり発音した数字を1つランダムに鳴らし、「どれだったか」を4択で聞く。
    //   **rounds 回続けて正解しないと先へ進めない。**
    //   外したら、その場でやり直せる（回数制限なし）。2回目からは
    //   マナーモード解除・音量を上げる、の案内を大きく出す。
    //
    // ■ なぜ数字なのか
    //   ・本命8字と無関係なので、刺激の情報が漏れない
    //     （それまでのサンプルは**本番の刺激そのものを5字ぶん**聞かせていた）
    //   ・4つが互いにはっきり違う音なので、音量が足りているかの判定として素直
    //   ・話者は本番と同じあみたろなので、音量の基準としても正しい
    //   ・「し(4)」ではなく「よん」にしてある（「し」は本命8字にあり、「ひ」と紛れる）
    //
    // ■ まぐれ通過の確率
    //   1回だけなら 1/4＝25% 通ってしまう。**2回続けて**求めれば 1/16＝6%。
    //   残り6%も確認問題A（6問中4問以下で除外）で落ちるので、二重の網になる。
    check: {
      dir: "audio_check",              // 音の置き場（build_audio_check.py が作る）
      // 表示 → 読み。ここを変えたら build_audio_check.py の DIGITS も合わせること。
      digits: { "1": "いち", "2": "に", "3": "さん", "4": "よん" },
      rounds: 2,                       // 何回続けて正解したら通すか
    },
  },

  // ---- 視覚課題(群A′ / 群B) ----------------------------------------------
  visual: {
    size_px: 256,            // 提示キャンバスの一辺
    base_dir: "base",        // <かな>.png (256px・黒文字白背景)
    fix_ms: 400,             // 注視点 ＋ の時間

    // ---- 見え方確認（課題に入る前の関門）------------------------------------
    // **2026-08-25 に追加（丸山判断）。聞き取り確認（audio.check）の視覚版。**
    //
    // ■ なぜ要るのか
    //   それまでの「見え方の確認」は、見本の字を出して**ボタンを押すだけ**だった。
    //   画面が暗い・小さい・画像が読み込めていない、といった状態でも通過できる。
    //   聴覚と同じ理由で、**実際に見えているかを確かめる関門**にする。
    //
    // ■ なにをするか
    //   **打ち切らずに全部表示した字**を1つ出し、「どれだったか」を4択で聞く。
    //   rounds 回続けて正解しないと先へ進めない。
    //   全部表示なら画面が正常なら必ず読めるので、外す＝環境に問題がある、と分かる。
    //
    // ■ 使う字
    //   **本命8字は使わない**（見本として見せると「これが本命だ」と教えてしまう）。
    //   その参加者の decoy 12字も避ける（出題の範囲を先に見せないため）。
    //   残り（68字 − 本命8 − decoy12 − 「ん」）から選ぶ。
    //   ⚠ それまでの見え方確認は見本に「あ」を出していた。**「あ」は本命8字である。**
    check: {
      rounds: 2,               // 何回続けて正解したら通すか
      n_choices: 4,            // 選択肢の数
    },
    fix_jitter_ms: 300,      // 注視点の追加ゆらぎ上限(先読み防止)
    // 基準アニメの長さ。等速モード(群A′)で進み具合 s が 0→1 になるまでの時間。
    // 旧製品由来の200msではなく、下見で決める設計項目(計画書 Q4)。
    //
    // ■ 2026-08-24 に 600ms → **300ms** へ変更した（丸山判断）。
    //   理由は、群B（検証フェーズの視覚）の対照条件が構造的に不利になっていたことである。
    //   群Bは音の打ち切り時刻に合わせて表示を打ち切るので、いちばん遅い打ち切りでも
    //   **220ms** で止まる。基準アニメが600msだと、対照1（等速 s = t / base_anim_ms）は
    //   220ms の時点でまだ **進み具合 37%** までしか進んでいない。つまり対照は
    //   「どの時刻でも字が薄すぎて読めない」状態で提案手法と比べられることになり、
    //   仮説1が勝って当たり前の比較になってしまう。
    //   300ms にすると 220ms で **進み具合 73%** まで進むので、対照にも
    //   「最後のほうならほぼ読める」余地が残り、公平な比較になる。
    //   （打ち切り時刻ごとの対照1の進み具合: 20ms→7% / 40ms→13% / 60ms→20% /
    //     80ms→27% / 110ms→37% / 150ms→50% / 220ms→73%。600msのときは
    //     それぞれ 3% / 7% / 10% / 13% / 18% / 25% / 37% だった。）
    //   なお群C（見え心地）もこの値をアニメ1本の長さに使うので、
    //   字と字の開始の間隔が 900ms → 600ms に縮む（干渉が起きる帯域200ms以下の3倍。
    //   experiment/transfer_comfort_config.js の timing の注記を見ること）。
    base_anim_ms: 300,

    // ---- 群A′(視覚較正)の打ち切り水準 s% -----------------------------------
    // ここに書いてあるのは**ずらし量0の人（連番が3で割り切れる人）の並び**である。
    // 実際に出る水準は、下の progress_pct_shift によって参加者ごとに少しずれる。
    //
    // ■ 2026-08-24 に [6, 13, 22, 34, 50, 70, 100]（＋下見だけの3%）から
    //   **[1, 2.5, 4, 5.5, 7, 8.5, 20, 100] へ全面的に置き換えた**（丸山判断）。
    //
    //   **なぜ変えたか。** 先生のパイロット（kurihara-pilot2・視覚較正134問・
    //   project/pilot_data/firestore/analysis/summary.md）で、
    //   **13%以上の6水準がほぼ全問正解**になり、8水準のうち6水準が
    //   「曲線の形について何も教えてくれない点」になっていた。方式ごとに見ても同じで、
    //   4方式すべてが 6% 以上でほぼ天井に張り付いていた（正答率: フェード 3%→25%・
    //   6%以上→100%／部分表示 3%→50%・6%以上→100%／ぼかし 3%→75%・6%以上→100%／
    //   ワイプ 3%→67%・13%以上→100%）。
    //   S字の曲線を当てるには「下端（当て推量の水準）」と「中間（急に立ち上がる帯）」が
    //   要るのに、いまの並びは上に偏りすぎていてどちらも取れていない。
    //
    //   **新しい置き方**: 薄い側の水準を **1〜7% の帯**に置き、上の2水準
    //   （**20%＝立ち上がりの終わり** と **100%＝天井の確認**）だけを固定する。
    //
    //   ⚠ 4方式で同じ並びを使っている点は据え置きである。上のパイロットでは
    //   ぼかしだけ 3% ですでに75%正解で、他の3方式より床が低い可能性がある。
    //   方式ごとに帯をずらすかどうかは、初見の人のデータを見て A2 で判断する。
    //
    // ■ 2026-08-24（同日の2度目の変更）に **8水準 → 5水準** へ減らした（丸山判断）。
    //   薄い側を6→3水準にし、固定の2水準（20%・100%）はそのまま残した。
    //
    //   **なぜ減らしたか。** 1人あたりの問題数を下げて所要を短くするためである
    //   （視覚較正は134問・約8分だったのが、この変更と「1人1方式1字×2速度」への
    //    割付変更を合わせて **68問・約4〜5分**になる）。
    //   1人あたりの点は減るが、**参加者ごとに水準をずらす仕掛け**（下の
    //   progress_pct_shift）と人数（50人）で、集団としては細かい格子が得られる。
    //   個人で多くの点を取るより、少ない点 × 多人数のほうが個人差が平均化されて
    //   曲線がなめらかになる。S字を当てるのに要る最低限の点数（4点）は確保している。
    //
    //   薄い側3水準の間隔 2.25% は「ずらしの刻み 0.75% × 3通り」で、
    //   3通りを合わせると 1.0〜7.0% が 0.75% 刻みで**すきまなく**埋まる。
    //
    // ■ **2026-08-25 に [1, 3.25, 5.5, 20, 100] → [3, 6, 9, 12, 16, 22, 35, 100] に
    //   置き直した（丸山判断）。上の 8/24 の2つの変更が逆向きに働いていた。**
    //
    //   **何が起きていたか。** 8/24 に同じ日に2つの変更を入れた。
    //     ① 基準アニメを 600ms → 300ms に半減（群Bの対照条件が不利だったため）
    //     ② 提示水準を薄い側へ寄せる（13%以上が天井に張り付いていたため）
    //   ところが **②の根拠にした先生のパイロットは 600ms で取ったもの**である
    //   （記録の base_anim_ms は 600 と 1000）。①で見ている時間が半分になれば
    //   同じ進み具合でも格段に難しくなるので、**水準は上へ動かすべきところを
    //   下へ動かしていた**。
    //
    //   **300ms での実測**（2026-08-25・丸山さんの自己テスト・ぼかしを除く18問）:
    //     1.75% → 0/4 ／ 4% → 0/5 ／ 6.25% → 0/2 ／ 20% → 2/3 ／ 100% → 4/4
    //   **6.25%以下は 0/11、20%以上は 6/7。** しきい値は 6〜20% のあいだにあるのに、
    //   旧の並びは **5.5% と 20% のあいだが空いていて、そこを跨いでいた**。
    //   立ち上がりを1点も測れない配置だった。
    //
    //   **新しい置き方**（床・立ち上がり・天井の3つを揃える）:
    //     床       3・6%           … 当て推量の水準（1/68＝1.5%）を取る
    //     立ち上がり 9・12・16・22%  … **旧の並びに1点も無かった帯。ここが本体**
    //     天井      35・100%        … 上端を押さえる
    //
    //   ⚠ **1人あたりの問題数は変わらない**（70問のまま）。1字につき3水準を担当する
    //     仕組みは同じで、`assignment.point_subsets` には既に "8": [0,3,5] がある。
    //     変わるのはセルの埋まり方だけ（320セル・1セル11.3回答 → 512セル・約7回答）。
    //     曲線の当てはめは1セル単位ではなく**字・方式ごとに全水準をまとめて**行うので
    //     （計画書 7.1）、水準が広く散っているほうが曲線は安定する。
    //     旧の並びは3点が床に固まっていて、実質2点しか情報がなかった。
    //
    //   ⚠ **ぼかし（blur）だけは別に手当てが要る。** 600msでも300msでも薄い側で
    //     当たっており（先生 3%で8/9、丸山 1.75%で4/4）、**床に一度も届いていない**。
    //     これはアニメの長さの問題ではなく `families.blur.max_radius_px` が
    //     弱いことが原因である。値は目で見て決める（experiment/tools/level_chooser.html）。
    //
    // ■ **2026-08-25（同日の2度目）に [3,6,9,12,16,22,35,100] → [6,12,22,35,50,70,100] へ。**
    //   **画面の書き換え周期（1秒に60回＝16.7msごと）を考えに入れていなかった**ため。
    //
    //   **何を見落としていたか。** 較正の進み具合は s = 経過ms ÷ base_anim_ms の等速で、
    //   打ち切りは**描く前**に判定する（transfer.js の frame()）。base_anim_ms が 300ms
    //   なので、たとえば「3%」は 9ms で止めたいことになるが、**最初の書き換えが 16.7ms**
    //   なので**1枚も描かれず真っ白のまま終わる**。同じ理由で「6%」と「9%」は
    //   どちらも実際には 5.6%、「12%」と「16%」はどちらも 11.1% になり、参加者から
    //   見て区別がつかない。8水準が実質5段階に潰れていた。
    //
    //   **機種でも変わる。** 60Hz では 5.6% 刻みだが、120Hz の端末では 2.8% 刻みになる。
    //   同じ「3%」が 60Hz では真っ白・120Hz では 2.8% が見える、という食い違いが出る。
    //   薄い水準ほどこの差が効く。
    //
    //   **どう扱うか（実験1・v1 で先生が決めた方針をそのまま引き継ぐ）。**
    //   `project/実験計画書_1文字課題.md`:
    //     > 60Hz端末では1フレーム16.7msの量子化により低fracの水準が実効的に潰れる。
    //     > 記録済みの actual_ms を f(·) の入力とする。**名目fracはあくまで割付水準の記録とする。**
    //   端末のリフレッシュレートは揃えようがないので、**提示の仕組みは直さず、
    //   解析では実測値（actual_s / refresh_hz を1問ごとに記録済み）を横軸に使う**。
    //
    //   **ただし「60Hzで1枚も描かれない水準」は置かない。** 理由は2つ:
    //     ① 刺激が出ないので曲線の点として使えない
    //     ② その水準のデータが**高リフレッシュの端末の人からしか出ない**ことになり、
    //        水準と機種が絡む
    //   1フレームぶん＝ 16.7 / 300 = **5.6%** なので、いちばん薄い水準は 6% 以上にする。
    //
    // ■ **2026-08-25（同日の3度目）に [6,12,17,25,30,50,100] → [2,4,7,11,16,25,40,100] へ。**
    //   **提示規則を直した（狙った s ちょうどを必ず1枚描く）ので、フレームの刻みに
    //   合わせる必要がなくなった。** それまでの並びは「60Hzで止まれる場所」に
    //   合わせた不自然な数字で、しかも30Hzの端末では破綻していた。
    //
    //   **新しい置き方の根拠**（4方式で同じ並びを使うので、どれにも合う幅を取る）:
    //     床       2・4%       … 当て推量の水準（1/68＝1.5%）を取る
    //     立ち上がり 7・11・16・25% … 300msでの立ち上がりはこのあたりと見込まれる
    //     天井     40・100%    … 上端を押さえる
    //
    //   見込みの根拠: 600msのパイロットでは4方式とも 3%→25〜75%・6%以上→ほぼ100% で、
    //   立ち上がりは 3〜6% にあった。300ms は見る時間が半分なのでそれより濃い側に
    //   ずれるはずで、丸山さんの300ms実測では濃さ11%で1/2（50%）だった。
    //   **ただし 300ms の 0〜11% にはデータが1つも無い**ので、位置には幅がある。
    //   だから床から天井まで広めに8点取る。
    //
    //   ⚠ `assignment.point_subsets` に "8": [0,3,5] があるので割付はそのまま動く。
    //     ⚠ **8 と方式の数 4 は互いに素ではない**ので、水準の回転を方式の割付と
    //     同じ数から作ると潰れる。transfer.js の buildVisualTrials は
    //     floor(連番 ÷ 方式の数) から回転を作るよう直してあるので問題ない
    //     （直す前は 512セルが 192セルに潰れていた）。
    //
    //   ⚠ **以下は提示規則を直す前の検討の記録。** フレームの刻みの話はもう当てはまらない。
    //   **（旧）指定値は「60Hzで実際に止まれる場所」にきっちり乗せる。**
    //   60Hz・300ms で止まれるのは 5.6 / 11.1 / 16.7 / 22.2 / 27.8 / 33.3% である。
    //   ⚠ 途中まで [6,12,22,35,50,70,100] にしていたが、**7水準のうち4つが天井に
    //     張り付く**ことが予想曲線で分かり、置き直した（experiment/tools/expected_curve.html）。
    //     丸山さんの300ms実測（ぼかしを除く18問）に当てはめると正答率50%は進み具合18%
    //     あたりで、35%以上はすべて99%を超える。先生のパイロットで起きた
    //     「8水準のうち6水準が天井」という失敗を、上下逆向きに繰り返すところだった。
    //     また「22%」と指定しても実際は 16.7% にしかならない（22.2%のフレームで
    //     打ち切られる）ため、22.2〜33.3% の帯が丸ごと空いていた。
    //
    //   **この並びの60Hzでの実測値と予想の正答率**:
    //     6% → 5.6%（予想3%・床）      12% → 11.1%（10%）
    //     17% → 16.7%（40%）           25% → 22.2%（77%）
    //     30% → 27.8%（95%）           50% → 44.4%（100%・保険）
    //     100% → 完成形（天井の確認）
    //   **立ち上がりに4点**入り、床と天井も押さえられる。
    //   50% を残すのは**保険**で、曲線の位置の推定が「20%での3問」しか根拠がないため、
    //   予想より右（難しい側）にあった場合にこの点が効く。
    //   重複なし・全部1枚以上描かれる。120Hz でもそれぞれ別の値になる。
    //   （`assignment.point_subsets` に "7": [0,2,4] があるので割付はそのまま動く。
    //     7 と 方式の数 4 は互いに素なので、回転が方式と絡む問題も起きない。）
    progress_pct_levels: [2, 4, 7, 11, 16, 25, 40, 100],

    // 群A′で、**狙った進み具合の絵を何ミリ秒残すか**（2026-08-25 追加）。
    //
    // ■ なぜ固定時間にするのか
    //   はじめは「描いた次のフレームで消す」にしていた。ところが負荷が高いときに
    //   **描いた1枚が画面に出る前に消えてしまう**ことがあり、
    //   「2%が描画されるときとされないときがある」という形で表面化した。
    //   requestAnimationFrame は描画の前に呼ばれるが、**実際に画面へ出る保証はない**
    //   （重いと1回ぶん飛ばされる）。固定時間だけ待てば確実に出る。
    //
    // ■ もう1つの利点: **表示時間の端末差が消える**
    //   次のフレームまで待つ方式だと、その1枚を見る時間が
    //   60Hzで17ms・30Hzで33ms・120Hzで8msとばらついた。
    //   固定時間にすれば全端末でそろう。**進み具合も表示時間も揃う**ことになる。
    //
    // ■ なぜ 34ms か
    //   いちばん遅い 30Hz の1フレーム（33.3ms）より少し長く取る。これより短いと
    //   30Hz の端末で1回も画面に出ないことがある。長くしすぎると「アニメを止めて
    //   見せる」より「静止画を見せる」に近づき、課題の性質が変わる。
    //   ⚠ **較正と生成・検証で同じ値を使うこと**（片方だけ変えると転写が成立しない）。
    endpoint_hold_ms: 34,

    // ---- 方式ごとの水準（2026-08-25 追加）------------------------------------
    // **4方式は同じ「進み具合」でも難しさが桁違いなので、並びを方式ごとに変える。**
    // 実測でも、同じ 1.75% で「ぼやけ→はっきり」だけ 4/4 正解・他の3方式は全滅だった。
    // 1つの並びを共用すると、ある方式では8水準のうち6つが床、別の方式では6つが天井、
    // ということが起きて曲線が描けない。
    // （この節の上にある「4方式で同じ並びを使う点は据え置き。方式ごとに帯をずらすかは
    //   初見のデータを見て判断する」という注記への回答にあたる。）
    //
    // ⚠ **個数は全方式で8にそろえること。** 割付（assignment.point_subsets）は
    //   個数しか見ていないので、そろえてあれば1人あたりの問題数もセルの数も変わらない。
    //   個数を変えると頻度差ゼロの設計（よく出る20字がどれも3回）が崩れる。
    //
    // ⚠ **決め方**: experiment/tools/level_chooser.html で**本番と同じ再生**をして、
    //   その方式が「まったく分からない → だんだん分かる → 確実に分かる」の3つを
    //   通るように並べる。静止画で判断してはいけない（1枚17msしか映らないため）。
    //
    // 書いていない方式は上の progress_pct_levels（共通の並び）を使う。
    progress_pct_levels_by_family: {
      // ■ うすい→濃い（fade）: **他より薄い側へ寄せる。**
      //   濃さ＝進み具合そのままなので、2%でも 255段階中5 あって読めてしまう
      //   （丸山さんが本番と同じ34ms再生で確認）。他の3方式は「一部しか出ていない」のに、
      //   fade は**字の形が最初から完成している**（薄いだけ）ぶん有利になる。
      //
      //   ⚠ **物理的な下限がある。** 画面の濃さは 0〜255 の256段階しか無いので、
      //     進み具合 0.4% ＝ 255段階の 1 が最低である。これより薄くはできない。
      //     隣り合う水準が同じ段に丸められないよう、薄い側は 0.4 / 0.8 / 1.4 と
      //     段が変わる幅で刻む（255段階では 1 / 2 / 4 にあたる）。
      fade: [0.4, 0.8, 1.4, 2.2, 3.5, 6, 12, 100],

      // ■ ぼやけ→はっきり（blur）: **他より濃い側へ寄せる。**
      //   ぼかしは墨の量を保存するので、薄い側でも灰色の雲が残り、
      //   「見え始め」が他の3方式より濃い側にある。丸山さんの聴取では
      //   40%（半径43px）でもまだ難しかったので、帯を濃い側へ寄せる。
      blur: [10, 18, 26, 34, 42, 52, 65, 100],

      // ■ 点が増える（reveal）: **他より薄い側へ寄せる。**
      //   進み具合＝墨の画素のうち何%を出すか。「あ」は墨がおよそ1万画素あるので、
      //   **2%でも約200点**出ることになり、形が十分に見えてしまう
      //   （丸山さんが本番と同じ34ms再生で確認）。
      //   下限は1点＝0.01%程度だが、数点では「何かある」しか分からないので
      //   0.1%（約10点）を床に置く。
      //     0.1%→約10点 ／ 0.25%→25点 ／ 0.5%→50点 ／ 1%→100点
      //     2%→200点 ／ 4%→400点 ／ 9%→900点 ／ 100%→全部
      reveal: [0.1, 0.25, 0.5, 1, 2, 4, 9, 100],

      // ■ 端から現れる（wipe）: **他より薄い側へ寄せる。**
      //   進み具合＝字の幅（インクのある範囲）のうち何%を出すか。
      //   2%だと幅のおよそ4画素ぶんの縦帯で、字によっては手がかりになる
      //   （丸山さんの聴取で「怪しい」）。
      //   下限は1画素ぶん＝0.5%程度。これより細かく刻んでも同じ絵になる。
      wipe: [0.5, 1, 2, 4, 8, 14, 25, 100],
    },

    // ---- 参加者ごとに薄い側の水準をずらす -------------------------------------
    // 全員が同じ5水準を見ると、測れる s の値も5個しかない。かといって完全な乱数に
    // すると、同じ人が再開したときに水準が変わってしまい、割付が再現できない。
    // そこで**参加者の連番から決まるずらし量を、薄い側の3水準にまとめて足す**。
    // 集団全体では 1.0〜7.0% を 0.75% 刻みで**すきまなく**埋められる（下の算数を見ること）。
    //
    // ■ ずらし量 = (参加者の連番 % count) × step_pct
    //     連番0,3,6…の人 → +0.00%  → 1.00 3.25 5.50（＋20・100）
    //     連番1,4,7…の人 → +0.75%  → 1.75 4.00 6.25（＋20・100）
    //     連番2,5,8…の人 → +1.50%  → 2.50 4.75 7.00（＋20・100）
    //   3通りを合わせると 1.00/1.75/2.50/3.25/4.00/4.75/5.50/6.25/7.00% の9点になり、
    //   水準の間隔2.25% ＝ 刻み0.75% × 3通り なので**帯の中に穴が空かない**。
    //
    // ■ **なぜ 0.75% 刻みなのか（これより細かくしても意味がない）**
    //   フェードは不透明度＝進み具合をそのまま画面に出す（visual.families.fade.gamma=1.0）。
    //   画面の不透明度は **0〜255 の256段階**でしか表現できないので、1〜7% の帯には
    //   物理的に **255×0.07 − 255×0.01 ≒ 15段階** しか存在しない。
    //   0.75% は不透明度にして約1.9段階ぶんにあたり、**隣の水準が別の絵になる**いちばん
    //   細かいあたりである。これより細かく刻むと、隣どうしが同じ不透明度に丸められて
    //   同じ絵になってしまう（0.5% だと約1.3段階で、丸めで同じ絵になる組が出る）。
    //
    // ■ **なぜ 3通りなのか**
    //   群A′の割付は「連番を8で割った余り」で一巡する
    //     ・担当する字の半分（偶数番目の字か奇数番目の字か） = 連番の2での余り
    //     ・方式の回し量 = (連番 ÷ 2) の4での余り
    //   ので、ずらしの通り数を8の約数（2・4・8）にすると、字・方式とずらし量が
    //   重なってしまい、どのセルも決まったずらし量しか集まらない。
    //   3は8と互いに素なので、**24人まわると字×方式×速さ×ずらし量の全組合せがそろう**。
    //
    // enabled を false にすると progress_pct_levels の並びがそのまま全員に出る。
    progress_pct_shift: {
      // **2026-08-25 に false（ずらさない）にした（丸山判断）。**
      //
      // ■ なぜ止めたか
      //   ずらしは「水準が5つしかなく、測れる s も5個しかない」状態を補うためのもの
      //   だった。8水準に置き直して格子が細かくなったので、役目が終わっている。
      //   むしろ**セルが薄くなる害のほうが大きい**: 実測（5水準＋ずらし）では
      //   「名目の水準で320セル・平均11.3回答」だったのが、
      //   「実際の水準で数えると704通り・平均5.1回答」まで薄まっていた。
      //   8水準にずらしを重ねると、さらに細かく散る。
      //
      // ■ 止めると何が変わるか
      //   全員が同じ8水準を見る。曲線の当てはめは字・方式ごとに全水準をまとめて
      //   行うので（計画書 7.1）、**同じ水準に人数が集まるほうが1点1点が安定する**。
      //   設計も論文の説明も単純になる。
      //
      // true に戻せば従来どおり動く（実装はそのまま残してある）。
      enabled: false,
      // 薄いほうから何水準にずらしを当てるか。残り（20%・100%）は全員固定。
      apply_to_lowest: 3,
      // ずらしの刻み%。上の「256段階」の根拠から 0.75% より細かくしない。
      step_pct: 0.75,
      // ずらしの通り数。上の「8と互いに素」の根拠から 3。
      count: 3,
    },

    // ---- RQ4: 基準アニメの速さを2通り測る(較正フェーズはずっと入れておく) ----
    // 問い: 「表示の識別率は、いま画面がどこまで進んでいるか(進み具合 s)だけで決まるのか、
    // そこへ**速く**着いたか**ゆっくり**着いたかにも左右されるのか」(計画書 3章 RQ4・6.1)。
    // 生成のやり方(時間軸の伸び縮み)は「s が同じなら正答率も同じ」を土台にしているので、
    // その土台が本当かどうかを正面から測る。差が無ければ土台を支える証拠、差が有れば
    // 「履歴に依存する＝時間の伸び縮みだけでは移せない」という条件の発見になる。
    //
    // **下見で切らない。較正フェーズ(群A′)のあいだはずっと入れたままにする**
    // (下見は予備の見当、本番の群A′が本答え)。検証フェーズを掲載するときも
    // 切る必要はない——群A′はもう走らないので、実際の出題には何も影響しない。
    //
    // enabled を false にすると全試行が base_anim_ms で動き、この測定が無かった頃と
    // 同じ挙動に戻る(RQ4 を取りやめるとき以外は触らない)。
    calib_speed_probe: {
      enabled: true,
      // どの方式で測るか。
      //
      // ■ 2026-08-24 に **フェードだけ → 4方式すべて** に広げた（丸山判断）。
      //   以前は問題数を抑えるためフェードだけにしていたが、そうすると RQ4 の答えが
      //   「フェードではこうだった」で止まり、生成が4方式にまたがっているのに
      //   土台（進み具合が状態を決めるという見方）を1方式でしか確かめられない。
      //   さらに「**速さの影響は方式によって違うか**」という問いにも答えられない
      //   （フェードは濃さで操るので速さの影響を受けやすく、部分表示は画素の有無なので
      //    受けにくい、という予想がある。計画書 3章 RQ4）。
      //   問題数が増えないようにするため、同時に**1人が担当する字を減らした**
      //   （「4方式 × 2字」→「4方式 × 1字 × 2速度」。下の assignment を見ること）。
      //
      // 空配列や未設定にすると「どの方式でも2水準」の意味になる。
      // 1方式に絞りたいときは要素1つの配列にする（例 ["fade"]）。
      families: ["fade", "reveal", "blur", "wipe"],
      // 基準アニメの長さの2水準ms。**1つ目は上の base_anim_ms と同じ値にすること**。
      // 1つ目＝いつもの速さ。**生成(warp)に使う視覚の曲線はこの1つ目の水準だけ**と
      // 事前に決めてある(計画書 7.1)。2つ目は1つ目の約1.7倍ゆっくりで、
      // **RQ4 に答えるためだけに使い、生成には入れない**。
      // どちらも暫定値で、下見の結果を見て A2 で確定する。
      //
      // 2026-08-24: base_anim_ms を 600→300ms にしたのに合わせ、**倍率1.67倍を保ったまま**
      // [600, 1000] → [300, 500] に下ろした。速さの比を変えると RQ4 が問うている
      // 「1.7倍ゆっくり着いたら正答率は変わるか」の中身が変わってしまうため、
      // 比だけは動かさない。
      base_anim_ms_levels: [300, 500],
      // 生成に使う水準ms。base_anim_ms_levels の1つ目と同じ値にすること。
      // 「結果を見てから生成に使う速さを選んだ」と言われないよう、ここで事前に固定する。
      generation_level_ms: 300,
    },

    // 群B の打ち切り時刻ms。聴覚と同じ時点で曲線を比べるため、既定は
    // audio.gates_ms と同じ値を書く(別々に動かせるよう表は分けてある)。
    gates_ms: {
      _default: [20, 40, 60, 80, 110, 150, 220],
    },

    // 4方式の描画パラメータ。方式の実装は transfer.js の Renderers。
    families: {
      // 不透明度 = s^gamma。文字全体が最初からあり、信号の強さが上がる。
      //
      // gamma は「進み具合 s を、画面の濃さにどう配るか」のつまみである。
      //   gamma = 1.0 … 濃さ = 進み具合そのまま（**現行の挙動**）
      //   gamma > 1.0 … 序盤を薄めに通す。s=0.3 のとき gamma=2 なら濃さ0.09
      //   gamma < 1.0 … 序盤から濃くする
      // なぜ用意したか: フェードは「見えるか見えないか」の二択に感じられ、
      // 中間の状態が薄い側に潰れている疑いがある（下見の内観報告。
      // project/パイロット分析_20260821.md）。初見データの曲線が階段状に出たら、
      // ここを 1.0 より大きくして薄い側を引き伸ばす手が使える。
      //
      // ■ **2026-08-25 に 1.0 → 2.0 にし、同日中に 1.0 に戻した。**
      //   **提示規則を直した（狙った s ちょうどを必ず1枚描く）ことで、gamma が不要になった。**
      //   2.0 にしたのは「画面の書き換え周期より薄い絵を出せない」という制約への対処
      //   だったが、その制約自体が消えたので、目盛りをいじる理由が無くなった。
      //   **1.0 なら「進み具合＝濃さ」の1対1**で、生成した表の数字がそのまま濃さとして
      //   読めるので、デバッグでも論文の図でも扱いやすい。
      //   ⚠ 以下は 2.0 にしたときの検討の記録。**水準の並びを変えて薄い側が
      //     床にならなくなったら、またここを上げる手がある。**
      //
      // ■ （記録）2026-08-25 に一度 1.0 → 2.0 にしたときの理由
      //
      //   **なぜ。** gamma=1.0 だと濃さ＝進み具合そのままなので、いちばん薄い水準
      //   6%（60Hzでの実測5.6%）でも**濃さ5.6%（255段階中14）**あり、
      //   しかも**字の形は最初から完成している**（薄いだけ）。他の3方式は
      //   「一部しか出ていない」ので、同じ進み具合でも fade だけ格段に分かりやすい。
      //   実際、丸山さんが本番と同じ時間で再生して「いちばん薄い水準でも分かる」と判定した。
      //   **床（当て推量に近い正答率）が取れないと曲線を当てはめられない。**
      //
      //   **先生のパイロット（600ms）でも同じ向きだった**: fade は 3% で25%正解・
      //   6% で100%正解。つまり **fade の立ち上がりは濃さ3〜6%の帯**にあり、
      //   それは**いまの最低水準5.6%より下**にある。gamma=1.0 のままでは、
      //   fade の曲線の動く部分がまるごと測定範囲の外に落ちる。
      //
      //   **gamma=2.0 にすると濃さが s² になる**ので、立ち上がりが測れる範囲に入る:
      //     進み具合（実測）  5.6% → 濃さ 0.3%    11.1% → 1.2%
      //                      16.7% → 2.8%       22.2% → 4.9%  ← 立ち上がり
      //                      27.8% → 7.7%       44.4% → 19.7%   100% → 100%
      //
      //   ⚠ **これは刺激の作り方を変えるだけで、解析の枠組みは変わらない。**
      //     方式ごとに曲線 V_i(s) を測って逆引きする設計なので、gamma は
      //     「その方式の s と見え方の対応」を変えるだけである。ただし
      //     **fade の s は他の方式の s と直接は比べられなくなる**（もともと
      //     方式ごとに難しさが違うので、比較は曲線を通して行う）。論文に明記する。
      //
      //   ⚠ **水準の並びを変えたら、この値も見直すこと。**
      //     確かめ方: experiment/tools/level_chooser.html の「▶ 再生」で、
      //     **本番と同じ時間**で動かして判断する（静止画で見てはいけない。
      //     いちばん薄い水準は60Hzで1枚＝約17msしか映らないので別物である）。
      fade: { gamma: 1.0 },
      // ストローク画素を、字ごとに固定した乱数順で表示していく。
      // 画素の順番はブラウザ側で決める(101段階の画像には依存しない)。
      // seed は "<seed_prefix><かな>" の文字列から作る。数値の並びは
      // Python の random.Random とは別物だが、字ごとに固定であればよい。
      reveal: { seed_prefix: "stroke-mask:", ink_threshold: 128 },
      // ガウスぼかしの半径 = max_radius_px × (1 − s)。s=1 で鮮明。
      // ⚠ **2026-08-25 に 24 → 48 に上げた（丸山判断）。**
      //   24px では**いちばんぼやけた状態でも字が読めてしまい、床が無かった**。
      //   実測: 先生600ms・3%で 8/9 正解／丸山300ms・1.75%で 4/4 正解。
      //   他の3方式は同じ水準で全滅しているので、blur だけ床に届いていない。
      //   床（当て推量に近い正答率）が取れないと、その方式の曲線の下半分を
      //   推定できず、生成に使う逆引きが定義できない。
      //   これはアニメの長さの問題ではない（600msでも300msでも読めていた）ので、
      //   値そのものを上げる。
      //
      // ■ **値の決め方（2026-08-25・丸山さんが目で判定して 72 に確定）**
      //   ⚠ 床が要るのは「進み具合0%」ではなく、**実際に出すいちばん薄い水準**である。
      //     いまの並びなら 6%（60Hzでの実測 5.6%）で、そこでのぼかしは
      //     **最大 × (1 − 0.056) = 最大 × 0.944** になる。
      //
      //   丸山さんの判定（進み具合0%の見た目）:
      //     48px → まだ分かる ／ 56px → 分かるかも ／
      //     64px → 分からないが時々見える ／ 80px → 見えない
      //
      //   これを 5.6% での実効ぼかしに直すと:
      //     最大48 → 45px（読める＝床にならない）
      //     最大64 → 60px（際どい）
      //     **最大72 → 68px（見えない寄り＝床になる）**  ← 採用
      //     最大80 → 76px（確実に見えないが、床が広がりすぎる）
      //
      //   **80 を採らない理由**: 立ち上がりが後ろへ寄り、測れる点が減る。
      //     各水準での実効ぼかし（最大72）: 5.6%→68 / 11.1%→64 / 16.7%→60 /
      //     22.2%→56 / 27.8%→52 / 44.4%→40 / 100%→0 px。
      //     11〜44% に立ち上がりが散る。最大80だとこれが 16〜44% に縮む。
      //
      //   ⚠ **水準の並びを変えたら、この値も見直すこと**（いちばん薄い水準が変われば
      //     床の条件も変わる）。確かめ方は experiment/tools/level_chooser.html。
      blur: { max_radius_px: 72 },
      // 見せる領域を単調に広げる。direction: "ltr" | "rtl" | "ttb" | "btt"
      wipe: { direction: "ltr", soft_edge_px: 0 },
    },

    // 転写(warp)の進み方 s(t)。生成工程の出力を JSON で置き、ここから読む。
    // 形式: {"frame_ms": 16.667, "tables": {"<方式>": {"<かな>": {"<条件>": [s,s,...]}}}}
    // 数値列は 60Hz(16.667ms 刻み)。t ms での s は線形補間で求める。
    warp: {
      tables_url: "transfer_warp.json",
      frozen_commit: "",        // 凍結したコミット番号(検証データを取る前に書く)
      // 表が無い/その組合せが無いときの代用(研究者の動作確認用)。
      // baseline1: 等速   s = t / base_anim_ms
      // baseline2: 1次変換 s = a·t + b (下の affine を使う)
      // proposed : 表が要る。無ければ baseline1 で代用し、fallback_used を記録する。
      //
      // 2026-08-24: base_anim_ms を半分（600→300ms）にしたので、この代用の傾き a も
      // 倍（0.0022→0.0044 /ms）にして、等速との相対関係を元のままに保った。
      // **本番データには影響しない**（生成した表がある状態でしか検証データは取らない。
      // ここは表が無いときの研究者の動作確認用の代用である）。
      fallback_affine: { a: 0.0044, b: -0.05 },
      // ステップ表示（群Cの5つ目の見せ方）で、何も出さない状態から完成形へ
      // 切り替える時刻ms。**本番は生成工程が字ごとに決める**
      // （transfer_warp.json の step_mid_ms。目標の音声曲線で正答率が50%に達する時刻）。
      // ここに書いてあるのは、その表がまだ無いとき（生成前・研究者の動作確認）に使う
      // 全字共通の代用値である。読むのは experiment/transfer_comfort.js だけ
      // （群Bの条件にステップ表示は入っていない）。
      fallback_step_mid_ms: 60,
    },
  },

  // ---- 群Bの条件 ----------------------------------------------------------
  // 1階(正式な検証): fade と reveal に3条件ずつ。2階(探索): blur と wipe は提案のみ。
  //
  // ■ **ステップ表示(ある時刻まで何も出さず、そこで一気に完成形を出す表示)は
  //   ここには入れない**(2026-08-24・丸山決定)。いったん群Bの9番目の条件として
  //   足しかけたが、**群C(見え心地)の5つ目の見せ方**に置き直した。
  //   群Bに足すと1条件ぶん出題の組合せが増えて必要人数が増えるうえ、
  //   本当に知りたいのは「従来型の字幕(語が確定してから一気に出す)と、
  //   段階的な表示のどちらが続けて見ていられるか」だからである。
  //   設定は experiment/transfer_comfort_config.js の families にある。
  conditions: [
    { family: "fade",   condition: "baseline1", tier: 1 },
    { family: "fade",   condition: "baseline2", tier: 1 },
    { family: "fade",   condition: "proposed",  tier: 1 },
    { family: "reveal", condition: "baseline1", tier: 1 },
    { family: "reveal", condition: "baseline2", tier: 1 },
    { family: "reveal", condition: "proposed",  tier: 1 },
    { family: "blur",   condition: "proposed",  tier: 2 },
    { family: "wipe",   condition: "proposed",  tier: 2 },
  ],

  // ---- 出題の設計 ---------------------------------------------------------
  // ■ 2026-08-24 に全面的に作り直した（config_version の prov-2026-08-24c を見ること）。
  //   1人あたりの構成は3集団とも同じ形である:
  //       本命8字 × **3点**（その字の測定点のうち3点）    = 24問
  //       decoy 12字 × **3点**                            = 36問
  //       確認問題A（全部提示・10%）                       =  6問
  //       確認問題C（いちばん情報の少ない点・6%）          =  4問
  //       ────────────────────────────────────────────
  //       合計 **70問**（よく出る20字はどれも3回ずつ。確認問題に当たった字だけ +1回）
  design: {
    // 各セルを1人が何回見るか。1のままにすること
    // （2以上にすると同じ字が増えて、せっかく等しくした出現頻度が崩れる）。
    reps: 1,
    // **本命1字につき、1人が担当する測定点の数**。
    //   聴覚 … 8点（その字の打ち切り時刻7点＋全長）のうち3点
    //   群A′ … 5点（進み具合の水準）のうち3点
    //   群B  … 7点（打ち切り時刻）のうち3点
    // どの3点かは参加者の連番から決まる（assignment.point_subsets）。
    // 集団全体では、どのセルにも同じ人数が入る（1セルあたり 人数 × 3 ÷ 点数）。
    target_points_per_char: 3,
    // 同じ字が続けて出ないようにする最小の間隔（この数だけ他の字を挟む）。
    // 0 にすると単純なシャッフルに戻る。20字×各3回なので4問は余裕で空く。
    min_same_char_gap: 4,
    // 1人あたりのターゲット問題数の上限(0以下なら制限なし)。
    // いまの割付では 8字×3点＝24問なので、既定では効かない。
    max_target_trials: 0,
    // 練習(記録しない)。最低1回やらないと本番へ進めない。
    // **練習には decoy の字しか出さない**(2026-08-24)。練習は答えを教える画面なので、
    // 本命字を出すと「この字が本命だ」と教えてしまうため。
    practice_min: 1,
    // 確認問題A: 全部見せ・全部聞かせ(打ち切りなし)を混ぜる割合。
    // ■ 2026-08-24 に 6% → **10%** へ上げた（丸山決定）。真面目に答えているかの判定に使うので、
    //   3〜4問だと「何問まちがえたら除外」という線が引けないためである（60問なら6問になる）。
    //   除外の基準は計画書 7.3 に事前登録してある（**6問中4問以下の正解で除外**）。
    // ■ 字は**本命字だけでなく、よく出る20字(本命＋decoy)から重複なく選ぶ**。
    //   本命字からだけ選ぶと、そのぶん本命の出現回数が増えて等頻度が崩れるため。
    check_full_rate: 0.10,
    // 確認問題C: ほとんど情報のない問題(その字のいちばん早い点)を混ぜる割合(学習の検出用)。
    // これも確認問題Aと同じく、よく出る20字から選ぶ。
    check_floor_rate: 0.06,
    // 課題の最後に「同じ字が繰り返し出たと感じたか」「どの字か」の2問を出すか。
    //
    // ■ **2026-08-25 に false（出さない）にした**（丸山判断）。
    //   理由は所要を延ばさないため。実装（transfer.js の awarenessScreen）は
    //   丸ごと残してあるので、true に戻せばそのまま復活する。
    //
    // ■ 消して何が変わるか（計画書 7.4 と対応させること）
    //   失う指標: aware_repeated / aware_chars / aware_n_hit_target。
    //     7.4 の**補助指標のうちの1つ**であり、**もともと除外の基準には使わない**
    //     （「学習が起きたかどうかを測る指標である」と明記してある）。
    //   残る指標: **主指標はそのまま無傷**である。
    //     回答が本命8字／よく出る20字に入る割合を試行の通し番号で追う
    //     （resp_in_target_set / resp_in_frequent_set）のは1問ごとの記録から計算する。
    //     確認問題Cの正答率・誤答時に本命字を答えた割合・回答分布のエントロピー・
    //     前半後半で曲線を分けて比べる、も同様に影響を受けない。
    //   つまり**「学習が起きたか」は引き続き測れる**。失うのは
    //   「本人が気づいたと自覚していたか」という自己申告だけである。
    ask_awareness: false,

    // ---- 課題のあとに聞く3問（2026-08-25 追加・丸山判断）--------------------
    //
    // ■ なぜ聞くのか
    //   識別率は**その人の見え方・聞こえ方**に左右される。クラウドソーシングは
    //   参加者の背景が分からないので、**解析で層を分けられるようにしておく**。
    //   とくに「聞き取りに支障がある」方が混ざると聴覚の曲線が下振れするが、
    //   それが分からないままだと原因を切り分けられない。
    //
    // ■ ⚠ **除外には使わない。** 感度分析（その層を除いても結論が変わらないか）に使う。
    //   事前に除外条件を決めていないものを、データを見てから除外に使ってはいけない。
    //
    // ■ ⚠ **すべて任意。答えなくても完了コードは出す。**
    //   答えを強制すると、正直に答えない人が出るか、途中で離脱する。
    //
    // ■ ⚠ **課題の「あと」に置くこと。**
    //   ・同意画面は1画面に収める制約があるので入れられない
    //   ・課題の前に聞くと「聞き取りに支障がある」と答えた人が身構えるおそれがある
    //
    // ■ ⚠ **掲載文の「取得する情報」にも書くこと。**
    //   個人を特定できる情報ではないが、取得する以上は開示が要る。
    //
    // ■ 年齢の区分について
    //   募集条件が18歳以上なので、「10代」は18・19歳にあたる。
    //   上は90代以上まで置く（クラウドソーシングには高齢の参加者もいる）。
    post_survey: {
      enabled: true,
      questions: [
        { id: "age_band", label: "年齢層",
          options: ["10代", "20代", "30代", "40代", "50代",
                    "60代", "70代", "80代", "90代以上"] },
        { id: "read_difficulty", label: "日常的に、ひらがなを読むのに支障はありますか？",
          options: ["支障なし", "多少ある（時々読み間違える等）",
                    "ある（読み取りに困難を感じる）"] },
        { id: "hear_difficulty", label: "日常的に、会話などの聞き取りに支障はありますか？",
          options: ["支障なし", "多少ある（聞き返すことがある等）",
                    "ある（聞き取りに困難を感じる）"] },
      ],
    },
    // 確認問題の位置は**すべてランダム**にする（1問目を特別扱いしない）。
    // テスト用の問題だと気づかれないことが、まじめに答えているかの判定には要るためである。
    // 短縮版(研究者の動作確認用チェックボックス)のターゲット問題数。
    short_run_trials: 4,
    // 2問目以降を自動で始めるまでの間(ms)。
    auto_start_ms: 700,
    // 打ち切りのない問題(確認問題A・練習)で、完成した姿を残す時間(ms)。
    full_hold_ms: 350,
  },

  // ---- 割付(参加者の連番から決定的に決める) --------------------------------
  // 連番はサーバの名簿(roster)が返す「その集団で何番目の参加者か」。
  // 研究者の動作確認のときだけ ?group= と ?pnum= で強制できる(本番モードでは無視)。
  //
  // ■ 2026-08-24 の作り直し（prov-2026-08-24c）で、3集団とも
  //   「**本命8字すべてを出す。ただし1字につき測定点は3点だけ**」に統一した。
  //   1人が8点(または5点・7点)を全部やるのをやめ、**参加者間で点を分担する**
  //   （不完全ブロック配置）。集団としては全セルが同じ人数で埋まる。
  //
  // 群Acal(聴覚) : 8字 × その字の8点(打ち切り7点＋全長)のうち3点 = 24問
  // 群A′(視覚較正): 8字 × 5水準のうち3水準 = 24問。
  //                 字ごとに方式1つ・速さ1つが当たる（1人で4方式・2速度すべてを経験する）。
  // 群B(視覚検証) : 8字 × 7時点のうち3時点 = 24問。字ごとに条件1つ。
  //
  // ── どの3点を担当するか（point_subsets） ──────────────────────────────
  //   点の数ごとに「基準となる3つの位置」を決めておき、それを**巡回でずらす**。
  //     ずらし量 rot = (連番 n + 字の番号 i) % 点の数
  //     担当する点 = 基準の各値に rot を足して、点の数で割った余り
  //   例（8点・基準 [0,3,5]）: rot=0 の字は 1・4・6番目の点、rot=1 の字は 2・5・7番目の点…
  //   この置き方には次の性質がある（check_transfer_stimuli.js が機械で検算する）。
  //     ・**1人の中で**、8字ぶんを合わせると、どの点もちょうど3回ずつ出る
  //       （rot が字ごとに1つずつずれるので、8字で8通りの組が1回ずつ当たる）
  //     ・**集団全体で**、どのセル(字×点)にも「人数 × 3 ÷ 点の数」人が入る
  //   基準は「早い点・中ほど・遅い点」が混ざるように選んである（巡回するので
  //   端と端がつながる形になり、どの人にも早い側と遅い側の両方が当たる）。
  //
  // ── 方式・速さ・条件の割付（連番を n、字の番号を i とする） ─────────────
  //   群A′ 方式 … aprime_families[(i + n) % 4]      → 1人で各方式が2字ずつ
  //   群A′ 速さ … calib_speed_probe.base_anim_ms_levels[(i + floor(n / 4)) % 2]
  //               → 1人で各速さが4字ずつ。8人で「字 × 方式 × 速さ」が1周する
  //   群B  条件 … conditions[(i × b_step + n) % 8]  → 1人で8条件すべてを1回ずつ
  //
  //   ■ 2026-08-24 に群A′の「速さは全字で2水準とも見る」をやめた。
  //     1人あたりの問題数を増やさずに8字すべてを出すためである。
  //     RQ4（速さの影響）は「同じ人が同じ字を2つの速さで見る」比較ではなく、
  //     「同じ人の中で、速さの違う字どうしを比べる」形になる（字は効果として入れる）。
  //     集団としては、どの字も両方の速さで測られる。
  //
  // ── 周期（何人でひと回りするか） ────────────────────────────────────
  //   群Acal 8人 ／ 群A′ 方式4・速さ8・点5 → lcm=40人、水準のずらし3通りを含めて120人
  //   群B  条件8・点7 → 56人
  //   人数をこの周期の倍数にすると、セルの埋まり方がぴったりそろう（→ 計画書11章）。
  assignment: {
    mode: "rotation",
    aprime_families: ["fade", "reveal", "blur", "wipe"],
    // 条件の数（8）と互いに素にすること。
    b_step: 3,
    // 参加者の連番を渡す URL パラメータ名。
    pnum_param: "pnum",
    // 「点がぜんぶで何個あるか」→「1人が担当する3点の基準の位置」。
    //   "8" … 聴覚（打ち切り7点＋全長）
    //   "7" … 群B の打ち切り時刻
    //   "5" … 群A′ の進み具合の水準
    // 値の数は design.target_points_per_char と一致させること。
    // 表に無い点数のときは「先頭から順に3点」を使う（安全側の代用）。
    point_subsets: {
      "8": [0, 3, 5],
      "7": [0, 2, 4],
      "5": [0, 1, 3],
    },
  },

  // ---- 見え心地の評価(いまは独立した実験・群C) ------------------------------
  // **2026-08-21 に群Bの末尾から切り離した**。見え心地(RQ3)は
  // experiment/transfer_comfort.html（群C）という別の掲載・別の集団で聞く。
  // 切り離した理由と群Cの設定は experiment/transfer_comfort_config.js にある。
  // ここに残っている値は、群Bの末尾で聞く旧方式に戻すときのためのものである。
  wellbeing: {
    // true にすると、群Bの識別課題が全部終わったあとに見え心地の画面が出る（旧方式）。
    // 既定は false。群Cを独立させたので、両方で聞くと同じ人に二重に聞くことになる。
    in_group_b: false,
    // 代表字 × 4方式 = 8本を、打ち切りなしで再生して評価してもらう。
    // **暫定**(2026-08-20): ターゲット8字の中から、字の複雑さの両端になる2字を
    // 事前に決め打ちした(あ=画数が少なく単純 / ぱ=半濁点があり画素が多い)。
    // 「結果を見てから代表字を選んだ」と言われないように事前固定するのが要点で、
    // どの2字にするかは下見の見え方の確認を経て A2 で確定する。
    chars: ["あ", "ぱ"],
    families: ["fade", "reveal", "blur", "wipe"],
    // 評価に使う進み方。"proposed" は生成した表、"linear" は等速。
    condition: "proposed",
    // 再生後に7件法で聞く3項目(1=まったくそう思わない 〜 7=とてもそう思う)。
    items: [
      { key: "readable", text: "この表示は継続して見やすい" },
      { key: "natural",  text: "変化が自然に感じる" },
      { key: "annoying", text: "目障り・わずらわしい" },
    ],
    scale_min_label: "まったくそう思わない",
    scale_max_label: "とてもそう思う",
    // 最後の1問(4択)。
    choice_text: "字幕として普段使うなら、どの表示がよいですか。",
    choice_labels: { fade: "だんだん濃くなる", reveal: "点が増えていく", blur: "ぼやけ→はっきり", wipe: "端から現れる" },
    // 見え心地の画面では見直しを許す(識別課題ではない・主観評価のため)。
    allow_replay: true,
  },

  // ---- 問い合わせ先とデータの扱い（同意画面・送信失敗の案内に出す） ----------
  // docs/informed_consent.md が「同意画面に載せること」と定めている項目のうち、
  // prod_common.js の同意画面に入っていない4つ（問い合わせ先・研究責任者・
  // 同意撤回の方法・データの保管期間）をここに置く。prod_common.js は実験1
  // （乙課題・frac課題）と共用なので触らない。転写検証の3ページ（較正・検証・群C）が
  // 同意画面を描いたあとに、それぞれの tidyConsentScreen() でこの値を差し込む。
  //
  // ⚠ 値の埋め残しと、暫定のまま残っている項目は次のコマンドで見られる
  //   （掲載前チェックリスト I-1）。何も出なければ全部確定している。
  //     node -e 'global.window={};require("./experiment/transfer_config.js");
  //              const c=window.TRANSFER_CONFIG.contact;
  //              Object.entries(c).forEach(([k,v])=>{
  //                if(String(v).includes("要確認")) console.log("未記入:",k); });
  //              (c.provisional||[]).forEach(k=>console.log("暫定（先生に確認）:",k,"=",c[k]));'
  contact: {
    // 研究責任者の氏名。**所属はここに書かない**——同意画面は
    // 「pi（institution）」の形で組み立てるので、ここに所属を入れると
    // 「栗原一貴（津田塾大学）（津田塾大学 栗原研究室）」と二重になる。
    // 実際に出る文字列: 栗原一貴（津田塾大学 栗原研究室）
    // 2026-08-24 に確定（丸山確認）。研究責任者は栗原一貴、所属の表記は下の institution。
    // 倫理審査の申請書に書いた研究責任者と同じ表記である。
    pi: "栗原一貴",
    // 問い合わせ用のメールアドレス。参加者がそのまま送れる宛先にする。
    //
    // 表示の仕方について（2026-08-22 の判断）。迷惑メール収集よけに
    // 「qurihara [at] gmail.com」と崩して書く手もあるが、**そのまま載せる**ことにした。
    // 理由は3つ。
    //   1. 問い合わせるのはクラウドソーシングの参加者で、報酬の承認がかかっている。
    //      打ち直しを求めると、その場で問い合わせをやめてしまう人が出る。
    //   2. 同意画面では mailto: のリンクにしてある（押せばメーラーが開く）。
    //      [at] 表記だとリンクにできない。
    //   3. 3ページとも <meta name="robots" content="noindex,nofollow"> を入れてあり、
    //      URL は募集サイトの中でしか配らないので、収集ロボットに拾われる筋が薄い。
    email: "qurihara@gmail.com",
    // 研究の実施機関。
    institution: "津田塾大学 栗原研究室",
    // **同意画面の「お問い合わせ：」に出す名前**。2026-08-25 に丸山判断で
    // 個人名（pi＝栗原一貴）から**研究室名**に変えた。
    // 問い合わせは研究室として受けるものなので、窓口の表示に個人名を出さない。
    // ⚠ pi のほうは変えないこと。**研究責任者としての表記**（同意の撤回の案内文
    //   「確認のうえ対応します（栗原一貴／津田塾大学 栗原研究室）」など）に使っており、
    //   倫理審査の申請書に書いた研究責任者と同じ表記でなければならない。
    contact_name: "栗原研究室",
    // データの保管期間と廃棄の方針。同意画面ではこの文のうしろに
    // 「保管するのは回答と技術情報だけで…」が続くので、句点で終える。
    //
    // ■ 2026-08-24 に確定（丸山決定）。暫定の「研究成果の公表後3年間保管し、
    //   その後に削除します」から、**姉妹研究 Kikiwake の書き方にそろえた**。
    //
    //   **なぜそろえるか。** Kikiwake は同じ研究室・同じ倫理審査委員会に出したもので、
    //   「匿名化されたデータは本プロジェクトの運営中は保管する／研究期間の終了前に
    //   継続審査を申請する」という、**年数で期限を切らない書き方で審査を通した実績**がある。
    //   同じ委員会に別々の基準の書類を出すほうが、かえって説明が要る。
    //
    //   ⚠ **Kikiwake との違いも押さえておくこと。** Kikiwake はメールアドレスを集めるので
    //   「論文発表後に個人情報を削除する」と書いている。**この実験は個人情報を一切集めない**
    //   （記録するのはクラウドソーシングの作業者IDだけで、氏名も連絡先も無い）。
    //   だから「個人情報を削除する」ではなく、**そもそも個人を特定できるデータが無い**と
    //   書くほうが正確である。その一文は同意画面のこの直後（prod_common.js 側ではなく
    //   transfer.js の consentExtraHTML）に続く。
    //
    //   倫理審査の申請書（docs/ethics_application_draft.md 10.4）と
    //   docs/informed_consent.md にも同じ文言を書いてある。**3か所そろえること。**
    retention: "収集したデータは本プロジェクトの運営中は保管し、研究終了時に完全に削除します。"
             + "データはGoogle Cloud Firestore（東京リージョン）に暗号化して保存し、"
             + "研究責任者と共同研究者のみがアクセスします。",
    // 募集サイトのメッセージ機能でも問い合わせを受けるか。
    // true なら「募集サイトのメッセージ機能でも受け付けます」の一文を足す。
    via_crowdsourcing: true,
    // まだ確定していない項目の名前。**先生に確認したら、この配列を空にする**。
    // 上のコマンドがここを読んで「暫定」として並べるので、確認漏れに気づける
    // （【要確認】を値の中に書いてしまうと、そのまま参加者の画面に出てしまうため、
    //   印は値ではなくこの配列で持つ）。
    // 2026-08-24: pi・retention とも確定したので空にした（暫定の項目はもう無い）。
    provisional: [],
  },

  // ---- 記録 ---------------------------------------------------------------
  logging: {
    // 送信に失敗したときの作り直し。名簿の問い合わせと同じ一過性の失敗に備える。
    // 応答は読めない作りなので(no-cors)、判断できるのは「送信そのものが失敗したか」だけ。
    // 通信が切れている間の回答を取りこぼさないための保険である。
    retry: {
      attempts: 3,
      delays_ms: [800, 2500, 6000],
      jitter_ms: 400,
    },

    // 回答の送信先(GAS ウェブアプリの /exec URL)。
    // **prod_common.js の SUBMIT_URL はわざと空のままにしてある**。あちらは実験1
    // (乙課題・frac課題)と共用で、そこを埋めると既存の実験の保存先まで動いてしまう。
    // そこで転写検証だけの送信先をここに持ち、transfer.js が自分で送る。
    // 通常は roster.status_url と同じURL(同じデプロイ)。
    submit_url: "https://script.google.com/macros/s/AKfycbw1bnn7H3HCYuD3fDUIi1djGcnea0cVqnA15XiN5ipgwlrsCNkQx4q16-bdN0wbGQRP/exec",
    // 保存レコードの modality 列。GAS 側の採点分岐に合わせる
    // (client 申告の target_char で採点する系統。gas/transfer_patch.md 参照)。
    modality_audio: "transfer_audio",
    modality_visual: "transfer_visual",
  },
};
