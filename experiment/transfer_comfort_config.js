// =========================================================================
// 見え心地の評価（群C）設定ファイル
//   読むページ: experiment/transfer_comfort.html
//   読むコード: experiment/transfer_comfort.js
//   計画書:     project/実験計画書_転写検証.md（3章 RQ3・4.1・4.5）
//
//   ■ 群Cとは何か
//   「生成した文字アニメーションを、字幕として続けて見ていられるか」だけを
//   聞く独立の実験である。2026-08-24 から、4方式に加えて**ステップ表示**
//   （従来型の字幕を模した「一気に出す」見せ方）も並べて比べる。もとは群B（検証・視覚）の識別課題が全部終わったあとに
//   くっつけていたが、2026-08-21 に切り出して別の集団・別の掲載にした。理由は3つ。
//     (a) 90問以上の識別課題のあとでは疲れが混ざり、印象が課題の疲労に引きずられる
//     (b) 独立させれば「打ち切りなしで何度も繰り返し流す」提示にできる。
//         字幕は本来くりかえし流れるものなので、そのほうが実態に近い
//     (c) 群Bの負担が減る（識別課題だけで終われる）
//   掲載は検証フェーズ（群B）と**並行**して行う。別の集団なので日程は延びない。
//
//   ■ このファイルの位置づけ
//   描画のパラメータ（キャンバスの大きさ・4方式の描き方・進み方の表）は
//   transfer_config.js（群A′・群Bと共用）を**読むだけ**で、こちらには持たない。
//   同じ絵を出さないと群Bの結果と並べて論じられないためである。
//   ここに置くのは「群Cだけの手続き」＝使う字・提示のしかた・間合い・質問・保存先の分岐。
//
//   ■ 数値を触るときにここだけ見ればよい項目
//     1. single_char / sequence … 使う字（事前固定。結果を見てから選び直さない）
//     2. families               … 見せる5方式（4方式＋ステップ表示）
//        family_renderer / presentations_by_family … ステップ表示だけの例外
//     3. presentations          … 提示のしかた3通り（1字／横に並べる／その場で入れ替える）
//     4. timing                 … 繰り返しの間合いと、字と字のあいだの空き
//     5. layout                 … 1字ぶんの大きさと字間
//     6. items / choice         … 7件法の3項目と、最後の2問
//     7. roster                 … 名簿サーバの使い方（重複参加の照合）
// =========================================================================
window.TRANSFER_COMFORT_CONFIG = {
  // 設定の版。全レコードの config_version 列に入る（どの数値で取ったデータか分かる）。
  // prov = provisional（暫定）。A2 のあとに確定版へ差し替える。
  config_version: "comfort-prov-2026-08-30b",

  // ---- 名簿（サーバ）での扱い ----------------------------------------------
  // 較正フェーズ（群Acal・群A′）とも検証フェーズ（群B）とも重ならない
  // 独立集団にする。判定はサーバの参加者名簿でしかできないので、既定では
  // サーバに繋がらないときは課題を始めさせない。
  //
  // ✓ **いまの保存基盤（Firestore）は phase="comfort" を知っている**
  //   （2026-08-21 に対応済み。transfer_config.js の phases.comfort = ["c"] と
  //   phase_blocks に入っていて、4方向すべての重複拒否が疎通確認で通っている。
  //   掲載前チェックリスト H-1／C-0）。**掲載前にサーバ側でやることは無い。**
  //
  //   重複拒否の向きは4本ある（群Cと検証フェーズは同時期に走るので、どちら向きにも塞ぐ）。
  //     ・comfort に来た人が calib にいれば断る ／ test にいれば断る
  //     ・test    に来た人が comfort にいれば断る
  //     ・calib は一番先に走るので断る相手がいない
  //
  // ⚠ GAS へ切り戻すときだけ注意。gas_transfer/code.gs にも同じ規則を書いてあるが、
  //   **まだデプロイしていない**。デプロイしないまま GAS を主にすると、
  //   require_server が true なので群Cの参加者が全員お断り画面になる
  //   （掲載前チェックリスト H-1b）。
  roster: {
    phase: "comfort",
    group: "c",
    // false にすると、サーバが答えないときでも参加者IDのハッシュで連番を決めて
    // 続行する（重複の照合はできない。研究者の動作確認と、緊急時の逃げ道）。
    require_server: true,
  },

  // ---- 代表字（事前固定・結果を見てから選び直さない） ------------------------
  // ターゲット8字の中から事前に決め打ちしてある。
  // 「効きそうな字だけ後から選んだ」と言われないための事前固定である。
  //
  // **字の種類は増やさない。** 群Cで比べたいのは「現れ方（4方式）」と
  // 「5文字続けるときの出し方（3通り）」であって、字の難しさではない。
  // 字を増やすと本数が倍々に増え、所要が伸びるだけで問いに答えられる量は増えない。
  //
  //   single_char … 1字だけ出す条件で使う字
  //   sequence    … 5字続けて出す条件で使う並び。
  //                 **意味のある語にしない**（言葉として読めてしまうと、
  //                 表示の見え心地ではなく語の読みやすさを答えられてしまう）。
  //                 あ・か・し・ま・ら は音の種類がばらけていて語を作らない。
  single_char: "あ",
  // ⚠ sequence は **5字を並べる出し方をやめた 2026-08-29 以降、字の大きさを決める
  //   ためだけに残してある**（charPx が「5字を横に並べたときの1字ぶん」で計算する）。
  //   ここを変えると字の大きさが変わり、既存13本のデータと見え方がそろわなくなる。
  sequence: ["あ", "か", "し", "ま", "ら"],

  // 予定人数。**32（8字 × 方格の回し方4通り）の倍数にすること。**
  // 224 = 32 × 7 なら、どの字も112人が見て、条件ごと・方式ごとの本数も揃う。
  // check_comfort_render.js がこの人数で釣り合いを検算する。
  planned_n: 224,

  // ---- 字の割り当て（1人に4字・ラテン方格）-----------------------------------
  // 16本すべてを同じ1字にすると、その字の結果は、割り当てられた人たちだけから
  // 出てくる。その人たちが辛口なら辛口に出る。字の性質か人の癖か分けられない
  // （栗原先生の指摘・2026-08-29「人物依存度が低いほうがよい」）。
  // 1人に4字を配り、4方式 × 4条件の16マスへ方格で並べる。どの条件も、どの方式も
  // その4字を1回ずつ使うので、1人の中で比べたときに字の違いが打ち消える。
  //
  // ---- どの字を出すか：外さずに、層で分ける -----------------------------------
  // **8字すべて出す。**「うまくいかない字」を落とすと、都合のよい字だけが残り、
  // 「測定だけで適否を判定できる」という主張の足元が崩れる（丸山判断・2026-08-29）。
  //
  // かわりに、**曲線をきちんと推定できたかどうかで層に分けて報告する。**
  // 判定は目視ではなく、生成工程がすでに出している2つの値で機械的に決める
  // （project/data_calib2_live/warp_exp_c/midpoints_exp_c.csv）。
  //
  //   基準1  U_distinguishable_from_L … 到達点が当てずっぽう水準と区別できるか
  //   基準2  T50_status === "ok"      … 中点が測った範囲の中で一意に決まるか
  //
  //   目標＝正答率 : 両方満たすのは6字（あ・か・し・ぱ・ま・ら）
  //                  が … 到達点が当てずっぽうと区別できない（2.9%）→ degenerate
  //                  つ … 中点が測った範囲（10ms〜）より前 → censored_low
  //   目標＝情報量 : **8字すべてが両方を満たす**
  //
  // この「正答率では6/8、情報量では8/8」という差そのものが、情報量を目標に取る
  // 根拠になる。
  //
  // ⚠ **この基準は「事前規定」ではない。** 到達点・中点・正答率か絞れ具合か、の
  //   扱いは、較正の実データを見ながら詰めてきたものである（2026-08-25〜29）。
  //   言えるのは「**実験Cを実施する前に、機械的な分類の基準として固定する**」
  //   までであって、「データを見る前から決まっていた」ではない。
  //   論文にもそう書くこと（結果を見て8字から都合よく選び直すわけではない、
  //   という主張はこれで十分に立つ）。
  //
  // ■ 分析は二層で報告する（丸山判断・2026-08-29）
  //   層1「全入力」  … 8字すべて。アルゴリズムが実際に返したものをそのまま扱う。
  //                    ＝この方式を任意の音声に当てはめると何が起きるか
  //   層2「曲線を十分に推定できた入力」… 上の基準1・2を両方満たす字に限る。
  //                    ＝較正データが十分なとき、方式のあいだで評価がどう変わるか
  //   書き方の例: 「全文字では〇〇の傾向が見られた。識別曲線を十分に推定できた
  //   文字に限ると、その傾向はより明瞭になった。」
  //   「が」については「正答率の上昇がほぼ得られなかったため、正答率にもとづく
  //   2条件は同一の表示を生成した。回答分布を用いる条件では異なる表示が生成された」
  //   と書く。**これは除外の理由ではなく結果である。**
  //
  // ⚠ 「が」では、正答率にもとづく①と③が**完全に同じ絵**になる（どちらも等速）。
  //   これを**実験の成立条件にしない**（丸山判断・2026-08-29）。
  //   「入力された音声の識別曲線によっては、違う写し方が同じ表示を返すことがある」
  //   のは、除外の理由ではなく**手法の出力そのもの**である。同じになったなら、
  //   同じになったこと自体を観測する。
  //   並べて選ばせる問いにも「が」を入れる。①と③が物理的に同じ絵なので、
  //   選ばれる率は**期待値として同じ**になる（224人でも偶然の差は出るので
  //   「同じになるはず」とは言えない）。片方だけが明らかに多く選ばれたら、
  //   位置の効果など別のものを拾っていることを疑える。
  //   ⚠ この検算を成り立たせるため、**並べる位置を系統的に釣り合わせている**
  //   （transfer_comfort.js の showChoiceCondition。巡回4通り × 左右反転2通りで、
  //    どの条件もどの位置に同じ回数だけ出る。224人なら各112回）。
  //
  // ⚠ 「つ」の③の中点は**境界の値**である。10ms以下のどこかを10msに置いた
  //   （外挿はしない方針）。推定値ではない。論文・資料に必ず明記すること。
  char_rotation: true,
  chars_pool: ["あ", "か", "が", "し", "つ", "ぱ", "ま", "ら"],


  // ---- 合わせ方の4条件（実験Cの主要な問い）-----------------------------------
  //   ①② 進み方にむらが出る … 音が急に分かるところで一気に進み、分からないところで止まる
  //   ③④ 一定の速さで進む   … 進み方は変えず、始まる時刻だけをずらす
  // **全員が4条件すべてを見る。**
  // 表は experiment/tools/merge_warp_comfort.py が4ファイルを1つにまとめたもの。
  conditions: [
    { key: "c1_acc_shape",  no: "①", label: "正答率揃え（形まで）" },
    { key: "c2_info_shape", no: "②", label: "情報量揃え（形まで）" },
    { key: "c3_acc_mid",    no: "③", label: "半分・正答率" },
    { key: "c4_info_mid",   no: "④", label: "半分・情報量" },
  ],
  // 4条件ぶんの進み方の表。**群B用の transfer_warp.json とは別ファイル。**
  warp_tables_url: "transfer_warp_comfort.json",

  // ---- 提示のしかた（3通り） -------------------------------------------------
  //   single … 1字だけ（従来どおり）
  //   row5   … 5字を左から1字ずつ出し、**出た字はそのまま残す**（字幕的な見せ方）
  //   swap5  … 5字を**同じ位置で1字ずつ入れ替える**
  // 2026-08-29 から**1字だけ**にした。5字を並べる出し方はやらない。
  // そのぶん本数が空いたので、方式を人ごとに割り当てるのをやめ、全員が全部見る。
  presentations: ["single"],
  presentation_labels: {
    single: "1字だけ",
    row5: "5字を左から並べて、出た字は残す",
    swap5: "5字を同じ場所で入れ替える",
  },

  // 見せる方式。フェード・部分表示・ぼかし解除・ワイプ、それに **ステップ表示**。
  //
  // ■ ステップ表示（"step"）とは何か・なぜ足したか（2026-08-24・丸山決定）
  //   ある時刻まで**何も出さず**、その時刻に一気に完成形を出す見せ方である。
  //   時刻は「目標の音声曲線で半数が識別できる時刻（中点）」で、生成工程が字ごとに
  //   計算して transfer_warp.json の step_mid_ms に入れる（表が無いときは
  //   transfer_config.js の visual.warp.fallback_step_mid_ms を使う）。
  //
  //   ねらいは、**いまの字幕との比較**である。既存の字幕は語が確定してから文字を出すので、
  //   「分かりかけ」の中間の状態を持たない。ステップ表示はそれを模した
  //   「**徐々に出すのをやらなかった場合**」の代表であり、
  //   「従来型の字幕と、段階的に出す表示の、どちらが続けて見ていられるか」を直接聞ける。
  //
  //   ⚠ **群Cでは識別の測定をしない**ので、「従来型は音声の曲線からどれだけ離れているか」
  //   という数字は出せない。その点は論文の考察で述べる範囲にとどめる（計画書 4.5）。
  //
  //   絵を描くのはフェードの描画器である（濃さが0か100かのどちらかしか出ないだけで、
  //   描き方そのものはフェードと同じ）。対応は下の family_renderer にある。
  // 2026-08-29 から4方式。**ステップ表示（一気に出す）はやめた。**
  //   4方式 × 4条件 = 16本を、全員が見る。
  //   うすい・点が増えるも外さない。分解能や時間への強さで劣るぶんは
  //   「自然さ」の点数が低く出るだけで、それ自体がデータになる（丸山判断）。
  families: ["fade", "reveal", "blur", "wipe"],

  // 方式名 → 実際に絵を描く描画器の名前。書いていない方式は同じ名前の描画器を使う。
  // ステップ表示は独立した描画器を持たず、フェードの描画器に進み具合0か1を渡すだけである
  // （transfer.js と transfer_comfort.js の RENDERERS の表は、群Bと群Cで同じ絵を出すために
  //   1文字も違ってはいけない決まりなので、そこには足さない）。
  // ステップ表示をやめたので、いまは借りる対象が無い（2026-08-29）。
  family_renderer: {},

  // 方式ごとに「この提示のしかただけ出す」と絞る表。書いていない方式は presentations 全部。
  //
  // ステップ表示だけ **1字条件に絞る**（2026-08-24）。5方式 × 3通り = 15本にすると
  // 所要が延びて、1人あたりを軽くしたいという方針に反するためである。
  // これで合計は 4方式×3通り ＋ ステップ×1通り = **13本**になる。
  // ステップ表示で聞きたいのは「一気に出る表示は続けて見ていられるか」であって、
  // 5字続けるときの出し方の良し悪しではないので、1字条件だけで用は足りる。
  // ステップ表示をやめたので、いまは絞る対象が無い（2026-08-29）。
  presentations_by_family: {},

  // 評価に使う進み方。"proposed" は生成した表（transfer_warp.json）、"linear" は等速。
  // 表が無いときは等速で代用する（研究者の動作確認のとき。記録に progress_source が残る）。
  // ⚠ 旧・単一条件の指定。**2026-08-29 に廃止**（条件は1本ごとの属性になった）。
  //   等速の対照だけを出したいときは "linear" を入れる（研究者の動作確認用）。
  force_condition: null,

  // ---- 繰り返し再生と、5字続けるときの間合い ---------------------------------
  // 群Cを切り出した理由のひとつが「字幕はくりかえし流れるものなので、
  // 1回こっきりで印象を聞くのは実態と違う」ことである。そこで評価しているあいだ、
  // 同じアニメを自動で流しつづける。
  //
  // ■ この条件で測りたいこと・測りたくないこと
  //   測りたいのは「字幕として続けて見ていられるか」だけである。
  //   **前後の字がお互いの見え方に干渉する現象（視覚的マスキング）は測らない。**
  //   それは連続提示そのものを扱う次の実験の領域なので、ここでは
  //   **干渉が起きない十分な間隔を空けたうえで**、見え心地だけを聞く。
  //
  // ■ inter_char_gap_ms = 300 の根拠
  //   視覚的マスキング（先に出た刺激と後から出た刺激が互いの処理を邪魔する現象）は、
  //   2つの刺激の**開始どうしの間隔（SOA）が0〜200ミリ秒**の範囲で起きるとされる。
  //   文字の識別に対する抑制は約60〜120ミリ秒で最も強く、120ミリ秒あたりで消える
  //   （Enns & Di Lollo のレビュー、経頭蓋磁気刺激による文字識別の抑制の研究など）。
  //   アニメ本体が300ミリ秒なので、**現れきってから300ミリ秒空ける**と、
  //   字と字の開始の間隔は 300 + 300 = **約600ミリ秒**になる。
  //   これは干渉が起きる時間帯（200ミリ秒以下）の3倍離れている。
  //
  //   ⚠ 2026-08-24: アニメ本体の長さ（transfer_config.js の visual.base_anim_ms）を
  //   600→300ミリ秒に変えた（群Bの対照条件を公平にするため。あちらの注記を見ること）ので、
  //   字と字の開始の間隔は 900→600ミリ秒に縮んだ。それでも干渉の帯域（200ミリ秒以下）の
  //   3倍あるので、inter_char_gap_ms は 300 のまま据え置いた。余裕をもっと取りたければ
  //   ここを 600 にすれば元の900ミリ秒に戻る。機械チェック
  //   （experiment/tools/check_comfort_render.js）は「アニメ＋空き が400ミリ秒未満」を
  //   不合格にするので、ここを削るときは注意すること。
  //
  // ■ 読み上げの速さとの関係
  //   録音した自然音声の実測（experiment/transfer_audio_manifest.json の全長）では、
  //   かな1字の長さは**ターゲット8字で303〜419ミリ秒（平均347）**、
  //   全68かなでも250〜451ミリ秒だった。字と字の間隔600ミリ秒は、
  //   その**1.7倍ほどゆっくり**にあたる（900ミリ秒のときは2.6倍だった）。
  //   読みを5文字続ける速さを土台にしつつ、
  //   前後の字が影響し合わないところまで十分に間を空けた、という位置づけである。
  //
  // ■ 横並び（row5）の字間について
  //   横並びでは出た字が残るので、残った字と新しく出る字が**空間的に隣り合う**。
  //   これは時間のマスキングではなく、隣の字が邪魔をする現象（crowding・側方抑制）の
  //   領域にあたる。対処として**字と字のすき間を1文字分以上あける**
  //   （layout.gap_ratio を1.0以上にする）。
  //
  //   hold_ms … 全部現れきった姿のまま止めておく時間
  //   gap_ms  … いったん消してから、次に流しはじめるまでの間
  timing: {
    // 1字が現れきってから、次の字が現れはじめるまでの間（ミリ秒）。上の根拠を参照。
    inter_char_gap_ms: 300,
    // 1字だけの条件の間合い。
    // （2026-08-29 深夜に hold_ms:0 へ変えたが、丸山さんの意図の読み違いだったので
    //   即日戻した。終端の字は 1200ms 据え置いてから消す。）
    single: { hold_ms: 1200, gap_ms: 300 },
    // 5字続ける条件の間合い（全部出そろってから消すまで／消してから次の周まで）。
    sequence: { hold_ms: 1500, gap_ms: 500 },
    // 自動での繰り返しをここで打ち切る（回答が長引いたときの保険。0で無制限）。
    max_cycles: 40,
  },

  // ---- 画面の並べ方 ---------------------------------------------------------
  // **3条件とも1字あたりの表示の大きさをそろえる。** 1字だけの条件を大きく出すと、
  // 「提示のしかたの違い」ではなく「字の大きさの違い」を答えられてしまうため、
  // 5字を横に並べたときの1字ぶんの幅を、3条件すべてで使う。
  layout: {
    // 字と字のすき間 ÷ 字の幅。1.0 で「1文字分あける」（crowding 対策。上の注記）。
    gap_ratio: 1.0,
    // 1字ぶんの表示幅の上限px（広い画面で大きくなりすぎないように）。
    max_char_px: 120,
  },

  // 「▶ もう一度みる」ボタンを出すか。押した回数は記録する。
  allow_replay: true,

  // ---- 各アニメのあとに聞く7件法3項目 ---------------------------------------
  // 1=まったくそう思わない 〜 7=とてもそう思う。
  // annoying（目障り・わずらわしい）だけ向きが逆なので、集計のときは反転すること。
  // ⚠ **2026-08-29 に1項目目を差し替えた。**
  //   旧:「この表示は継続して見やすい」→ 新:「見ていて快適だ」
  //   理由: 「続けて見ていられるか」は visual comfort（目や知覚の負担が少ないこと）と
  //   「続けて見たい」（文字への興味・アニメの面白さ）が混ざる。1項目で2つの概念を
  //   聞くと、何を測ったのか言えない。フォント・表示の研究で使われている
  //   visual comfort に絞る。長時間見ていられるかは、文章全体で評価する段階になったら
  //   visual fatigue / acceptability として別に測るのが筋である。
  //
  //   3項目は互いに別の概念で、**1つの指標にまとめない**（主要な検定も3項目それぞれ
  //   1回ずつの計3回）。
  //     visual comfort … 見ていて負担でないか
  //     naturalness   … 変化のしかたが自然か（進み方のむらの本丸はここ）
  //     annoyance     … わずらわしくないか（向きが逆の項目）
  //   「滑らかさ」は入れない。コマ間が滑らかでも「途中で急に遅くなる」動きは
  //   変に感じられるので、時間の変形そのものを聞くなら自然さのほうが本丸に近い。
  items: [
    { key: "comfort", text: "この文字の表示は、見ていて快適だ" },
    { key: "natural", text: "文字の変化のしかたは、自然に感じる" },
    { key: "annoying", text: "文字の変化を、わずらわしく感じる" },
  ],
  scale_min_label: "まったくそう思わない",
  scale_max_label: "とてもそう思う",

  // ---- 最後の質問（2問に分ける） ---------------------------------------------
  // 聞きたいことが2つある——「どの**現れ方**（方式）がよいか」と
  // 「5文字続けるとき、どの**出し方**がよいか」。1問にまとめると、
  // 参加者がどちらについて答えたのか分からなくなるので、**別々の問いに分ける**。
  //
  // 2問目の見本は、1問目で選ばれた方式で出す。こうすると、参加者は
  // 「自分が good と思った現れ方で、横に並べるか入れ替えるか」を見くらべられる。
  choice: {
    // ---- 4条件を並べて選ばせる（2026-08-29 から。方式ごとに1問）--------------
    // 7件法は「どれくらい悪いか」を測る。こちらは**集団として特定の条件への選好が
    // あるか**を測る。
    //
    // ⚠ **「見分けられるか」を測るものではない。** 4条件を完全に見分けていても、
    //   どれが良いかの好みが割れれば選択は4分の1ずつになる（外部AIのレビュー・
    //   2026-08-29）。強制選択が測るのは選好であって弁別ではない。
    //   結果は「選択率が25%付近 → 明確な選好は観察されなかった」までとする。
    //
    // ⚠ **説明の言葉を出さない。** 「だんだん濃くなる」のように現れ方を言葉で
    //   説明すると、参加者の注意がそこへ向いて答えが偏る（丸山指摘・2026-08-29）。
    //   見本と選ぶボタンだけを出し、方式の名前も条件の名前も画面に出さない。
    //
    // ⚠ **ぼやけ・端からの2方式だけで聞く。**同じ方式の見本を4つ同時に
    //   再生するため。うすい・点が増えるは描画の作業領域（revealState など）を
    //   共有していて、並行再生で絵が壊れうる。ぼやけ・端からは壊れないことを
    //   実測で確認済み（2026-08-29）。
    //   （以前は「③④が完成しない」も理由だったが、2026-08-30 に③④を
    //    完成まで再生する形にしたため、この理由は消えた。）
    condition: {
      families: ["blur", "wipe"],
      // ⚠ **「字幕として」と聞かない（2026-08-29・外部AIのレビュー）。**
      //   この実験は1字しか出さない。字幕は本来並ぶものなので、参加者に見せて
      //   いない「字幕での使用」を想像させることになる。見せたものだけを聞く。
      text: "この4つのうち、<b>文字の現れ方として最も見やすいもの</b>を選んでください。",
      note: "4つの見本がくりかえし流れています。見くらべてから選んでください。",
      show_samples: true,
    },
  },

  // ---- 記録 -----------------------------------------------------------------
  logging: {
    // 保存先と再送の設定は transfer_config.js の backend / firestore / logging を
    // そのまま使う（**既定は Firestore**。GAS は切り戻し用の受け皿として残してある）。
    // ここで決めるのは行き先の入れ物だけ。
    // "transfer_wellbeing" は群Bの見え心地用に作ってあるコレクションで、
    // 列はそのまま使える（phase 列に "comfort"、group 列に "c" が入る）。
    //
    // 同じ入れ物に3種類の行が入るので、**record_kind 列で見分ける**
    // （transfer_comfort.js が付ける）。
    //   "clip"    … 1本ぶんの中間レコード（12行／人・保険。分析には使わない）
    //   "final"   … 1参加者1行の本記録（**分析はこれを読む**）
    //   "session" … 完走レコード（承認の照合はこの1行で済む）
    modality: "transfer_wellbeing",
  },
};
