# WORKLOG

> セッションをまたぐ作業状態の記録。プロジェクトの全履歴は `project/project_log_260723.md`、
> セットアップと全体像は `project/handover_260723.md` を参照。

## 2026-08-21（深夜） 掲載前の点検で見つかった実害を直した（記録の取りこぼし・問題数の嘘・同意画面の不足）

### 概要
掲載直前の点検で見つかった4つの実害と、参加者向け文言のずれを直した。
**いちばん重いのは群C（見え心地）の記録がまるごと消えうる作りだったこと**で、
1参加者1レコードしかないのに送信を待たずに完了コードを出していた。
あわせて、画面に出る問題数が実装と最大43問ずれていたのを直し、
承認の判定に使える「完走レコード」を足し、同意画面に倫理審査で要る4項目を入れた。

### 直したこと（実害）

| 何が起きていたか | どう直したか |
|---|---|
| **群Cの記録が全損しうる**。`submitAndFinish()` が送信を待たずに完了コードを出していた。1参加者1レコードなので、この1件が落ちると完了コードがサーバのどこにも残らず、最後までやった人を非承認にしてしまう | 送信を待って作り直す → 駄目なら**完了コードを出す前に**「送れませんでした・再送する」の画面 → 手の再送も駄目なら、コードを出したうえで「このコードと時刻を控えて問い合わせて」と案内。保険として**1本ごとの中間レコード**（`record_kind="clip"`・12行／人）も送る。閉じられても開き直せば送り直す（未送信の記録を端末に控える） |
| **画面の問題数が嘘**。「約91問」と出しながら実際は 108／134／94 問（最大43問ずれ） | 推定式をやめ、**実際に組み立てた出題の配列の長さ**を出す。そのために出題を「進め方」の画面の時点で組み立てるようにした（本番へ入るときは組み立て済みのものを使う） |
| **完走を示す記録が無かった**。承認の判定を1問1行の記録の行数から推測するしかなく、通信が悪くて取りこぼした人を非承認にしかねなかった | 最後の画面に着いたときに**完走レコード**を1行送る（`record_kind="session"`）。`completion_code` / `n_trials` / `duration_s` / **`send_failures`（送れなかった件数）** が入る。**承認はこの1行だけで済む** |
| 掲載文の「100問ほど」「およそ10分」が実装とずれ | 「約110〜135問」「約10〜12分」の幅表記に。集団で問題数が違うが掲載は1本なので幅で書く |

### 直したこと（文言）
- タブ名「かなの課題（前半の実験）／（後半の実験）」→ **「ひらがなの聞き取り・見え方の課題」**。
  参加者は較正フェーズか検証フェーズの**片方しか通らない**ので「前半／後半」は誤解を招く。
  研究者がフェーズを見分けられるよう、`?prod=1` が無いときだけ JS がタブ名の後ろに
  「［研究者確認：calibフェーズ］」を足す。
- 同意画面に**データの保管期間・同意の撤回の方法・問い合わせ先（研究責任者・連絡先）**を追加。
  `docs/informed_consent.md` が要求している項目。`prod_common.js` は実験1と共用なので触らず、
  転写検証側の `tidyConsentScreen()` で足している。
- 掲載文の手順の順序を実装に合わせた（**同意 → 進め方 → 音量／見え方の確認 → 練習 → 本番**）。
- 「4つの集団」→ 5集団。群Cの「GASに comfort を足すこと」警告は対応済みなので削除。
- 研究者モードでは**完了コードを出さない**ようにした（照合できない使い捨てのコードを
  控えてしまう事故を防ぐ）。かわりに問題数・所要・送信失敗件数を出す。

### ⚠ ユーザーが埋める値（掲載前に必須）
`experiment/transfer_config.js` の `contact` が**プレースホルダのまま**。
ここ1か所で3ページの同意画面と、群Cの「送れませんでした」画面の問い合わせ先が変わる。

| 項目 | 埋めるもの |
|---|---|
| `contact.pi` | 研究責任者の氏名・所属 |
| `contact.email` | 参加者がそのまま送れる問い合わせ用メールアドレス |
| `contact.retention` | データの保管期間と、期間経過後に削除する旨 |

埋め残しの確認は掲載前チェックリスト I-1 の node ワンライナー。

### ⚠ 消す必要のある行（Firestore）
動作確認中に、目印の付かない名簿の行を1件作ってしまった。
**`transfer_roster/calib_anon-eiq54sm9`**（集団 acal・`assign_index` 10）。
`?group=acal` を付けたつもりが、ローカルの静的サーバが `.html` を落とすリダイレクトで
クエリごと消え、名簿の問い合わせに進んでしまったもの。`is_test` が付かないので
`purge_transfer_firestore.py` では消せない。掲載前に手で消すこと（チェックリスト C-0b2）。

```bash
firebase firestore:delete transfer_roster/calib_anon-eiq54sm9 --project ifont-transfer --force
```

### 記録の形が増えた（`transfer_wellbeing`）
同じコレクションに3種類の行が入るようになった。**`record_kind` 列で見分ける。**
新しいコレクションを作るとルールの置き直し（`firebase deploy`）が要るので避けた。

| `record_kind` | 中身 | 分析に使うか |
|---|---|---|
| `final`（または空） | 見え心地の回答。1参加者1行 | **使う** |
| `clip` | 群Cの1本ぶんの中間レコード（12行／人） | 使わない（全損防止の保険） |
| `session` | 完走レコード。較正・検証・群Cのどの集団も送る | 使わない（**承認の判定に使う**） |

`export_transfer_firestore.py` と `gas_transfer/code.gs` の列にも足した。

### 検算のしかた（回帰の防ぎ方）
`node experiment/tools/check_transfer_stimuli.js` に、**`transfer.js` を実際に読み込んで
出題を組み立てさせ、その配列の長さを数える**検査を足した（推定式では数えない）。
出るべき値は **acal 108問 / aprime 134問 / b 94問**。
画面が推定式に戻っていないかも一緒に見ている。

### ファイル
| ファイル | 何を変えたか |
|---|---|
| `experiment/transfer.js` | v3.4→**3.5**。完走レコード、問題数を実物から数える、同意画面の3項目、研究者モードで完了コードを出さない、タブ名の研究者向け表示 |
| `experiment/transfer_comfort.js` | vc1.2→**c1.3**。送信を待つ・再送画面・中間レコード・完走レコード・同意画面の3項目・所要引数 4→7分 |
| `experiment/transfer_config.js` | `contact`（問い合わせ先・保管期間）を追加。`config_version` は据え置き（刺激の索引と一致させるため） |
| `experiment/transfer_comfort_config.js` | 誤記2件を修正（GASが comfort を知らない／保存先がスプレッドシート）。`record_kind` の説明を追加 |
| `experiment/transfer_calib.html` / `transfer_test.html` / `transfer_comfort.html` | タブ名・`?v=`・陳腐化したコメント |
| `experiment/tools/check_transfer_stimuli.js` | 問題数の検算（transfer.js を実際に走らせる）を追加 |
| `experiment/tools/export_transfer_firestore.py` | `transfer_wellbeing` に `record_kind` ほかの列を追加 |
| `gas_transfer/code.gs` | 切り戻し用に同じ列を追加（未デプロイ） |
| `project/掲載文_下見.md` | 問題数・分数・手順・集団数・承認の判定 |
| `project/掲載前チェックリスト.md` | D-1 に問題数の検算、**I（同意画面に出す値）**、**J（完走レコードと承認）**、C-0b2（目印の付かない行）を追加 |

### 残タスク
- [ ] `contact` の3項目を実際の値に書き換える（I-1）。書き換えたら `?v=` を両方上げる
- [ ] `transfer_roster/calib_anon-eiq54sm9` を消す（C-0b2）
- [ ] 掲載直前に採番を0に戻す（C-0c）
- [ ] 群Cの所要を自分で1回通して実測し、報酬を確定する（H-4・掲載文 6.2）
- [ ] `docs/completion_code_flow.md` と `docs/informed_consent.md` の全面書き直し（別実験の
      内容のままで陳腐化している。今回は同意画面の実装だけ直し、文書は手つかず）
- [ ] 検証フェーズ・本番較正フェーズの掲載文はまだ無い（下見と群Cの分だけある）

### 学び
- **画面に出す数値を式で書くと、必ず実装から遅れる。** 数えられるものは実物を数える。
- **研究者モードでもページは名簿に問い合わせる。** 動作確認で開くときは
  `?group=` / `?pnum=` で素通りさせるか `wid=uitest-…` を付ける。
  静的サーバのリダイレクトでクエリが落ちることがあるので、拡張子なしのパスに付ける。

## 2026-08-21（夜） 転写検証実験の保存基盤を Firestore へ移した

### 概要
名簿（集団の振り分け・連番の採番・フェーズをまたいだ重複拒否）と記録の保存を、
GAS ウェブアプリから **Firestore に移した**。GAS は設定2行で戻せる受け皿として残してある。
きっかけは 2026-08-21 昼に実機で出た「サーバに接続できませんでした」のお断り画面で、
GAS が応答の本体を別ドメインへリダイレクトして返す経路が一過性で失敗し、
JSON のかわりに HTML を返していたことが原因だった（再試行で緩和はしたが根治ではない）。

### Firebase プロジェクトは**コンソールを触らずに作れた**
`docs/FIRESTORE_TRANSFER_MIGRATION.md` の旧4章は「コンソールで10分の手作業が要る」と
書いていたが、`firebase` コマンドだけで完結した（丸山のアカウントはログイン済みだった）。

```bash
firebase projects:create ifont-transfer --display-name "iFont transfer"
firebase deploy --only firestore:rules --project ifont-transfer  # API有効化とDB作成も込み
firebase apps:create web "transfer-web" --project ifont-transfer
firebase apps:sdkconfig WEB <アプリID> --project ifont-transfer  # ウェブAPIキーが出る
```

| 項目 | 値 |
|---|---|
| プロジェクトID | `ifont-transfer` |
| 置き場所 | `asia-northeast1`（東京） |
| コンソール | https://console.firebase.google.com/project/ifont-transfer |

> **ハマりどころ**: `firebase deploy --only firestore:rules` はデータベースが無ければ
> 自分で作るが、置き場所が**既定のアメリカ（`nam5`）**になる。日本の参加者には遅いので、
> `firestore:databases:delete` → `--location asia-northeast1` で作り直した。
> **置き場所はあとから変えられない。中身が空のうちにしか直せない。**
> なお `(default)` を消したあと同じ名前で作り直せるまで**約5分待たされる**。

> 流用の可否: ログイン中のアカウントにあった Firebase プロジェクトは **Kikiwake だけ**。
> あちらは別の研究が動いている本番で、ルールが「ログイン済みの本人だけ」を前提に
> 組んである。転写検証はログインしない設計なので、流用するとルールを緩める改修が要る。
> **動いている研究のルールに手を入れるのは危ない**ので、新しく作った。

### 追加・変更したファイル

| ファイル | 何をするものか |
|---|---|
| `experiment/transfer_firestore.js`（新規） | Firestore の窓口。名簿の問い合わせと記録の保存。**ここに移行の中身がすべてある** |
| `firebase/firestore.transfer.rules` | セキュリティルール。全面的に書き直した（下の「設計判断2」を参照） |
| `firebase.json`（新規） | `firebase deploy` にルールの場所を教えるだけのもの |
| `firebase/firestore.transfer.indexes.json`（新規） | 索引は使わないので空。deploy に要る |
| `experiment/transfer_config.js` | `firestore` / `backend` / `phases.comfort` / `phase_blocks` を追加 |
| `experiment/transfer.js` | 名簿と記録の送り先を切り替えられるようにした。版 3.3 → **3.4** |
| `experiment/transfer_comfort.js` | 同上。版 c1.1 → **c1.2** |
| `transfer_calib.html` / `transfer_test.html` / `transfer_comfort.html` | `transfer_firestore.js` の読み込みを追加。`?v=` を全部上げた |
| `gas_transfer/code.gs` | 切り戻し用に、群Cと4方向の重複拒否を追加（**デプロイはまだ**） |
| `experiment/tools/test_transfer_firestore.js`（新規） | 疎通確認。32項目を本物の Firestore に書き込んで確かめる |
| `experiment/tools/export_transfer_firestore.py`（新規） | 解析用の取り出し。`analyze_transfer.py` がそのまま読める CSV を出す |
| `experiment/tools/purge_transfer_firestore.py`（新規） | 試し打ちの行だけ消す（GAS の `transfer_purge_test` と同じ役目） |
| `.gitignore` | サービスアカウントの鍵を除外 |

### 設計判断1: 採番にトランザクションを**書かなかった**理由

「読んで、1足して、書き戻す」だと同時に2人来たとき同じ番号を配ってしまう。
ふつうはトランザクションで囲むが、Firestore には **increment 変換**という
サーバ側で不可分に1増やす仕組みがあり、こちらのほうが強く往復も1回で済む。

さらに**増やしたあとの値が書き込みの応答に入って返る**（`transformResults`）ので、
「カウンタを読む」権限を与えなくても自分の番号が分かる。
ルール側で「`n` が 1 だけ増える更新」に限っているため、番号を飛ばすことも巻き戻すこともできない。

> 最初の1人だけ挙動が違う。まだドキュメントが無いところへ増やすので
> `create` として評価され、`n` は 1 になる。ルールに
> `allow create: if request.resource.data.n == 1` を入れておかないと**1人目が必ず失敗する**
> （元の計画は `n == 0` と書いてあり、そのままでは動かなかった）。

### 設計判断2: **名簿だけ「1件ずつの読み出し」を許した**（当初計画からの変更）

`docs/FIRESTORE_TRANSFER_MIGRATION.md` 3.1 の当初案は
「読み出しは全面禁止。名簿は create の成否だけで初参加/再訪を見分ける」だった。
**実装したら成り立たなかった。** 理由は2つ。

1. **開き直した人に、前と同じ集団・同じ連番を返せない。** create が失敗したことは
   分かるが、「では何に割り当てられていたか」が分からない。別の集団で再開させると
   音声と視覚の人数の釣り合いが崩れ、字×条件の割付（連番から決まる）も食い違う。
   GAS 版は `returning: true` で返していたので、**機能が後退してしまう**。
2. **フェーズをまたいだ重複参加を判定できない。** 「較正に出た人が検証へ来たら断る」を
   create の成否で見ると、**断るべき人を較正の名簿に載せてしまう**。

そこで名簿だけ `get`（ドキュメントIDを名指しでの1件読み）を許した。
`list`（一覧・検索）は禁止のままなので**名簿を端から舐めることはできない**し、
ドキュメントIDは `<フェーズ>_<参加者ID>` なので**参加者IDを知っている人しか引けない**。
名簿の中身は割り当てだけ（フェーズ・集団・連番・作業者ID）で回答は入らない。
**回答（`transfer_trials` / `transfer_wellbeing`）は読み出し全面禁止のまま**なので、
回答データの秘匿性は当初の設計から下がっていない。

### 設計判断3: 名簿を決める手順の**順番**

`transfer_firestore.js` の `resolveAssignment()`。順番に意味がある。

1. 同じフェーズの名簿に自分がいたら、**そのときの割り当てをそのまま返す**（開き直し）
2. 断るべきフェーズの名簿に自分がいたら、断る
3. **ここで初めて採番する** → 断られた人は番号を消費しない
4. 名簿に載せる（`exists: false` の条件付き。同時に2つのタブから来たら先着を正とする）

### 疎通と負荷の確認（すべて合格）

`node experiment/tools/test_transfer_firestore.js` で **32項目すべて合格**（2回実行）。

| 見たこと | 結果 |
|---|---|
| 連続16人の採番 | 交互に配られ、連番は 0..7 で飛びも重複もなし。8人 / 8人 |
| 24人が同時 | 24件成功。「集団＋連番」が24通りすべて別。飛びなし。12人 / 12人 |
| 60人が同時（別途） | 60件成功（1.2秒）。一意な割り当て 60/60。30人 / 30人 |
| 開き直し | 同じ集団・同じ連番に戻る。**番号を消費しない** |
| 重複拒否 | 較正→検証・較正→群C・群C→検証・検証→群C の**4方向すべて**効く |
| 断られた人 | 番号を消費しない |
| 回答 | 30件全部入る。見え心地も入る |
| ルール（8項目） | 回答の一覧・名簿の一覧・カウンタが読めない。名簿を書き換えられない。採番を飛ばせない。列の足りない回答が入らない。決めた4か所以外に書けない |

**実物のページでも確認した**（ローカルサーバでブラウザに読ませて）。

- 較正の入口: 集団 `acal`・`assign_source` が `firestore`・回答の送信が通る
- 群Cの入口: 集団 `c`・`assign_source` が `firestore`・見え心地の送信が通る
- キャッシュを消して開き直すと、サーバから**同じ割り当て**が返る
- **Firestore をわざと壊すと GAS に落ちて続行できた**（`assign_source` が `gas-fallback`。
  Firestore を3回試して約7秒 → GAS が応答。合計9.7秒）
- コンソールにエラーなし

**速さ**（東京に置いたので GAS より2桁速い）。

| | GAS | Firestore |
|---|---|---|
| 名簿の問い合わせ | 約6秒 | **0.1〜0.3秒** |
| 記録1件の書き込み | 応答を読めない | **中央値 31ms**（最小25 / 最大91） |

> **負荷の注意**: 1つの Node プロセスから**同時に300本**の接続を張ると半分ほど失敗する。
> これは **Firestore ではなく Node の fetch が一度に張れる接続数の頭打ち**である
> （同時100本までなら300件全部通る）。ブラウザは1つのサーバに6本程度しか同時に
> 張らないうえ参加者ごとに別の機械なので、実際の掲載でこの形にはならない。
> **この数字を「Firestore は300件で落ちる」と読み違えないこと。**

確認で作った行は Firestore・GAS とも消し済み（GAS 側は本物の 413 行を残して
試し打ち 8 行だけ削除）。

### 切り替えと切り戻し

`transfer_config.js` の `backend` を見る。いまは `roster: "firestore"` / `logging: "firestore"` /
`fallback: true`。**GAS に戻すのはこの2行を `"gas"` にするだけ。**
GAS のコードもデプロイも `/exec` URL もそのまま残してある。

> ⚠ **群Cだけは例外。** GAS へ戻して群Cを掲載するなら、先に `gas_transfer/` で
> `clasp push -f` → `clasp deploy` → **利用許可を与え直す**こと。
> 規則はコードに書いたが**デプロイしていない**ので、そのまま戻すと群Cの参加者が
> 全員お断り画面になる。較正・検証フェーズは今すぐ戻せる。

`assign_source` 列で、どちらの基盤で何回目に通ったかがあとから数えられる。

| 値 | 意味 |
|---|---|
| `firestore` / `firestore:retry2` / `firestore:retry3` | Firestore が主で、1〜3回目で通った |
| `gas-fallback` | Firestore がだめで GAS に落ちた（**増えたら要調査**） |
| `server` / `server:retry2` | GAS が主で通った（2026-08-20 までと同じ表記） |
| `cache` | 同じ端末で前に決まった割り当てを使った |
| `local_hash` | サーバに繋がらず参加者IDから手元で決めた |

### 鍵の扱い（**混同しやすいので明記**）

| 鍵 | 性質 | 置き場所 |
|---|---|---|
| **ウェブAPIキー**（`AIzaSy…`） | **公開してよい。** ブラウザに埋め込む前提の「宛名」で、合言葉ではない。守るのはルールのほう | `transfer_config.js`（コミット済み） |
| **サービスアカウントの鍵** | **本物の秘密。** 全データを読めるし消せる | `firebase/ifont-transfer-sa.json`（`.gitignore` 済み・**まだ作っていない**） |

### 残タスク

- [ ] **解析用のサービスアカウントの鍵を作る**（人の手作業。**これだけ**）。
      掲載や実験の実施には要らず、**データを取り出して分析するときに初めて必要**。
      コンソール → プロジェクトの設定 → サービス アカウント → 「新しい秘密鍵の生成」
      → `firebase/ifont-transfer-sa.json` に置く。
      自動化できないのは、鍵の発行が `firebase` コマンドに無く、`gcloud`
      （この機械に未導入）かコンソールでしか作れないため。
- [ ] `export_transfer_firestore.py` を**実データで1回通す**（鍵ができてから）。
      列の並びは GAS のシートに合わせてあるが、実物で突き合わせていない。
- [ ] 掲載の直前に採番を0へ戻す（掲載前チェックリスト C-0c）。
- [ ] GAS を主に戻す予定があるなら `gas_transfer/` をデプロイし直す（同 H-1b）。
- [ ] 実データで Firestore に全件入ることを見届けたら、GAS を落とすか決める。

### 学び

- **`firebase deploy` は足りない API を自分で有効にし、データベースまで作る。**
  「コンソールで手作業」と決めつける前に CLI を試すと、手作業が丸ごと消えることがある。
  ただし**置き場所は既定（アメリカ）になる**ので、空のうちに作り直すこと。
- **「読み出しを全面禁止」は、名簿には適用できない。** 書き込みの成否だけでは
  「では何に割り当てられていたか」を答えられない。**設計を紙の上で決めたときは、
  読めなくて困る場面を先に数え上げる。**
- **increment 変換の1人目は `create` として評価される。** ルールを `n == 0` で
  書くと1人目だけ必ず落ちる。境界の1件目を試験に入れておくと気づける。

## 2026-08-20 転写検証実験の実験環境 v3.0（新規実装・既存ページは無変更）

### 概要
`project/実験計画書_転写検証.md`（第2稿・3章と9章）の実験環境を新規に作った。既存の統合セッション
`experiment/audio1char.*`（v3.41）を**土台として写し**、聴覚の打ち切りを「割合」から
「音の実体の開始からの絶対ミリ秒」に変え、視覚に4方式（フェード・部分表示・ぼかし解除・ワイプ）の
描画器を入れ、生成した進み方 s(t) をなぞる再生と、群Bの最後の見え心地評価を足した。
**既存ファイルは1つも変更していない**（`audio1char.*`・`pilot_*`・`prod_common.js`・
既存の style・本番プールは無変更。`prod_common.js` は読み込んで使うだけ）。

### 入口は2ページ（集団はサイト内で自動振り分け）
発注者の指示で 4集団＝4URL をやめ、**同時期に走る集団を1つの入口にまとめる**構成にした。
クラウドソーシングでは別々の掲載どうしで「片方にしか参加できない」を強制できないため。

| 入口 | フェーズ | 振り分け先の集団 |
|---|---|---|
| `experiment/transfer_calib.html` | calib（較正） | acal（音声を測る）／ aprime（4方式の見え方を測る） |
| `experiment/transfer_test.html` | test（検証） | atest（音声の再測定）／ b（生成アニメ＋見え心地） |

振り分けはサーバ（GAS）の参加者名簿が決める。同じ参加者IDが開き直したときは同じ集団に戻る
（途中再開と整合）。検証フェーズでは較正フェーズの名簿と照合し、参加済みの人には
お断り画面を出す（完了コードは出さない。「以前ご参加いただいた分の報酬には影響しません」と明記）。
名簿サーバが未設定・不通のときは、参加者IDのハッシュから割り当てて課題は続行する
（`roster.require_server: true` にすると、そのときは始めさせない）。

### 追加ファイル一覧（すべて新規）
| パス | 中身 |
|---|---|
| `experiment/transfer_calib.html` | 較正フェーズの入口。`window.TRANSFER_PAGE = {phase:"calib"}` を埋めるだけの薄いページ |
| `experiment/transfer_test.html` | 検証フェーズの入口（同上・phase は "test"） |
| `experiment/transfer.js` | 本体（v3.0。土台の audio1char.js とは別実験なので採番し直した） |
| `experiment/transfer_config.js` | 実験パラメータを全部集めた設定ファイル。**数値はここだけ触ればよい** |
| `experiment/transfer.css` | 見た目（既存 style 系に触らないため独立させた） |
| `experiment/tools/build_transfer_gates.py` | 打ち切り済みWAVと索引を作る（聴覚刺激の生成） |
| `experiment/tools/build_transfer_warp.py` | 較正の曲線から進み方 s(t) を作る（生成工程の骨組み・凍結対象） |
| `experiment/transfer_stimuli/README.md` | 音声刺激の置き場所の説明（本番音源は未収録なので中身は空） |
| `experiment/transfer_onsets.sample.json` | 音の実体の開始（acoustic onset）の表の見本 |
| `gas/transfer_patch.md` | GAS の差分（記録用シート2枚＋名簿API）。**貼り付け用の文書で、デプロイはしていない** |

### 4方式の描画のしかた（共通の形にそろえた）
どの方式も「進み具合 s∈[0,1] を渡すと1フレーム描く」1つの関数にした（`RENDERERS`）。

- **フェード**: 不透明度 = s（既存の実装と同じ）
- **部分表示**: `base/<かな>.png` を読んで輝度128以下を「ストローク画素」とし、
  字ごとに固定した種（`"stroke-mask:<かな>"` のハッシュ）の乱数で並べ替え、先頭から
  `画素数 × s` 個を黒く塗る。**101段階の画像には依存せず任意の s を描ける**。
  1試行の中では s が単調に増えるので、増えたぶんの画素だけ塗り足して putImageData する
  （毎フレーム全画素を描き直さない）。乱数の並びは Python の `random.Random` とは別物になるが、
  字ごとに固定であれば実験の要件は満たす（同じ字はいつも同じ順で現れる）
- **ぼかし解除**: 別キャンバスに描いた字を、`ctx.filter = blur(半径px)` で重ねる。
  半径 = `max_radius_px × (1 − s)`（既定24px）。s=1 で鮮明
- **ワイプ**: `clip()` で見せる領域を単調に広げる。向きは設定（既定は左→右）

再生は2通り。**較正モード**は等速（s = 経過ms / `base_anim_ms`）で進めて指定の s% で打ち切り、
**転写モード**は 60Hz の数値列 s(t) を線形補間してなぞり、指定の t ms で打ち切る。
打ち切り判定は描画の前に行うので、**実際に見えた最大の進み具合は狙いよりフレーム1枚ぶん手前**に
なる。分析では実測値 `actual_s` を使うこと（名目値も別列に残してある）。

### 設定ファイルで差し替える項目（A2の結果が出たら、ここだけ触る）
`targets`（ターゲット8字）／`audio.gates_ms`（聴覚の打ち切り時刻）／
`visual.progress_pct_levels`（視覚較正の水準）／`visual.gates_ms`（群Bの打ち切り時刻）／
`visual.base_anim_ms`（基準アニメの長さ・Q4）／`visual.families.*`（ぼかし半径上限・ワイプの向き）／
`design.*`（まぎれ字の比率・問題数・確認問題の割合）／`assignment.*`（割付の回し方）／
`conditions`（8条件）／`wellbeing.chars`（見え心地の代表字）／
`roster.status_url`（デプロイ時に GAS の /exec を入れる）。

**現在の暫定値**: 8字＝あ・か・が・た・だ・し・ま・な、7時点＝20/40/60/80/110/150/220ms、
視覚の水準＝6/13/22/34/50/70/100%、まぎれ字はターゲット2問につき1問、
確認問題は全部見せ6%＋最小時点6%。1人あたり94問（ターゲット56＋まぎれ字28＋確認10）。
**どれも計画書の「暫定」であり、A2（10章）で確定する**。

### 割付（1人が何を見るか）
1人の参加者は「1つの字を1つの方式（群Bでは1つの条件）でだけ」見る。どの字にどれを当てるかは
名簿から返る連番で回す。群A′は `方式 = families[(字の番号 + 連番) % 4]`、
群Bは `条件 = conditions[(字の番号 × 3 + 連番) % 8]`（8字で8条件を一巡するので、1人が3条件を経験する）。
釣り合いは集団全体で取る。**この回し方も設定ファイルにあり、A2で見直せる**。

### 記録（1問1行）
既存の `PROD.saveFracTrial` に流す。足した列は
`phase, group, assign_index, assign_source, family, condition, gate_ms, progress_pct,
actual_ms, actual_frames, actual_s, progress_source, trial_index, is_filler, check_kind, config_version`。
GAS 側は既存の `trials` シートを触らず、`transfer_trials`・`transfer_wellbeing`・`transfer_roster` の
3枚を新設する差分にした（`gas/transfer_patch.md`。既存シートのヘッダを手で直す事故を避けるため）。
採点は client が申告する `target_char` で行う（視覚1文字課題と同じ契約）。

### 打ち切りWAVの作り方
```bash
# 本番（録音した自然音声と、目視確認した onset の表）
python3 experiment/tools/build_transfer_gates.py --src <採用テイクのdir> --onsets <onset表.json>
# 動作確認（既存の合成音で通す。onset は自動検出して experiment/transfer_onsets.json に書く）
python3 experiment/tools/build_transfer_gates.py --src audio_base_Kyoko --onsets auto \
    --out-dir /tmp/transfer_stimuli_test --manifest /tmp/transfer_audio_manifest_test.json
```
打ち切り時刻の表は `transfer_config.js` を node 経由で読む（表を2か所に持たないため）。
出力は `transfer_stimuli/<かな>_g0060.wav` 形式と索引 `transfer_audio_manifest.json`。
終端は t で終わる5msの余弦フェード。音源が t に足りないときは索引に `truncated: true` を書く。
`--salt` を付けるとファイル名がハッシュになる。

### 進み方 s(t) の生成（凍結の対象）
```bash
python3 experiment/tools/build_transfer_warp.py --curves <較正の曲線.json>   # 本番
python3 experiment/tools/build_transfer_warp.py --demo --out /tmp/transfer_warp.json  # 動作確認
```
提案条件は `s_i(t) = qV^{-1}(qA(t))`（単調にならした数値列の逆引き＋線形補間、範囲外は端に丸めて
`clipped` に記録）、対照2は較正データだけから距離が最小になる1次変換 `s = a·t + b`（2段階の格子探索）、
対照1は等速。出力 `experiment/transfer_warp.json` を**検証データを取る前にコミットし、
そのコミット番号を `transfer_config.js` の `visual.warp.frozen_commit` に書く**（計画書 Q3）。

### 動作確認したこと（ブラウザ実機は未確認）
- `node --check` で構文、HTMLの `<script src>` とファイル名の一致
- node 上に最小のDOMを立てて、4集団ぶんの出題の組み立て・記録の列・進み方の計算・
  集団の振り分け（サーバ無し／サーバがお断り／サーバが割り当て）を実行
- 仮想時計で提示ループを回し、打ち切りが狙いどおりのフレーム数で止まることを確認
  （60Hz・打ち切り80ms→5フレーム、220ms→14フレーム、打ち切りなし→最後まで＋完成形を350ms保持）
- 生成スクリプト2本を実行（WAV 48本＋索引の書き出しと長さの検算、仮の曲線からの s(t) 生成）
- **未確認**: 実ブラウザでの描画（部分表示の putImageData・ぼかしの `ctx.filter`）と、
  端末上の実測フレーム時刻。ここは実機で見ること

### 実機確認からのUI修正（同日・2件）

**1. かな表が画面に収まらずスクロールが要る問題**
回答用の五十音表は 5段×2ブロック＝10段あり、1マス44px固定だったため、刺激の枠（256px）と
合わせるとノートPCでも縦にはみ出していた。**並び（紙の五十音表と同じ配置）は変えずに**、
1マスの高さ・文字の大きさ・すき間を視口の残りの高さから決めるようにした。

- `transfer.css`: `.kana` を高さ固定＋中央そろえにし、`--kana-cell-h` / `--kana-font` /
  `--kana-gap` / `--kana-block-gap` の4変数で寸法を受け取る形に。外側の余白（`#app`・`.card`）と
  刺激の枠のまわりの余白も `clamp()` で画面が低いときに詰まるようにした。
  併記のボタン（「お／を」など）は狭い画面で折り返して1マスに収まるようにした
- `transfer.js`: `fitKanaGrid()` を追加。`表の上端から画面下までの残り ÷ 10段` で1マスの高さを
  出し（18〜40pxに収める）、文字は高さの0.55倍（11〜19px）、すき間はマスに応じて3段階。
  表を作るたび（`requestAnimationFrame` 後）と、窓の大きさ・向きが変わったときに測り直す
- 計算での確認（`10×マス + 8×すき間 + 2×ブロック間` が視口に収まるか）:
  ノートPC 1366×768 → 1マス28px・文字15px で収まる／1440×900 → 37px・19px ／
  iPhone 12縦 → 32px・18px ／ iPhone SE縦（視口550px）→ 19px・11px で収まる／
  聴覚課題（刺激の枠が無いぶん余裕がある）はどの画面でも上限の40px・19px
- 残る制約: 視口が 500px を切るとマスが下限18pxでも収まらず、ページがわずかにスクロールする。
  さらに詰めるには刺激の枠を縮めるしかないが、**表示する字の大きさは実験条件そのもの**
  （視距離・文字サイズとして論文に書く値）なので、自動では縮めない判断にした

**2. 聴覚の集団に視覚課題向けの画面が残っていた問題**
土台の統合セッション（1人が聴覚と視覚の両方をやる設計）の名残で、聴覚の集団（acal / atest）でも
「見え方の確認」を通っていた。準備の画面を集団ごとに出し分けるようにした。

| 集団 | 同意画面 | 準備の画面 | 説明と練習 |
|---|---|---|---|
| acal / atest（聴覚） | 再生機器の申告あり・聴覚の説明文 | **音量の確認のみ** | 聴覚の図解と聴覚の練習 |
| aprime / b（視覚） | 機器の申告なし・明るい環境の案内・視覚の説明文 | **見え方の確認のみ** | 視覚の図解と視覚の練習 |

（説明の図解と練習はもともと集団で分かれていた。直したのは「音量の確認」の次が
「見え方の確認」に固定されていた点と、同意画面の環境案内。）

### 保留（意図的に作っていないもの）
- 本番の音声刺激（自然音声の録音が未実施）。索引が無い間は既存の合成音を同じ規則で切る
  **代用モード**で動く（画面下に「音声は代用モード」と出る。データ取得には使わない）
- `experiment/transfer_warp.json`（進み方の表）は**コミットしない**。較正の実データが出てから
  生成して凍結する。表が無い間は等速で代用し、記録の `progress_source` に "linear" が残る
- GAS のデプロイ（差分は `gas/transfer_patch.md` に書いてあるだけ）

### 次にやること
- [ ] A2（計画書10章）の結果で `transfer_config.js` の暫定値を差し替える
- [ ] 自然音声の録音 → `build_transfer_gates.py` で刺激を作り、onset を全数目視確認
- [ ] 較正データ → `build_transfer_warp.py` で s(t) を生成 → コミットして凍結し、番号を設定に書く
- [ ] `gas/transfer_patch.md` を `gas/code.gs` に反映してデプロイし、`roster.status_url` と
      `prod_common.js` の `SUBMIT_URL` を入れる
- [ ] ブラウザ実機で4集団ぶんの見え方（特に部分表示とぼかし）とフレーム実測を確認

### 学び
入口URLの数は「実験の集団の数」ではなく「**同時に募集できる排他の単位**」で決まる。
集団ごとにURLを分けると、同じ人が別々の掲載から複数の集団に入れてしまい、
計画書3.1が要求する「4集団は互いに独立」が壊れる。

## 2026-08-19 音源比較セット raw_compare_v1（無加工の音源に切り替えられるかの判定材料）

### 概要
独自の音声加工（声の高さの平坦化・1文字0.2秒への強制伸縮・VOT自動修正・文脈語からの
切り出し・字ごとのフィルタ）をやめ、**無加工で明瞭な音源**を選ぶための聴き比べ資材を作った。
12字 × 2話者の無加工 VOICEVOX 音声を新規に合成し、現行の加工版を参照コピーし、
機械測定の QC 表と聴き比べページを用意した。仕様は
`project/設計案_生成アルゴリズム.md`（8/19確定分）と `project/録音手順_自然音声12字.md` に準拠。

### 追加ファイル（すべて新規。既存ファイルの変更はゼロ）
| パス | 中身 |
|---|---|
| `experiment/tools/build_raw_compare.py` | 無加工の合成と、現行加工版の参照コピー。音源セットを作る本体 |
| `experiment/tools/rawcompare_measure_lib.py` | 測定の共通関数（立ち上がり・破裂・VOT・振幅）。測り方の説明は冒頭 |
| `experiment/tools/measure_raw_compare.py` | 全ファイルの測定 → QC表・波形図・聴き比べページを生成 |
| `experiment/tools/source_compare.html` | 聴き比べページ（生成物。手で直さず上のスクリプトを直す） |
| `experiment/candidate_pools/raw_compare_v1/` | 音源36本＋`manifest.json`＋`measurements.json`＋`waveforms/`（波形図36枚） |
| `project/音源比較_QC表.md` | 人が読む測定結果の表 |

### 再現方法
VOICEVOX を localhost:50021 で起動してから、この順に実行する。
```
~/ifont_env/bin/python experiment/tools/build_raw_compare.py     # 音源を作る
~/ifont_env/bin/python experiment/tools/measure_raw_compare.py   # 測ってページを作る
```
話者を変えるときは `build_raw_compare.py` の `SPEAKERS` を編集する。

### B2（比較用のもう1話者）に玄野武宏 ノーマル（speaker=11）を選んだ理由
候補7話者（きりたん108・四国めたん2・青山龍星13・玄野武宏11・No.7アナウンス30・
東北ずん子107・九州そら16）に対し、無加工で ぱ・ば・か・が・た・だ を合成して VOT
（破裂から声が出るまでの時間。マイナスは破裂より前から声が出ている＝prevoicing）を実測し、
清音と濁音の VOT の開きが最小となる値で比べた。玄野武宏が最小84ミリ秒で最大。
声の高さも約104Hz で、きりたん（約229Hz）とほぼ1オクターブ違い、
「明らかに声質の違う話者」という条件も満たす。

### 主要発見
- **無加工でも話者しだいで清濁の作りが決定的に違う。** 同じ無加工でも B1（きりたん）は
  清濁の VOT の開きが最小 -1.7 ミリ秒（た +8.3 / だ +10.0 で**逆転**）しかない。
  B2（玄野武宏）は最小でも +84.0 ミリ秒あり、濁音3字すべてに prevoicing が出る。
  → **清濁が聞き分けにくい原因は加工だけでなく、話者の選び方にもあった。**
- **現行の加工版（C）は か と が の開きが +14.0 ミリ秒しかない。** 3列中で最も狭い。
  一方 ぱ/ば は +38.0、た/だ は +37.0 と確保できている。
- 音割れ（clipping）は全36本でゼロ。最大振幅は -7.05 dBFS で安全余裕あり。

### 設計判断（なぜそうしたか）
- **合成の問い合わせ内容は `outputSamplingRate` 以外いっさい変えない。** 44.1kHz 指定は
  合成器の出力設定であって、音の高さ・長さ・音量・音素のならびに触れないため無加工の定義に反しない。
  前後の無音（既定 各0.1秒）も既定のまま残した。
- **音量そろえは話者ごとに単一の倍率のみ。** 字ごとに変えると、破裂直後の振幅という
  清濁の手がかりを壊す。結果として摩擦音（し・す）は母音を含む字より10dBほど小さいままだが、
  これは合成器が出した自然な差なので**あえて残している**（QC表に注釈済み）。
- **C は再加工せずそのままコピー。** 参加者に実際に届いている実物を基準にするため。
  再生時の倍率（manifest の gate_gain）はどの字も概ね1.0なので、そのまま鳴らして比較できる。
- **ページは全長をそのまま鳴らす（切り出し・フェードなし）。** 仕様の「全長で聴き比べ」に従う。

### 数値の読みまちがい注意
- **VOT の符号**: プラスは破裂から声までの間、マイナスは破裂より前から声が出ていた長さ。
  絶対値だけで比べない。
- **prevoicing の測定値は控えめ**に出る（声帯振動の検出に数フレーム要るため）。
  B2「が」は実際約87ミリ秒の先行があるが測定値は -42.0 ミリ秒。
- **onset はファイル先頭からの時刻**。B1/B2 が100前後なのは既定の無音ぶんで正常。
- **C の全長がどの字も456ミリ秒前後にそろっている**のが加工の証拠。無加工は字ごとに違う。

### 残タスク
- [ ] A列（自然録音）が未収録。`project/録音手順_自然音声12字.md` の手順で録音 →
      切り出し・一定gainのレベル合わせ後に `A_<かな>.wav` として同ディレクトリへ追加し、
      `measure_raw_compare.py` の `COLS` に `"A"` を足して再生成する
- [ ] 聴き比べで B1／B2／C のどれを刺激候補に残すか判定（耳で即決せず、必要なら数人の全長識別チェック）

### 学び
話者の違いは「声の好み」ではなく**清濁の音響的な作りそのもの**を変える。
無加工に戻すだけでは清濁混同は解決せず、話者選定と対で見る必要がある。

## 2026-07-24 品質処方の連続合成への移植（CLI本体）

### 現在の状態
- **完了**: 単音・2文字プールで確立した品質処方を `ifont_tool` の設計提示モードへ移植した。
  7/21懐疑レビューで「最重要の残課題」(C3=REFUTED)と判定されていたもの。
  - `ifont_tool/ifont/quality.py` **新設**: 強制有声化(母音小文字化)・F0適合(実測→減衰0.55倍の
    逆補正→再合成、最大4回・最良反復採用)・音量ならし(モーラ節点±8dB・2dB不感帯・60ms平滑)。
    出典は `experiment/tools/build_karuta_samples.py`（karuta_5316/ooyama で検証済みの処方）
  - `audio.py` の `synth_voicevox_designed` に組み込み（既定ON、`quality=False`で旧挙動）
  - `cli.py` に `--raw-voice`（処方を切る）と `[quality]` 経過ログ表示を追加
  - `experiment/sample_assets/waka_ifont.mp4` を品質処方ONで再生成（quiz_ifont は自然韻律
    モードなので対象外・未変更）
- **検証結果**（waka=百人一首17番ちはやぶる、31モーラ全実測、scratchpadの
  measure_waka_before/after.py）:
  - 「みずくくる」の く(モーラ26): -47.1dBFS・有声0フレーム → **-23.7dBFS・有声13/20**（無声化根治）
  - F0最大偏差: +60セント(ず) → 30セント（許容内）
  - モーラ音量範囲: 26.9dB → **4.2dB**（mp4のAAC経由でも同値を確認）
  - `--raw-voice`（旧経路）の後方互換もOK
- **未コミット**: 上記すべて（ユーザーの試聴確認待ち）

## 2026-07-25 実読み(木本)の伸ばしに合わせたかるた読み + 伸ばし・余韻の処方追加

### やったこと
- Kikiwake の木本実読み「ちは 上/下」(読み取り専用)から伸ばし方を実測:
  MFA(mfa_v3/onset_extracted_v3.json、歌17)+自前の音響分析(スロット別RMS・F0)。
  結果: るー0.85s・ずー0.75s+休止0.45s・上句末わー0.95s / にー0.90s+休止0.45s・
  最終わー(余韻)2.9s。基調モーラは0.17-0.32s
- これを --durations/--pause-after に翻訳して合成したところ、**新しい欠陥**が発覚:
  VOICEVOX は0.5秒超の持続母音を保てない(かすれ→無声化)。特に「ワ」のE4持続は
  114Hzのボーカルフライに落ちる(B3なら健全。実測で確認)
- **処方を quality.py / audio.py に追加**(コミット対象):
  1. 0.55s超のモーラは「本体0.35s+ー継続(各0.5s以下)」に自動分割。ーは読みテキストに
     挿入して audio_query に解釈させる(手製モーラ辞書はエンジンが無視する。ハマった)
  2. 伸ばし部分は安全音高(260Hz以下)で合成し、PSOLA で設計音高へ載せ替え
     (quality.psola_contour。build_karuta_samples.py の余韻処理の一般化)
  3. ー継続部は F0適合の採点・補正から除外(無声判定でworst=999にロックするため)、
     音量ならしの節点からも除外(余韻の自然減衰を壊さないため)
  4. 最終モーラの余韻: -25セント下降 + -12dB減衰
- 検証(waka_kimoto_style): 全伸ばしが有声(最終わー 266/270fr)・E4±2c・
  余韻 -22.7→-37.9dB の滑らかな減衰
- 再現コマンドは scratchpad の実行履歴と下記パラメータ:
  durations "0.22,0.24,0.19,0.26,0.85,0.17,0.19,0.20,0.28,0.16,0.32,0.75,0.27,0.27,0.27,0.12,0.95,0.19,0.19,0.22,0.23,0.25,0.20,0.90,0.23,0.25,0.20,0.20,0.30,0.28,2.90"
  pause-after "11:0.45,16:0.90,23:0.45" (pitch は等速版と同じ B3@0,5,17)
- kimoto流サンプルは exploratory 扱いで sample_assets には入れていない(等速版は
  論文の運用速度デモとして別の意図があるため。差し替えはユーザー/PI判断)

### 次にやること
1. ユーザーが waka_ifont.mp4（新）を試聴して確認 → OKなら commit+push
2. 残りの連続合成課題: `build_karuta_samples.py` を `quality.py` を使う形に共通化するか検討
   （現状は同等処方の独立実装が残っている。動作は正常なので急ぎではない）
3. Kikiwake実読み（sounds_Inaba/kimoto、**読み取り専用**）との韻律突き合わせ検証は未着手

### 技術的な決定と理由
- 処方は `synth_voicevox_designed` 内（audio.py）に組み込み、CLI/MCP両方が自動で恩恵を
  受ける形にした。自然韻律モード(`--natural`)は「VOICEVOXの自然な読みのまま」が意図なので
  処方を適用しない
- parselmouth無し環境ではF0適合のみスキップ（強制有声化・音量ならしは純numpy/標準lib）。
  numpyも無ければ素の合成にフォールバック（依存を増やさない）
- 減衰付き逆補正(damp=0.55)はVOICEVOXのF0応答が非線形（等倍補正だと発振）のため。
  build_karuta_samples.py の実証値をそのまま採用

### 環境（このMac）
- Python venv: `~/ifont_env`（numpy・parselmouth・Pillow入り、ifont は editable install済み）
- VOICEVOX ENGINE 0.25.2: `~/ifont_env/voicevox_engine/run` で起動（ポート50021、
  東北きりたん=speaker 108）。導入記録は同ディレクトリの INSTALL_NOTES.md
- before/after計測スクリプトと音声はセッションscratchpad（一時領域）にあり。恒久化するなら
  experiment/tools/ へ移す

### ハマりポイント
- waka_ifont.mp4 の生成条件は ifont_tool/README.md の実声かるた読み例そのもの
  （1280x720・ラベルなし）。再生成時はこのコマンドを使う
- 「みずくくる」の無声化はVOICEVOXクエリの母音大文字化（無声化マーク）が原因。
  母音を小文字に置換すると強制有声化できる

## 2026-07-25(続) 木本比較の追い込み: 張り・スライド・余韻形

- ユーザー比較の指摘のうち「テンポ2倍」は比較対象の取り違え(kimoto_kami=上の句のみ6.5s vs
  合成=全体14.7s)。句ごとに揃えると上6.2s/6.2s・下7.5s/7.4sで一致済み
- 実測で確定した真の差: (1)木本は伸ばし中も音量を張り末尾で抜く(合成は即減衰8dB)、
  (2)伸ばし中F0を+25cじわ上げ(合成はフラット)、(3)句頭はスライド上昇(合成は階段)
- 処方v2: quality.shape_region(hold=張り+末尾リリース / yoin=4割保持→対数減衰)、
  elongationsをdict化(points=F0輪郭・env=エンベロープ指定)、句頭B3→E4は次モーラ6割
  時点到達のPSOLAスライドに。適用順を fit→gain→PSOLA→shape に変更
- v2実測: 冒頭266→295→325Hzスライド・るー328→333のじわ上げ・余韻-24,-25,-26,-32,-38
- 未解決(構造的): 子音の立ち上がりの鋭さ(VOT処方の連続版・未着手)、マイクロプロソディ
  (自然なゆらぎ)の欠如。披講の伸ばしは歌に近く、歌声合成(NEUTRINO/Sinsy、
  プロジェクトログ2026-06に選択肢(i)として記載あり)の方が土俵として適する可能性

## 2026-07-26 歌声合成(VoiSona/知声)の試作 — 伸ばし問題の構造的解決の検証

- ユーザー要望で VoiSona 標準シンガー「知声(Chis-A)」を試行。VoiSona は CLI/API の
  ない GUI 専用のため、(1)こちらで MusicXML 生成(project/voisona/chihayaburu_chisa.musicxml、
  tempo60で音価=秒、木本実測タイミング・B3/E4・読み仮名歌詞) → (2)ユーザーが VoiSona で
  WAV 書き出し(+.lab 音素ラベル) → (3)以降は自動、の半自動フロー
- 実測結果(0_chihayaburu_chisa.wav、Desktop/ifont_listen・ローカルのみ):
  - 31モーラ全一致・内容約14.1s(設計14.05s とほぼ一致。末尾に無音パディングで全長30s)
  - E4モーラは±10c内、句頭B3も設計通り。フレーズ頭は低めから上がる自然なポルタメンタが
    勝手に付く(木本の「からくれ」214→341Hz のスライドと同型)
  - 伸ばし: るー0.8s・最終わー2.97s とも音量-21〜-24dBで張ったまま持続、F0±0c。
    **VOICEVOXで4段の処方(ー分割・安全音高・PSOLA・shape)が必要だった性質が素で出る**
- .lab の音素境界からモーラonsetを取り、render_frames_gated で字幕同期した
  waka_chisa.mp4 を生成(scratchpad と Desktop/ifont_listen)
- 音声・labは知声の利用規約(クレジット「VoiSona:知声」等)配慮で git には入れない
- 含意: 披講用の製品音声は歌声合成が土俵として適切。完全自動化が要るなら
  NEUTRINO/Sinsy(CLI あり)が候補。実験刺激・等速提示は VOICEVOX 凍結インベントリのまま

## 2026-07-26(続) 知声への木本韻律の完全転写

- 「もっと木本に近づけられるか(タイミングなど)」→ 韻律転写を実装。木本実読みの
  (1)F0輪郭 (2)音量エンベロープの形 (3)モーラ長・休止長 をモーラ対応のタイムワープで
  知声WAVへ PSOLA 転写(parselmouth Manipulation: DurationTier+PitchTier+ゲイン)
- 結果: モーラ長誤差 中央値0ms・最大74ms(木本の「が」70msにレート下限0.6が当たる箇所のみ)、
  伸ばしF0 341→346Hz のじわ上げ・余韻の減衰カーブも木本の実測形そのまま
- 成果物: waka_chisa_kimoto.mp4 / chisa_kimoto_full.wav (Desktop/ifont_listen)、
  境界 project/voisona/chisa_full_moras.json、手順 project/voisona/README.md
- 残る差: 声質(知声そのもの)と子音の質(転写は母音・韻律のみで分節音は知声のまま)

## 2026-07-28 概念整理: Perceptual Score・文字合成・iFont

- 乙課題の議論(短いS活用・陽性対照・実効理解度)から発展し、丸山の発案で概念の三層化が確定:
  Perceptual Score(中間表現: 文字情報が時間とともに徐々に識別可能になる過程。対象レベル・
  累積・モダリティ非依存) / 音声合成→音声・文字合成→iFont(動的文字) / F が対象レベルと
  文字レベルの橋渡し、g が両合成器の較正表
- 詳細と栗原先生相談用メモ: project/perceptual_score_concept.md(アブスト反映案・未決事項つき)
- アブスト修正版(文体チェック適用+干渉判定の1文追加)は会話内。採否・論文反映はPI判断待ち

## 2026-07-31 Perceptual Score デモ実装(手描きカーブ→音声+iFontの両合成)

- ユーザー要望「私が理解度グラフを描いたら、音声合成と文字合成でそうなるものを作れるか」
  → experiment/tools/pscore_demo.py として実装。スコアJSON(文字ごとの[時刻,目標識別可能性]
  点列)を入力に、(1)視覚: 仮の心理測定関数の逆写像で不透明度の時間変化を生成、
  (2)聴覚: 目標区間長の設計合成(今週の伸ばし処方を活用)+PSOLAワープで内容到達がカーブに
  追従、(3)下部にスコアと進行カーソルを描いた mp4 を出力
- 較正はプレースホルダのロジスティック(FLOOR=0.02, PLAT=0.95, 視覚mid0.45/聴覚mid0.5)。
  実験の実データで差し替える前提を画面とdocstringに明記
- 例: pscore_example.json(あ・お2本)。実行:
  ~/ifont_env/bin/python experiment/tools/pscore_demo.py pscore_example.json out.mp4
  (VOICEVOX起動が必要)
- ユーザーのカーブ入力手段: JSON編集 / 手描き画像を渡してClaudeが点列化 / (将来)ブラウザ編集UI

## 2026-07-31(続) 知覚楽譜エディタ: ブラウザで描く→その場で合成

- pscore_editor.html(和風UI・方眼にドラッグでカーブ描画・文字と長さ可変・JSON書き出し)+
  pscore_server.py(localhost:8765。POST /synth で pscore_demo.py を実行し /video で mp4 配信)
- 起動: PSCORE_WORK=<作業dir> ~/ifont_env/bin/python experiment/tools/pscore_server.py
  (VOICEVOX 起動下)→ http://localhost:8765
- pscore_demo.py は PSCORE_WORK 環境変数で作業ディレクトリを外部指定可能に

## 2026-07-31(続) 栗原議論の受領とRSVPプロトタイプ

- Cosense「珠玉の議論」(7/31)受領: 公開物の明確化(同理解度・速度指定の視聴覚同期動画、
  いろは/オリジナル決まり字かるた展開)、シングルモダリティ単体の価値、「ユーザに入力させる」
  ことの受容効果、コアデータ=時系列理解度、入力言語の未検討、話速別f関数の未実測
  (0.2s/moraのみ。放送は約0.14s/mora)
- アブスト最終版(栗原改稿・一般化された導入)受領。指摘2点のみ:「変換 (g)」の括弧、
  「適用できるかも検証する」の誤読リスク
- TODO文献調査はユーザー判断で別途(起動したエージェントは停止・破棄)
- TODO AIプロトタイピング → experiment/tools/rsvp_proto.html を作成:
  ①置換(素のRSVP) ②残像減衰(前の文字が同枠で薄く残る) ③スクロール窓 の3方式を
  0.10〜0.30s/mora(プリセット0.14/0.20)で体感比較。ニュース風/百人一首/いろはのプリセット付き

## 2026-08-12 セグメント操作パイロット(母音か子音か)の実験環境v1

- 丸山の新アイデア「理解度グラフに効くのは音のどの部分か(全体/母音/子音)」を実験環境化
- experiment/tools/build_segment_pilot.py: 木本「ちは上の句」(Kikiwake読み取りのみ)を
  MFA音素境界でPSOLA伸縮({母音,子音}×{0.6,1.0,1.5})。決まり字聞き分け用の打ち切り点
  6段階を伸縮後時刻に変換してmanifest.jsonへ。音声は実読み話者の声のためgit外(ローカル生成)
- experiment/tools/segment_pilot.html: ち群3択(ちはやぶる/ちぎりきな/ちぎりおきし)+確信度、
  全長試行のみ自然さ評価。30試行、結果表(正答率×ゲート×変形)とCSV書き出し
- 起動: build_segment_pilot.py <dir> → dirでhttp.server → index.html(=segment_pilot.htmlコピー)
- デモ音声(seg_*.wav 6種: 原音/全体1.5/母音1.5/子音1.5/母音0.6/子音0.6)はDesktop/ifont_listen
- 知見メモ: 有音部の77%は母音(母音3.24s vs 子音0.97s)→「全体の短縮」はほぼ母音の短縮

## 2026-08-12(続) か行5択版に設計変更(丸山指定・確認の上で実装)

- 設計: きりたん合成 か〜こ(B3・0.3s)、子音×母音の倍率{0.5,1,2}の9条件×5かな=45問、
  全長のみ・毎問「5択+確信度+自然さ」約10分。打ち切りゲートは天井なら二周目で追加
- experiment/tools/build_kana_segment_pilot.py(境界は合成クエリのconsonant_lengthから正確、
  PSOLA伸縮) + kana_segment_pilot.html(結果は子音×母音の3表: 正答率/確信度/自然さ、CSV)
- 素の子音長: か16ms き41ms く24ms け24ms こ18ms → 0.5倍で8msまで縮む(VOT操作の領域)
- 留意: 選択肢が清音のみなので「く→ぐに聞こえた」型の誤りは捕捉できない(二周目で
  濁音を選択肢に足すか要検討)
- きりたん音声なので刺激も公開可(現状はローカル生成のまま)

## 2026-08-12(続) v2: 打ち切り×伸縮で「どこで分かるか」曲線

- 丸山の指摘「打ち切りを入れれば、どこで分かるかのグラフが描ける」を実装(規模は確認の上
  十字5条件×4ゲート×5かな=100問・約18分)
- 条件: ×1×1/子音×0.5/子音×2/母音×0.5/母音×2。ゲート: 子音終わり/母音1/3/母音2/3/全長
  (伸縮後時刻に換算してmanifestへ)。自然さは全長試行のみ。途中終了ボタンあり
- 結果画面に条件別の正答率曲線(canvas)+表+CSV。核心の問い:「音の中身の位置」で
  揃えたとき伸縮条件間で曲線が重なるか(重なれば時間伸縮不変=引き伸ばし自由の根拠)
- 生成器は --grid で旧3×3全長版も出せる

## 2026-08-12(続) 設計精査の結論: 倍率・5択を維持、v3案(絶対ms+10択)は撤回

- 議論の結論: 研究質問は「時間伸縮したとき知覚情報の時間構造も同様に伸縮するか
  (P(correct|意味位置)の保存)」であり、倍率操作がその操作そのもの。絶対ms(8-60ms)に
  すると問いが「子音絶対長の効果」にすり替わるため不採用。5択維持(が行を足すと有声性
  同定が混入しRQ1を濁す)。VOT≒25ms境界を根拠にしない(VOTは子音区間長と別物・多手がかり)
- 子音×0.5の壊れ方が心配なら別枠の小チェック(か/が/その他 10-20試行)で見る
- 一周目のチェックリスト: (1)子音終了でチャンス超えするか (2)母音1/3→2/3で上昇するか
  (3)×0.5/×2で対応する打ち切り点の正答率が重なるか (4)子音×0.5だけ異常な誤答/違和感が
  出ないか (5)母音1/3で既に天井でないか→天井なら二周目で母音1/12・1/6の相対ゲートを追加
  (相対位置のままなのでRQ2の論理は保存)
- 曲線の重なりの主張にはマスあたり20-30試行=4-6周の累積が必要。一周目は坂の位置の偵察

## 2026-08-12(続) 重大バグ修正: 子音終了ゲートが実質無音だった(v3)

- 丸山の報告「音が入っていない試行がある」→実測で確定: 全ての子音終了ゲート断片が
  -40〜-88dBFS。原因は (1)kの破裂音自体が母音より30dB以上小さい(かは素で-64dBFS)
  (2)クライアント側フェード6msが8〜40msの断片の大半を食う (3)先頭無音50ms
- run1の子音終了ゲートのデータは可聴性の交絡で無効。かな別解釈(こ100% vs か0%=情報の差)は
  撤回——音量差だった可能性が高い。母音1/3以降のデータは有効
- 修正(v3): 断片を事前レンダリング方式に変更(render_fragment: 切り出し+2msフェード焼き込み
  +有音部RMSを-26dBFSへ正規化・増幅上限+30dB)。ページは断片ファイルを再生するだけに。
  gain_db/gain_cappedをCSVに記録
- 正規化後の検品: 可聴域に入らないのは「か」の子音断片4つのみ(元のkに物理エネルギーが無い。
  これは欠陥でなく測定結果としてフラグで区別)
- 教訓: frac課題の「先頭無音+短ゲート=無音」と同型の穴に二度目に落ちた。短い切り出し刺激は
  必ず切り出し後の実効音量を検品してから使うこと

## 2026-08-12(続) v3.1: 測定点を坂に集中、自然さは別調査へ切り出し

- run1の結果に基づく試行計画の最適化(丸山確認済み):
  廃止=母音2/3(天井)・全長(天井+自然さ専用だった)・子音終了ゲートの母音条件(基準と同一刺激)
  追加=母音1/12・1/6(坂は子音終了〜母音1/3の間にある)
- 構成: 子音終了(3条件)15問 + 母音1/12・1/6・1/3(各5条件)75問 = 90問(約15分)
- 自然さ評価は後日、全長専用ミニ調査(倍率を1.25/1.5/2/3等に細分)として独立実施する方針
- 検品: 可聴域外は引き続き「か」子音断片のみ(物理的にエネルギー無し・フラグ記録済み)

## 2026-08-12(続) run2-3の累積分析とv3.2

- 2周累積(180問): 母音1/12で全5条件80%一致を再確認。誤答10件中9件が「か→く」
  →「80%の壁」の正体は「か」の遅さ。曲線の条件間一致は「かなごとの分かる距離が
  伸縮しても保存される」形で成立。母音×0.5@母音1/6=80%が2周とも再現(いずれもか→く)
  →実時間の下限仮説の芽。子音×2の子音のみゲート優位(80% vs 50%)も維持
- 「39問目無音」の報告は検証の結果、第39問は音あり・正解・確信度5。設計上無音なのは
  42/85問目(かの子音断片・どちらも「わからない」回答=正しい挙動)
- v3.2(丸山承認): 母音1/3廃止(天井)・母音1/6は母音×0.5とかのみ・母音1/24を新設。
  74問(約12分)。ゲート=子音終了/母音1/24/1/12/1/6

## 2026-08-12(続) run4分析・PSOLA非決定性の発見・総まとめ図

- run4(v3.2): 母音1/24でか以外の4かなは全条件100%(識別は母音の最初の約12msで完了)。
  かは1/24で0%(全部く)だが1/12で100%に急上昇
- 丸山の疑い「音の開始位置とか変えてるからでは」→検証の結果**的中**: 伸縮変形の断片は
  世代間で波形が違った(か母音×0.5@1/12は増幅が10.8→20.5dBと9.7dB差)。決定性テストで
  **PSOLA(parselmouth Manipulation)は同一入力でも毎回微妙に異なる出力**と確定
  (最大差0.087)。VOICEVOX合成は完全決定的
- 帰結: (1)run4のか急上昇は「刺激の微変化+学習」の混合で分離不能 (2)RQ2の主張は
  run2-3(完全同一刺激)に基づける——母音1/12で5条件80%一致は堅い
  (3)**刺激凍結ポリシー**: 実験用刺激は一度生成したディレクトリを正とし再生成しない。
  kana_segment_v32を凍結版とする
- 総まとめ図 kana_summary_all.png (A周回別/Bかの物語/C同一刺激でのRQ2/D子音勾配)を
  Desktop/ifont_listenに。1周目子音終了は音量バグ除外・4周目は別刺激と注記済み
- 自己周回はここで打ち止め(学習飽和)。次候補: 新鮮な耳(栗原先生ら)/すり替え実験/他の行

## 2026-08-14 栗原ノート(8/13)受領: 子音固定長問題と「い」の分析タスク

- 栗原の指摘: 実験音声の200ms/mora生成は_set_moraが「子音=エンジン既定長で固定、
  母音=残り」としており、エンジンに任せれば得られる自然な子音/母音比を拒否していた。
  133ms/mora等でも子音固定のまま。一部のカナの不自然さの原因の可能性
- 最終決定: 実験は200ms/moraで進める。ただし「日本で一番たかい山」の「い」だけ
  特異的に変に感じるので分析する(本セッションで実施)
- 方向性の合意: 話速変化(一様伸縮 vs 母音子音別伸縮)での理解度時系列のモデル化ができれば
  任意速度のiFont合成・理解度の人工調整(Perceptual Score実現)につながる

## 2026-08-14(続) 倫理承認確認・実験環境の最終整備

- **倫理審査の承認を確認(丸山報告)** → 本実験開始のゲートがクリア
- 実験環境棚卸し: 乙本番ページ・刺激凍結・事前登録ドラフト・発注文面は準備済み。
  監査指摘(GASのactual_*/env列挙漏れ)を修正しコミット(23cfce726)。ページ側もenv/duration送信
- 発注までの残り: ①GASデプロイ(栗原・修正版code.gsで) ②ブランチ→mainマージ(Pages配信)
  ③Yahoo発注画面確認。スケジュールは8/31投稿の圧縮版+実験トラック(8/26カットオフ)

## 2026-08-14(続) Firestore二重書き込みを実装(丸山提案)

- 根拠: GASは同時実行30本上限+no-cors投げっぱなしで混雑時の取りこぼしを検知できない。
  Kikiwake(Firebase)の運用実績あり
- prod_common.js に FIREBASE={projectId, apiKey} と fsPost を追加。設定時は
  soa_trials/soa_sessionsコレクションへ並行保存(create-onlyルール)。GASと併用可
- 手順書 docs/FIREBASE_SETUP.md(10分作業・ルール込み)。解析はFirestoreを正、GASを照合用

## 2026-08-16 公開体制の変更: 丸山フォークのPagesで公開(重要ルール)

- origin/main(栗原氏の8月開発)をブランチへマージ(9521b4cfa)。手元=先生版+丸山追加49件の全部入り
- **事故と復旧**: 「整えて公開したい」を誤解釈し qurihara/iFont の main へ push → ユーザー指摘で
  即 force-with-lease により d4a5cf048(先生の最新)へ完全復元。先生側は無傷
- **新ルール(記憶にも保存済)**: 先生のmainへは明示承認なしにpushしない。公開は丸山フォークで行う
  - フォーク: g19922ma/iFont (remote名 `fork`)。丸山アカウントは学生アップグレード版
  - 公開URL: **https://g19922ma.github.io/iFont/** (Pages有効化済・main配信)
  - 合体版をフォークmainへpush済。主要ページ6本が200、新prod_common(FIREBASE/無線UI)配信を確認
- 先生への反映は「URLを見せる→承認→プルリクエスト」の順で行う(未着手、タスク#5の栗原連絡に含める)

## 2026-08-17 プロジェクトv2(1文字iFont)の実験1環境が完成

### 路線の整理(丸山の指摘で確定)
- **v1 = 連続提示のiFont**(従来路線): 乙課題(3連続・干渉判定)や先生のfrac系はこの路線の前提チェック
- **v2 = 1文字に限定したiFont**(現在の本線。1字決まりかるた・いろはカルタ向け):
  - 実験1 = 1文字の基準曲線(聴覚・視覚を同一参加者で) → **audio1char v3 統合セッションが本体**
  - 実験2 = 2文字で後続文字の影響(マスキング・予測) → 次回設計
- 乙課題はv2では主役でない。今日の乙ページ磨き込みはv1路線+UI共有資産の位置づけ

### 今日の主な実装(すべて丸山フォーク g19922ma/iFont に公開・先生側は無傷)
- 公開体制: 丸山フォークのPages(https://g19922ma.github.io/iFont/)。先生mainへの誤pushは即復元済み
- 乙課題UX: 回答確認(直す/決定)・途中再開(localStorage・60分)・進捗バー・五十音表式かな表・
  教示2分割(音量/見え方確認必須)・同意文全面改稿・体裁刷新・無線OK(合図音追加後)
- audio1char: v2(乙UX移植・jsPsych廃止) → **v3 統合セッション**(聴覚+視覚 25問×4ブロック・
  AVAV/VAVA/AVVA/VAAVランダム割当・合計100問約10分・?set=50で拡大可・無線OK)
- GAS: audio_device/resume_count/resume_gap_s/block_order/block_pos 列を追加
- 詳細は project/changes_from_main.md(先生提出用リスト)を正とする

### 次にやること
- [ ] 実験1(統合セッション)を先生に見せて承認 → GAS/Firebaseデプロイ → Yahoo発注
- [ ] 実験2(2文字)の設計(あかのマスキング・予測。audio2charを土台に検討)
- [ ] 乙課題の位置づけを先生と確認(v1路線を並走するか、v2に集中するか)

## 2026-08-18 候補プール uniform200（子音と母音の時間の配り方の比較）

### 何をしたか
1 モーラ 0.2 秒に固定するとき、その 0.2 秒を子音と母音にどう配るかの 2 案を
同一条件で聴き比べられるようにした。本番プール（`experiment/audio1char_stimuli/`）は無変更。

- **方式1（現行）**: 子音長を素の値のまま残し、母音長 = 0.2 秒 − 子音長。
  子音は 0.16 秒で頭打ち。実装は `two_char_audio/build_2char_pool.py:81-89` の `set_mora`。
  もとは競技かるたの「伸ばし・余韻」（母音だけを引き延ばす操作）用の配り方だった。
- **方式2（新規プール uniform200）**: 子音長と母音長を同じ倍率で伸縮して合計を 0.2 秒にする。
  PI が `ifont_tool` 側のコミット d4a5cf048 で採り入れた規則
  （`synth_voicevox_slot(mode="uniform")`）と同じ。

### 追加・変更したファイル
| パス | 中身 |
|---|---|
| `experiment/candidate_pools/uniform200/build_uniform200.py` | 方式2 で 68 音を合成する。`b2.set_mora` を差し替えるだけで、ほかの処方は `build_1char_pool.py` のものをそのまま使う |
| `experiment/candidate_pools/uniform200/merge_answer_key.py` | かな→ファイルの対応を `answer_key_merged.json` に `pool="uniform200"` として統合する（点検ページが読む） |
| `experiment/candidate_pools/uniform200/make_compare_table.py` | 比較表の数値を作る |
| `experiment/candidate_pools/uniform200/audio1char_{manifest,onsets}.json` + `audio1char_stimuli/*.mp3`（72 個） | プール本体 |
| `experiment/tools/build_gate_windows.py` | 配り方の別（`slot_mode`）を引数に足し、`--pool uniform200` を追加。`production` と `slot133` は従来どおり方式1 |
| `experiment/pilot_soa_audio.js` | `POOL_NAMES` に `uniform200: "候補: 一様伸縮200ms(方式2)"` を1行追加（それ以外は無変更） |
| `project/子音母音配分_方式比較.md` | 68 音の子音長・母音長を両方式で並べた表と、聴き比べ URL |

### 主要な結果
- 68 音すべて合成に成功。失敗音なし。`build_gate_windows.py` の「模型で予測した長さ＝実際の
  mp3 の復号後の長さ」という検算も 68 音すべてで通った。
- 配分が動くのは 30 音、うち母音長が動くのは 23 音（21 音が伸び、2 音が縮む）。残りは同一。
- 母音の伸びが大きい順に **し・せ（+42.7 ミリ秒）、さ・す（+32.0）、ち・ひ・へ（+21.3）**。
  すべて摩擦音・破擦音。**「し」の母音は 42.7 → 85.3 ミリ秒でちょうど 2 倍**になる。
- 「つ」は素のモーラ長が 203.6 ミリ秒とほぼ 0.2 秒なので両方式で一致（変化なし）。
  「ぽ」は「オッポ」の文脈から切り出す処方で配り方の規則を通らないため、2 プールで同一音声。
- モーラに占める子音の割合（子音のある 61 音の平均）: 素の合成 19.4% / 方式1 23.6% / 方式2 20.9%。
  方式2 のほうが素の比に近い。PI が文章で測ったとき（素 27.6% → 方式1 16.6% → 方式2 24.9%）は
  方式1 が下に外れていたが、孤立モーラでは逆に上に外れる。外れる向きは逆でも、
  素の比から遠いのはどちらも方式1。
- VOT（破裂から声が出るまでの間）の自動修正ラダーは「て」だけ収束先が 2.5 倍 → 3.0 倍に変わった。
  量子化後の子音長・母音長は両方式とも 42.7 / 160.0 ミリ秒で同じ。

### 聴き比べ URL（本番の実物で 200 応答・対応 68/68 を確認済み）
| どちら | URL |
|---|---|
| 現行（方式1） | https://g19922ma.github.io/iFont/experiment/pilot_soa_audio.html?check=1 |
| 試作（方式2） | https://g19922ma.github.io/iFont/experiment/pilot_soa_audio.html?pool=uniform200&check=1 |

### 設計判断
- 差し替えたのは `set_mora` 1 か所だけにした。話者・音高・VOT 修正・ぱ行の処方・音量の均一化・
  切り出し窓の作り方を触ると、聴き比べで「配り方の差」と「そのほかの差」が混ざって
  判断できなくなるため。
- VOT 修正は「値」ではなく「手順」を共通にした（1.0→2.0→2.5→3.0 倍を順に試して
  25 ミリ秒に最初に届いた倍率を採る）。方式2 では倍率を掛けたあと全体を縮めるので、
  同じ倍率でも実際の子音長が短くなる。値を固定すると方式2 側で VOT が不足しうる。
- 比較表の値は audio_query の音素長を量子化したもので、波形の実測ではない。
  波形との整合は `build_gate_windows.py` の長さの突き合わせで担保している。

### ハマりどころ・注意
- 音声ファイル名のハッシュに使う `SECRET_SALT`（`.env`）が手元に無く、既定値
  `dev_2char_salt` で生成した。プール内で一貫し他プールとも衝突しないので点検には支障ないが、
  **本番に採用するなら本来のソルトで作り直すこと**。
- `answer_key_merged.json` への統合を忘れると、点検ページは「正解表を選択してください」の
  ファイル選択画面で止まる（かな→ファイルの対応が引けないため）。

### 次にやること
- [ ] 2 つの URL を聴き比べて、どちらを本番プールにするか PI と決める（第0段階の試聴）
- [ ] 採用する場合は本来の `SECRET_SALT` でプールを作り直し、本番プールを差し替える

### 追記: 波形つきの聴き比べページ（判定用）

どちらを本番にするか耳と目の両方で決められるように、13音を左右に並べた静的ページを作った。

- ページ: https://g19922ma.github.io/iFont/experiment/tools/pool_ab_compare.html
- 生成器: `experiment/tools/build_pool_ab_compare.py`（VOICEVOX 起動が要る）
- 図: `experiment/tools/pool_ab_assets/<かな>_<m1|m2>.png` 26枚。子音を琥珀・母音を青緑の帯で
  塗り分け、境目と各区間のミリ秒を図中に書いた。横軸は全図共通で0〜210ミリ秒
  （最長のモーラ 202.67 ミリ秒が収まる幅）、縦軸（振幅）も全図共通なので、
  図どうしの波の高さの差はそのまま聞こえの大きさの差にあたる。
- 並べた13音: 母音の変化が大きい8音（し・せ・さ・す・ひ・へ・ち・は）、変化のない代表（つ・ぽ）、
  母音が最長の音（あ）、母音が縮む2音（る・ろ）。方式1で母音が最短の音は「し」で、
  変化が大きい8音の先頭と重なるので行を統合した。
- 再生は実験本番と同じ処理（音響的な立ち上がりからモーラ末尾まで・音量をそろえる増幅つき・
  両端8ミリ秒のフェード）。既存プールの mp3 を参照するだけで、実験ページの挙動は変えていない。

**新しく分かったこと**: 時間の配分が同じ音（つ・ぽ・あ など）でも、2つのプールの波形は
完全には一致しない。音量をそろえる倍率がプール全体の中央値を基準に決まるので、
プールの中身が変われば中央値もわずかに動くため。配分の同じ38音で測った差は最大0.13デシベルで、
聞き分けられる大きさではない。当初「ぽ は2つのプールで同一の音声」と書きかけたが、
ファイルのハッシュを突き合わせて誤りと分かり、書き直した。

**ハマった点**: 「続けて鳴らす」ボタンに `class="play"` を付けたため単体再生のハンドラにも当たり、
`data-clip` の無いボタンで `JSON.parse("undefined")` が投げられていた。ブラウザの実物で
コンソールを見るまで気づかなかった（生成物は静的HTMLなので、生成器を読むだけでは出ない類の欠陥）。

## 2026-08-20 転写検証実験: 本番の聴覚刺激を組み込み、下見の掲載準備をした

### 概要
録音した自然音声（68かな・採用テイク）から、打ち切り済みの配信用WAVを544本作って
`experiment/transfer_stimuli/` に置き、実験ページが合成音の代用モードではなく
本番刺激を読むようにした。あわせてターゲット8字を確定値に差し替え、
下見（16人）の掲載文と掲載前チェックリストを用意した。

### 追加・変更ファイル
| パス | 中身 |
|---|---|
| `experiment/transfer_config.js` | ターゲット8字を確定値へ（あ・か・が・ぱ・し・つ・ま・ら）。見え心地の代表字を あ・ぱ に（暫定・事前固定）。代用モードを無効化。設定版 `prov-2026-08-20b` |
| `experiment/tools/build_transfer_gates.py` | 採用テイク形式の onset 表（`recordings_raw/adopted_onsets.json`）を直接読めるようにした。`--lead-ms`（既定50）で onset の手前を切り揃える機能を追加 |
| `experiment/transfer_stimuli/*.wav` | 68かな × (打ち切り7時点 + 全長) = **544本**。約9.9MB |
| `experiment/transfer_audio_manifest.json` | 索引。見出しは `<かな>\|<時点ms>` と `<かな>\|full` |
| `experiment/tools/check_transfer_stimuli.js` | 掲載前の一式検査（新規）。HTML参照・設定と索引の一致・実験ページが引きうる全組合せ・WAV実測長さ |
| `experiment/transfer.js` | 刺激のURLを `encodeURIComponent` で符号化（ファイル名にかなが入るため。1行） |
| `experiment/transfer_stimuli/README.md` | 「まだ空」→ 中身の説明・1本のWAVの構造図・作り直し手順に全面改稿 |
| `project/掲載文_下見.md` | 下見の掲載文（そのまま貼れる本文つき）・掲載設定値・承認基準（新規） |
| `project/掲載前チェックリスト.md` | GASの適用とデプロイ・疎通確認・公開確認・掲載直前の自己通し（新規） |

### 刺激の作り方（再現手順）
```bash
python3 experiment/tools/build_transfer_gates.py \
    --src recordings_raw/adopted --onsets recordings_raw/adopted_onsets.json
node experiment/tools/check_transfer_stimuli.js     # → 「合格」で終わること
```
`recordings_raw/` は `.gitignore` 済み（生の録音はコミットしない）。
`experiment/transfer_stimuli/` の打ち切り済みWAVは**実験で公開配信するもの**なのでコミットする。

### 1本のWAVの中身（誤読しやすいので明記）
- **0ms は acoustic onset**（音の実体が始まる点）。t ミリ秒の刺激は onset から t ミリ秒ぶん。
- ファイルは onset の **50ミリ秒手前**から始まる（前置き＝録音の静かな区間）。
  したがって **ファイル全長 `dur_ms` ＝ 前置き `lead_ms` ＋ 打ち切り `gate_ms`**。
  前置きは onset より前なので刺激の長さには数えない。索引に1本ずつ `lead_ms` が入っている。
- 終端は t で終わる 5ミリ秒の余弦フェードアウト。t より後ろの音は1サンプルも入らない。
- モノラル・16bit・48kHz。

### 設計判断
- **前置き50ミリ秒を付けた理由**: (1) 全字で「鳴り出す前の静けさ」をそろえる。「に」は頭に
  息継ぎを316ミリ秒巻き込んでいた（`project/録音QC_20260820.md` §10-2）が、ここで切り揃えて
  落ちた。(2) 波形の途中から鳴らすと出る「プツッ」を防ぐ。onset より前なので測定には影響しない。
  録音の余白が50ミリ秒に満たない字はあるだけ付けた（実際は43〜50ミリ秒の幅）。
- **まぎれ字60字ぶんも全時点作った理由**: 出題はまぎれ字にも同じ打ち切り時点を使い、
  音量確認では「あ・い・う・え・お」を全長で鳴らすため。8字だけでは足りない。
- **代用モード（合成音への切り替え）を無効にした理由**: 索引が読めないときに別の音で静かに
  走り続けると、気づかずに使えないデータを取ってしまう。止まったほうが安全。
- **ファイル名を伏せなかった理由**: 索引が「かな→ファイル」の対応を公開しており、視覚課題の
  画像も最初から `base/<かな>.png` なので、ハッシュ化しても答えは隠れない。検査しやすさを取った。

### 確認したこと（実物）
- `node experiment/tools/check_transfer_stimuli.js` が合格。WAV 544本の実測長さと索引の
  食い違いは最大 0.000ミリ秒。索引に無い刺激なし・余分なWAVなし・音源が短くて届かない時点なし。
- ローカルに配信して Chrome で `transfer_calib.html` を開き、**同意 → 説明の画面まで進んで
  画面下に「音声は代用モード」が出ないこと**を目で確認（＝索引が読めている）。
  ブラウザで `ぱ|60` `あ|20` `に|full` `し|220` を復号し、長さが索引と一致することも確認。

### 残タスク（すべて人手）
- [ ] GAS に `gas/transfer_patch.md` の3つの差分を当ててデプロイ（→ 掲載前チェックリスト B）
- [ ] `/exec` URL を `prod_common.js` の `SUBMIT_URL` と `transfer_config.js` の
      `roster.status_url` に貼る（→ B-6）
- [ ] `roster.require_server` を `true` にするか決める（勧め: true。→ B-7）
- [ ] 自分で1回通して**所要時間を実測**する。掲載文のタイトルは「約7分」だが、
      現在の設定では1人90問ほどになるので**8分を超える可能性が高い**。超えたら
      分数・報酬・問題数を直す（→ F-4）
- [ ] 倫理審査の承認と先生の承認（→ A-1・A-2）
- [ ] 下見の記録を本番と混ぜない手当て（下見専用のスプレッドシートを勧める。→ G-1）

### 学び
索引（manifest）を作るスクリプトと実験ページの両方が同じ設定ファイルを読んでいても、
**ページが実際に要求する見出しの集合**（練習・確認問題・音量確認のサンプルまで含む）は
コードを追わないと分からない。`check_transfer_stimuli.js` はそこを出題の組み立てから
逆算して照合しているので、時点や字を変えたら必ずこれを通すこと。

## 2026-08-20 転写検証実験: サーバ(GAS)を clasp で立て、下見用の窓口をデプロイした

### 概要
下見の記録先スプレッドシートに紐づく Apps Script プロジェクトを作り、リポジトリの
`gas_transfer/` として恒久化してデプロイした。ページ側は集団の振り分け先と回答の送信先を
設定ファイルに持たせた。**まだ2つ残っている**（下の「残タスク」を必ず読むこと）。

### 追加・変更ファイル
| パス | 中身 |
|---|---|
| `gas_transfer/code.gs` | サーバ本体。`gas/code.gs` に `gas/transfer_patch.md` の3差分を当てたもの。差分は `diff gas/code.gs gas_transfer/code.gs` で見える |
| `gas_transfer/appsscript.json` | タイムゾーン Asia/Tokyo、ウェブアプリは匿名を含む全員がアクセス可・実行者はデプロイした本人 |
| `gas_transfer/.clasp.json` | 繋ぎ先（scriptId / parentId）。秘密の値ではないのでコミットする |
| `gas_transfer/README.md` | 入れ直し方・利用許可・疎通確認の窓口 |
| `experiment/transfer_config.js` | `roster.status_url` と `logging.submit_url` に /exec URL、`roster.require_server` を true |
| `project/掲載前チェックリスト.md` | B節を「自動化済み」に書き換え、C節を curl だけで保存まで検証できる形に。G-1 に本番用シートの作り直し手順 |

### 繋ぎ先（下見用。本番は別シートで作り直す）
- スプレッドシート `1gsJ6_Rucv5uoKsgrs_m-Y41sxh5B0qcPKWyHkxveKMs`
- デプロイID `AKfycbw1bnn7H3HCYuD3fDUIi1djGcnea0cVqnA15XiN5ipgwlrsCNkQx4q16-bdN0wbGQRP`
- `/exec` = `https://script.google.com/macros/s/AKfycbw1bnn7H3HCYuD3fDUIi1djGcnea0cVqnA15XiN5ipgwlrsCNkQx4q16-bdN0wbGQRP/exec`

### 設計判断
- **`SPREADSHEET_ID` を定数に直書きした**。スクリプトプロパティは clasp から入れられない
  （入れるには画面を開くか `clasp run` の設定が要る）。このスクリプトは転写検証専用の
  シートに紐づくコンテナバインド型なので、シートIDは1つに決まる。秘密の値でもない。
- **`prod_common.js` は触らない**。あれは実験1（乙課題・frac課題）と共用で、`SUBMIT_URL` を
  埋めると既存の実験の保存先まで動く。転写検証の送信先は `transfer_config.js` の
  `logging.submit_url` に分けた。
- **試し打ちの行に印を付ける仕組みを足した**。参加者IDの頭が `curltest-` の行は `is_test` 列に
  true が立ち、`?action=transfer_purge_test` でまとめて消せる。消せるのはこの接頭辞の行だけ
  なので、本物のデータには手が届かない。これで疎通確認から後始末まで curl だけで完結する。
- **`?action=transfer_health` を足した**。3枚のシートの行数を返す。POST した行が本当に
  シートに入ったかを、スプレッドシートを開かずに確かめられる。
- **`roster.require_server` を true にした**。false だとサーバが落ちているあいだの参加者が
  参加者IDのハッシュで勝手に集団を決めて進み、音声と視覚の人数の釣り合いが崩れる。

### 残タスク（**掲載の前に必ず**）
- [ ] **ウェブアプリの利用許可を1回与える（アカウントの持ち主しかできない）**。
      いま `/exec` は HTTP 403 で `Authorization needed` を返す。clasp で作った
      スクリプトは持ち主が一度も許可を与えていないため、シートを読み書きできない。
      ブラウザで `/exec` を開き「REVIEW PERMISSIONS」→ アカウント選択 →（未確認アプリの
      警告が出たら「詳細」→「移動」）→「許可」。これを済ませるまで疎通確認は全部失敗する。
      → 掲載前チェックリスト B-8
- [ ] **`transfer.js` に「回答を `logging.submit_url` へ送る」処理を入れる**。
      いまページは集団の振り分けだけサーバを使っていて、**1問ごとの回答はどこにも
      送っていない**。封筒の形は `prod_common.js` の `post()` に合わせること。
      → 掲載前チェックリスト B-9

### 学び
clasp は「コードを置く」「デプロイを作る」までは自動化できるが、**Google アカウントの
利用許可（OAuth の同意）だけは持ち主が画面で押すしかない**。自動化の設計をするときは、
この1点が必ず人手として残ることを前提に手順書を書く。

## 2026-08-20 追記: 回答の保存をつなぎ、疎通確認を全部通した

### したこと
- `experiment/transfer.js` に保存の処理を足した（`serverBody` / `postRecord` / `sendRecord`）。
  1問ごとの回答と見え心地の回答を、設定ファイルの `logging.submit_url` へ渡す。
  `prod_common.js` は無変更（実験1と共用のため）。
- `prod_common.js` が外に見せていない3つの値を、同じ規則で作り直して封筒に載せた。
  - 再生機器の申告 … 同意画面のラジオ（`input[name="dev"]`）に自前で聞き手を付けて控える
  - 再開回数・中断していた合計秒 … `PROD.loadState()` の戻り値（`resume_count` /
    `resume_gap_s` / `saved_at`）から、prod_common.js と同じ式で作る
- 本人がウェブアプリの利用許可を与えたので、疎通確認を全部流した。

### 疎通確認の結果（全部合格）
| 確認 | 結果 |
|---|---|
| C-1 生きているか | `{"status":"ok","message":"iFont experiment endpoint"}` |
| C-2 振り分けが交互か | 1人目 `acal` / 2人目 `aprime`（音声と視覚が半々になる） |
| C-3 同じ人が戻ったら | `acal` + `returning:true`（前と同じ集団） |
| C-4 前半に出た人の後半 | `blocked:true, reason:"already_in_calib"` |
| C-5 正答のPOST | `{"status":"ok","correct":true}` |
| C-5 誤答のPOST | `{"status":"ok","correct":false}`（採点が効いている） |
| C-5 見え心地のPOST | `{"status":"ok"}` |
| C-5 行数の確認 | `transfer_trials:4, transfer_wellbeing:2, transfer_roster:2`（**入ったことを確認**） |
| C-7 後始末 | `removed:{4,2,2}` → 行数はすべて 0 に戻った |

### ハマった点（チェックリストにも書いた）
`curl` で GAS に POST するとき **`--post301 --post302 --post303` を付けてはいけない**。
GAS は POST を受けたあと、応答の中身を別ドメイン（`script.googleusercontent.com`）へ 302 で
取りに行かせる作りで、その2本目は GET でなければならない。`--post30x` を付けると2本目まで
POST になり「ページが見つかりません」の HTML が返る。
**このときシートへの書き込みは成功している**ので、失敗と思ってやり直すと行が二重に入る。
実際に最初の試行で 2行ぶん余計に入れてしまった（後始末で消えた）。
応答が HTML だったら、やり直す前に必ず `transfer_health` で行数を見ること。

### 追記: 公開ページでの通し確認（実物）

手元やcurlでなく、**公開されている本番URL**で確かめた。
`https://g19922ma.github.io/iFont/experiment/transfer_calib.html?prod=1&wid=curltest-live`

| 見たこと | 結果 |
|---|---|
| 集団の割り当て | 名簿サーバが `acal`(音声) を返した（`assign_source="server"`）。ハッシュの代用ではない |
| 音量確認のサンプル音 | 録音した肉声が鳴る（代用の合成音ではない） |
| 練習1問 | 動く。**練習は記録されない**ことを行数で確認（本番開始前 `transfer_trials:0`） |
| 本番の回答 | 回答後 `transfer_trials:1` に増えた。**公開ページからの保存が通っている** |
| 封筒の中身 | `participant_id`/`worker_id`=curltest-live、完了コード12桁、**`audio_device`="有線ヘッドホン"**（同意画面のラジオから拾えている）、`resume_count`=0 |
| 二重送信 | `PROD.hasEndpoint`=false。prod_common.js の SUBMIT_URL は空のままで、記録は1か所だけ |

確認に使った行は `?action=transfer_purge_test` で消し、3シートとも0行に戻した。

**注意**: このとき `refresh_hz` が空だった。画面の更新頻度は `requestAnimationFrame` を
40フレーム数えて測るが、タブが裏に回っていると数え終わらないため。自動操作で裏に
回していたせいで、参加者が普通に開いているときは埋まる。既存の実験も同じ作りなので
仕様どおりだが、**もし本番データで `refresh_hz` が大量に空なら「参加者がタブを裏にした」
サインとして読める**（視覚課題の実提示時間の解釈に使える）。

## 2026-08-20 追記2: 下見だけの速さ2水準と、内挿の確認手順を足した

### 1. 基準アニメの速さを2通り試す仕掛け（下見限定）
「同じ進み具合 s まで見せたとき、そこへ**速く**着いたか**ゆっくり**着いたかで正答率が
変わるか」を少人数で先に見る（計画書 6.1・Q12）。本手法は「s が同じなら正答率も同じ」を
前提に組んであるので、その前提の確からしさを凍結の前に知っておきたい。

- 設定: `experiment/transfer_config.js` の `visual.pilot_speed_probe`
  （`enabled` / `family: "fade"` / `base_anim_ms_levels: [600, 1000]`。どちらも暫定値）。
- 実装: `experiment/transfer.js` に `speedProbe()` `speedsFor()` `baseAnimMs()` を追加。
  試行1件ごとに `base_anim_ms` を持たせ、`progressFn()` と記録がその値を見るようにした。
- 効くのは**群A′（視覚較正）でフェードに割り当たった字だけ**。群Acal・群Atest（聴覚）と
  群B（検証）には影響しない。
- 記録は既存の `base_anim_ms` 列に入る（GAS 側の変更は不要）。分析はこの列で2群に分ける。

**実物で確かめたこと**（ローカル配信のページで出題を組み立てて中身を見た）:

| | 本命問題数 | 速さの内訳 | 同じ字×同じ水準の出題 |
|---|---|---|---|
| フラグ入（下見） | 70 | 600ms×56・1000ms×14 | 2回（速い版と遅い版） |
| フラグ切（本番） | 56 | 600ms×56 | 1回 |

フェードに当たるのは1人あたり2字（割付は `families[(字番号 + 連番) % 4]` なので、
どの参加者でもちょうど2字）。非フェードの字はどちらの状態でも 600ms のまま。
`progressFn` も確認: s=0.5 に着くのが 速い版300ms / 遅い版500ms、s=1.0 が 600ms / 1000ms。
`base_anim_ms` を持たない試行（練習・確認問題）は既定の600msに落ちる。

- まぎれ字の速さも本命と同じ集合から選ぶ（まぎれ字だけいつも同じ速さだと、速さの違いが
  「本命かどうか」の手がかりになるため）。確認問題は「いつもの速さ」に固定した
  （操作チェックの基準を1つに保つため）。
- **`enabled` を false にすると、全試行が `base_anim_ms` で動き、いまの本番と完全に同じ
  挙動に戻る**。掲載前チェックリストに D-4 として入り切りの確認手順を足した。

### 2. 計画書の追記（`project/実験計画書_転写検証.md`）
- **6.1 に「内挿の確認」**: 較正で測った提示水準を1つずつ伏せて残りだけで曲線を当てはめ、
  伏せた水準の正答率を予測できるかを見る（1つ抜き交差確認）。既存データだけででき、
  参加者も費用も増えない。大きく外れる水準があれば水準の間隔が粗すぎるということなので、
  A2 で足すか配置を変える。外れの大きさは論文にも載せる。
- **6.1 に「速さによらないかの予備確認」**: 上の仕掛けの位置づけを書いた。
  **どちらの結果でも知見**という書き方にしてある——違わなければ「識別率は現在の進み具合で
  ほぼ決まり履歴・速さに強くよらない」の予備的証拠として生成の前提を支え、違えば
  「履歴依存モデルが要る」という発見として設計を広げる。ただし下見は少人数なので
  「違わない」は効果量と信頼区間つきの予備確認にとどめ、**本当の検定は本番の検証
  （群B と群Atest の一致）が兼ねる**と明記した。
- **10章の A2 確定項目に ⑦** を追加（下見の2速度データによる速さ依存の予備判定）。
- **Q12 を二段構え**（下見で当たりを見る／本番の一致で検定する）に書き直した。

### 注意（所要時間）
フラグを入れると視覚較正の参加者だけ問題数が **94問 → 117問** に増える（約25%増）。
所要時間の実測は必ずこの状態で行うこと。掲載文の「約7分」はもともと不足の疑いがあり、
ここでさらに延びるので、視覚側の実測は特に慎重に。

## 2026-08-20 追記3: 速さの測定を RQ4 に格上げした（下見限定 → 較正フェーズ全体）

前の追記2では「下見だけの予備確認」として入れたが、**正式なリサーチクエスチョンに
格上げ**した（ユーザー決定）。設定の名前も `pilot_speed_probe` → **`calib_speed_probe`**
に変えたので、古い名前で grep しても出てこない。

### RQ4 とは
**「表示の識別率は、いま画面がどこまで進んでいるか（進み具合 s）だけで決まるのか、
そこへ至る速さにも左右されるのか」**（計画書 2章）。
生成のやり方（時間軸の伸び縮み）は「s が同じなら正答率も同じ」＝進み具合が状態を決める、
という見方の上に立っているので、その土台を正面から測る。
- 差が出ない → 進み具合が状態を決めるという見方の予備的証拠（生成の土台を支える）
- 差が出る → 履歴に依存する＝**時間の伸び縮みだけでは移せない**という限界条件の発見

どちらも報告する。否定的結果ではなく、次の設計（速さも入力に持つ生成／扱う速さの範囲の
限定）につながる。

### 三段構え（どこで何に答えるか）
| 段階 | 位置づけ |
|---|---|
| 下見（16人） | **予備**。当たりを見るだけ。「差がない」の証明にはしない |
| 本番の視覚較正（群A′） | **本回答**。効果量と信頼区間を付けて報告 |
| 検証（群B と群Atest の一致） | 進み具合が状態を決めるという見方そのものの**総合テスト** |

### 運用が変わった点（重要）
- **下見のあとに切らない**。較正フェーズのあいだは `calib_speed_probe.enabled` を
  `true` のまま。以前書いた「掲載前に false へ戻す」は**取り消し**。
- 検証フェーズを掲載するときも切らなくてよい（群A′はもう走らないので出題に影響しない）。
  凍結のときも触らない。
- **生成に使う視覚の曲線は600ミリ秒の水準だけ**と事前固定し、**凍結の対象に含めた**。
  1000ミリ秒側は RQ4 に答えるためだけに使い、逆引きには入れない。
  設定に `calib_speed_probe.generation_level_ms: 600` として明示し、
  チェックリスト D-4 のコマンドが `base_anim_ms_levels[0]` と `base_anim_ms` との
  一致まで見るようにした（「整合: OK」と出る）。
  理由は2つ: (1) 2水準を混ぜると速さの効果と進み具合の効果が分けられない
  (2)「結果を見てから生成に都合のよい速さを選んだ」と言われないため。
- 10章 A2 ⑦ は「オフに戻す判定」から**「RQ4 の検出力の確認」**に書き換えた
  ——本番の群A′の人数だと差にどのくらいの幅の信頼区間が付くかを見積もり、
  広すぎるなら 人数を増やす／速さの2水準を離す／測る進み具合の水準を絞る、を先に決める。

### 直したファイル
- `experiment/transfer_config.js` … 名称変更・コメント全面書き換え・`generation_level_ms` 追加
- `experiment/transfer.js` … 参照名とコメントの追従（挙動は追記2から変えていない）
- `project/実験計画書_転写検証.md` … 2章に RQ4 追加／3.1 の凍結に600ms固定を明記／
  6.1 を「生成は600ms版だけ（事前固定）」＋「RQ4の分析」に書き換え／4章の補助指標に
  `base_anim_ms`／10章⑦を検出力確認へ／10章の決定の記録に1行／Q12 を三段構えに
- `project/掲載前チェックリスト.md` … D-4 を「入っていることの確認」へ反転・G-2b 追加・
  G-4 に「逆引きに1000ms側を混ぜない」を追記
- `project/掲載文_下見.md` … §1 の目的表と §5 の事後手順を RQ4 の言い方に統一

挙動は追記2から変わっていない（連番0〜3のどれでも群A′は本命70問、
`generation_level_ms` との整合も OK）。

## 2026-08-20 追記4: 計画書の文体と構成を直した（否定形・括弧・関連研究・RQの一本化）

ユーザーからの4点の指摘に対応。**中身の約束事は一切変えていない**（書き方と置き場所だけ）。

### 1. 否定形をやめ、やることを書く形にした
「〜しない」「〜せず」「〜ではなく」を、やることが主語の肯定文に置き換えた。約35か所。
手続き上どうしても禁止の形が要るものは、12章の「使わない言葉の一覧」に**表として集約**した
（探索2方式について「転写できた」と書かない、という取り決めもここに移した）。
grepで残ったのは3か所だけで、いずれも意図的に残したもの——
「一致しないという形で必ず現れる」×2（検定が失敗を示す形の説明）と
「何もしない翻訳＝恒等写像」（数学用語の言い換え）。

### 2. 括弧を減らした
164組 → 140組。重要な補足は括弧から出して独立した文にし、なくても通じるものは削った。
3組以上残っている段落は4つだけで、すべて短い言い換えの並び
（音の種類のラベル「あ（母音）・か（無声破裂）…」、仮説の見出し「（転写は効く）」など）。

### 3. 関連研究を独立した章にした（新2章）
本文に散っていた文献への言及を集約し、4つの小見出しに整理した。
2.1 時間で打ち切って測る方法／2.2 動的な文字表示／2.3 感覚間の対応づけ／
2.4 刺激を少数に絞るときの先例。
地の文からは文献名を外し、「→ 2.1」のような参照だけを置いた。
Q&Aの中の文献言及は、説明に必要な範囲で残した。

### 4. RQと仮説を3章に一本化した
1.2にあったRQ2・RQ3の先取り（方式の階層の説明・評価の軸2つ）を3章へ移した。
1.2に残したのは主RQの1文と背景・4方式の表・1階/2階の構造で、問いに触れる箇所は
「→ 3章」の参照にした。RQ1という呼び名も新たに与えた。

### 章立ての変更（参照先がずれるので注意）
| 旧 | 新 |
|---|---|
| 1. 背景とリサーチクエスチョン | 1. 背景 |
| （なし） | **2. 関連研究**（新設） |
| 2. 検証したい仮説 | 3. 問いと仮説 |
| 3. 実験の構成 | 4. 実験の構成 |
| 4〜11章 | 5〜12章（1つずつ繰り下げ） |
| （なし） | **4.5 見え心地の質問**（新設。旧「3.5相当」の宙に浮いた参照を実体化した） |

**この繰り下げに合わせて、参照している側のファイルも直した**（33か所）:
`project/掲載文_下見.md` `project/掲載前チェックリスト.md` `experiment/transfer_config.js`
`experiment/transfer.js` `experiment/transfer_stimuli/README.md`
`experiment/tools/build_transfer_gates.py`。
「計画書 3.3」→「計画書 4.3」、「計画書 6.1」→「計画書 7.1」、「2章 RQ4」→「3章 RQ4」など。

### 確認
節・章・Qへの参照を機械的に突き合わせ、**行き先の無い参照はゼロ**。
コードは `node --check` と設定の読み込み、`check_transfer_stimuli.js` の合格まで確認した。

### 注意: 名簿に手作業で消す行が2つ残っている
下見用スプレッドシートの `transfer_roster` に、参加者IDが `anon-` で始まる行が2つある
（8/20 16:11ごろ、`wid` を付けずに `?prod=1` でページを開いたときのもの）。
`transfer_purge_test` は目印のある行しか消せないので、**シートを開いて手で消す**必要がある。
残したまま募集を始めると、音声8人／視覚8人の交互配りが1つずれる。
チェックリストの C-6b に手順を書いた。動作確認でページを開くときは
`&wid=curltest-…` を付けておくと、あとでまとめて消せる。

## 2026-08-21 パイロット（本人2周）の分析と、掲載文の数字確定

### したこと
公開ページを本人が2回通した記録（聴覚94問・視覚117問＝211行）を取り出して分析し、
分析器を雛形として残した。あわせて掲載文の所要時間を実測値で確定した。

### 追加・変更ファイル
| パス | 中身 |
|---|---|
| `experiment/tools/analyze_transfer.py` | 分析の雛形（新規）。dump JSON でもシートのCSVでも読める。出力=字×時点の正答表・曲線PNG・集計まとめ |
| `project/pilot_data/pilot_20260821_trials.csv` | 分析に使う列だけ抜き出した211行の控え |
| `project/pilot_data/out/` | 実行結果（正答表・図2枚・summary.md） |
| `project/パイロット分析_20260821.md` | 結果と読み方の注意（新規） |
| `project/掲載文_下見.md` | 所要を「約10分」に確定。報酬200円据え置き。「まったく分からない問題も混ざる」の一文を追加 |
| `experiment/transfer.js` | 課題説明の箇条書きに「まったく聞こえない／見えない問題も混ざる」を追加 |
| `project/実験計画書_転写検証.md` | 12章の次研究に「あり得る字をすべて選ぶ」回答方式の案を記録 |

### 曲線の要点（**n=1・本人・正解も出題も既知**なので天井。数値は結果ではなく下見）
- **聴覚**: 8字が4段に分かれた。**あ・が=20ms**（最も早い）／か・し・ぱ・ま=40ms／
  つ=60ms／**ら=110ms**（最も遅い）。**最速と最遅の差は約90ミリ秒**。
  理屈と合う: あ・が は最初から声が鳴っている。ら は弾く音が短く弱い。
  つ は破擦なので摩擦が始まるまで「た」と区別できない。
  誤答も筋が通る——ら→な（3回・鼻音と混同）、つ→ち、か→ぱ（20msで調音位置が未着）。
- **視覚**: 表は「字の曲線」ではなく**「字×方式の曲線」**（1人は1字を1方式でしか見ない）。
  誤答はほぼ**ワイプ（5/14=36%）**に集中し、フェード28/28・部分表示14/14・ぼかし13/14。
  ワイプは左端から見せるので、左半分が似た字で取り違える（か→た、つ→て など）。
  他の3方式は**6%でも全問正解**で、本人が答えを知っているためと考えられる。
- **RQ4**: フェードの が・ま で 600ms=14/14、1000ms=14/14。両方とも天井で**判定不能**。
  仕掛けが設計どおり動くことだけ確認できた。本回答は本番の群A′。

### 処理経路の検証（今回の主目的・達成）
ブラウザ → GAS → シート → 取り出し → 集計 → 図 が端から端まで通った。
狙った提示量と実測のずれは**平均1.07ポイント手前**（最大2.20）で、
打ち切り判定を描画の前に置いた設計どおり（1フレームぶん手前で止まる。計画書 7.1）。
確認問題A は聴覚5/5・視覚6/6。確認問題C（最小提示）は聴覚**5/5で天井**——
本番でここが高ければ学習の兆候として扱う。

### 所要時間の実測 → 掲載文を確定
| 集団 | 問題数 | 実測 |
|---|---|---|
| 聴覚 | 94問 | 598秒（約10.0分） |
| 視覚 | 117問 | 444秒（約7.4分） |

**問題数が多い視覚のほうが短い。** 聴覚は1問ごとに合図音＋0.6秒の待ち＋再生＋終了音が
固定で乗るため。掲載文は**「約10分」**に確定し、報酬200円は据え置き（時給1,200円ほど）。
内部メモとして「測ったのは本人なので一般の方は1〜2分延びる見込み」を併記した。

### 学び
1人ぶんの正答率は0%か100%しか取らないので、曲線を重ねると線が完全に隠れる。
`analyze_transfer.py` では字ごとに縦へ最大±1.4ポイントずらし、線の種類も変えて
8本すべてが見えるようにした。図に「ずらしてある」旨を書いてある。

### 追記: パイロット分析を1枚のHTMLにまとめた

`project/パイロット分析_20260821.html`（245KB・自己完結）。
生成器は `project/pilot_data/build_report_html.py` で、`out/*.png` を base64 で埋め込む。
**外部への参照はゼロ**（フォントもCDNも画像ファイルも参照しない）ので、
このファイル1つを渡せばどこでも同じ見た目で開ける。

- 構成: 冒頭に「これは実験の結果ではありません」の注意枠（n=1・本人・正解既知の4点）→
  結論の要約（数値カード4枚＋箇条書き）→ 処理経路の検証 → 音声の段位表と曲線図 →
  視覚の方式別結果とワイプの説明 → 誤答の内訳（1件ずつ読み方を添えた）→ RQ4の状態と操作チェック →
  実測所要時間と掲載文への反映 → 本番への宿題。
- デザイン: 日本語ゴシック（Hiragino Sans → Yu Gothic → Noto Sans JP の順で解決）。
  装飾は罫線と淡い面だけ。表は9つとも横スクロールの枠に入れてある。
  ライト／ダーク両対応（`prefers-color-scheme` ＋ `data-theme` の両方を見る）で、
  body の背景色を明示してあるので、どちらで開いても地色が抜けない。
- 実物で確認: ローカル配信して Chrome でライト／ダークの両方を見た。
  日本語・図・表・○×の色分けが両方で読めることを確認済み。

**ハマった点**: 最初 `td` に一律 `white-space:nowrap` を掛けたため、文章の入るセルが
1行に伸びて表が不必要に横長になった。折り返しを既定にし、数値と○×の列だけ
（`.n` `.c`）1行に保つ形に直した。

## 2026-08-21 追記2: 「フェードは薄い側に潰れている」仮説への備え2件と、報告のRQ対応表化

### 1. フェードの濃さの割り当てを設定に出した
`visual.families.fade.gamma` を追加。フェードの不透明度を **s^gamma** にした。
- **既定 1.0 は現行と完全に同一**。0〜1を1万分割した10001点すべてでビット単位に一致することを
  確認済み（`gamma===1` のときは `Math.pow` を通さず s をそのまま返す実装）。
- 1.0より大きくすると序盤を薄めに通せる（gamma=2 なら s=0.30 で濃さ0.09）。
  内観報告どおり初見データの曲線が階段状に出たときの手当てになる。
- reveal / blur / wipe には触れていない。

### 2. 下見だけ、視覚の打ち切り水準に 3% を足した
`visual.pilot_extra_levels`（`calib_speed_probe` と同じ「下見だけ」の仕組み）。
- 入: 水準が7つ → 8つ（3,6,13,22,34,50,70,100）。1人あたりの本命 70問 → **80問**、
  全体でおよそ 117問 → **134問**（約8.5分見込み。掲載の「約10分」には収まる）。
- 切: `progress_pct_levels` だけになり、無かった頃と同じ挙動。実物のページで
  本命70問・水準7つに戻ることを確認済み。
- ねらいは**初見の人の床がどこにあるか**を見ること。下見では本人が6%でも全問正解して
  しまい、いちばん薄い水準が床として働かなかった。
- チェックリストに **D-5** として入り切りの確認を追加。
  `calib_speed_probe`（較正フェーズ中は維持）と扱いが違う点も明記した。

### 3. パイロット報告を「RQ対応表」構造に作り替えた
`project/パイロット分析_20260821.md` と `.html` の両方。生成器も差し替え。
- 冒頭に **RQ対応表**（RQ1〜RQ4 × 状態バッジ × 一行要約。HTMLは各節へのリンクつき）。
- 各RQに節を立て、**(a) このパイロットから言えること / (b) 言えない理由 /
  (c) いつ・どのデータが答えるか** の3点で記述。
  - RQ1 = まだ答えられない（生成前。配管の検証のみ）
  - RQ2 = 予備的な兆候あり（方式差は実在しそう。ワイプ36%・フェードの二値性）
  - RQ3 = 未実施（見え心地画面は検証フェーズにしか出ない）
  - RQ4 = 仕組みの動作確認済み・判定は不能（両速さとも天井）
- 既存の詳細表・図・注意枠は各RQ節の下に再配置。処理経路の検証は「0. 土台」として
  RQ群の前に置いた。誤答・操作チェック・所要時間は「横断」としてRQ節の後ろへ。
- **話者本人の内観報告**をRQ2の節に引用として追記（フェードは二択に感じられ中間が無い／
  文字が大きいため検出と識別が同時に来る可能性／部分表示は「画素は見えるが字は分からない」
  中間状態が構造的に存在／初見データが階段状かが判定材料）。
- バッジは4色（未回答=赤系・兆候=緑系・未実施=灰・動作確認=青系）でライト/ダーク両対応。
  実物のブラウザで両テーマを確認済み。

### 学び
表の中にバッジを置くと、狭い列で2行に折り返して読みにくくなる。`white-space:nowrap` を
バッジ側に付け、列幅は横スクロールに任せるほうが読みやすい。

## 2026-08-21 追記3: 下見の聴覚に 10ms 水準を追加

### なぜ
パイロットで「あ」「が」が最も早い20msですでに全問正解となり、**曲線の立ち上がりが
20msより手前にある疑い**が出た。早い側の解像度が足りていない可能性がある
（`project/パイロット分析_20260821.md` の RQ1）。

### したこと
- `audio.pilot_extra_gates`（`visual.pilot_extra_levels` と同じ「下見だけ」の仕組み）を追加。
  既定 `{enabled:true, gate_ms:[10]}`。切れば `gates_ms` だけになり、無かった頃と同じ挙動。
- `transfer.js` に `audioGates(ch)` を足し、出題・まぎれ字・確認問題C・問題数の見積もりが
  すべてこの関数を通るようにした。
- `build_transfer_gates.py` にも `extra_gates()` を足し、**打ち切りWAVを作り直した**。
  544本 → **612本**（68かな × 時点8 + 全長）。索引に `pilot_extra_gates_ms` を記録。
- `check_transfer_stimuli.js` も同じ規則で混ぜるようにした。**一式検査は合格**
  （612本の実測長さと索引の食い違い 0.000ms、必要な556件すべてあり）。

### 実物で確かめたこと（ローカル配信のページで出題を組み立てて中身を見た）
| | 合計 | 本命 | 時点 | 確認問題Cの時点 |
|---|---|---|---|---|
| 下見（10msあり） | **108問** | 64 | 10,20,40,60,80,110,150,220 | **10ms** |
| 本番（10msなし） | 94問 | 56 | 20,40,60,80,110,150,220 | 20ms |

確認問題C（最小の提示）が自動で10msへ移ることも確認した。これは意図どおりで、
「いちばん短い時点で当たらない」ことを見るのが確認Cの役目である。

### ⚠ 10ms の刺激は実効の音がごく短い（明記した箇所）
打ち切りの終端には5msの余弦フェードが掛かる決まりなので、10msの刺激では後半5msが
減衰区間になり、**そのままの大きさで鳴るのは頭の約5msだけ**になる。
したがってこの水準は**床の位置を見るためのもので、生成（warp）の材料には使わない**。
この注意を4か所に書いた——`transfer_config.js` のコメント／計画書 7.1／
掲載前チェックリスト D-6／パイロット分析（md・html）。

### ⚠ 所要時間が掲載文の「約10分」を超える見込み
下見用の仕掛けを2つとも入れた状態での見積もり（パイロット実測からの換算）。

| 集団 | 問題数 | 所要の見込み |
|---|---|---|
| 聴覚 | 94問 → **108問** | 598秒 → **約11.5分** |
| 視覚 | 117問 → **134問** | 444秒 → 約8.5分 |

**聴覚が「約10分」を超える。** 測ったのは操作に慣れた本人なので、一般の方はさらに延びる。
掲載文のタイトルは「約10分」のままにしてあり、**掲載直前に「約12分」へ直すかを判断する**
項目としてチェックリスト D-6 と掲載文の備考欄に書いた（報酬200円は12分でも時給1,000円で
計画書11章の目安を満たす）。ここは人が決めることなので、こちらでは変えていない。

## 2026-08-21 追記4: 実機で出た「サーバに接続できませんでした」を潰した＋保存基盤の方針

### 何が起きていたか
実機で `transfer_test` を開いたときお断り画面が出た。直後に curl で叩くと**サーバは正常**。
原因は GAS の一過性の失敗である。GAS は応答の本体を `script.googleusercontent.com` へ
リダイレクトして返す作りで、この経路がときどき失敗し、JSON ではなく **HTML のエラーページ**を
200 で返す。旧実装は問い合わせが1回きりで、しかも `r.json()` が投げた例外を
「通信できなかった」と同じ扱いで握りつぶしていたため、**サーバが動いているのに
参加者を取りこぼしていた**。

### 直したこと（`experiment/transfer.js` / `transfer_config.js`）
- `rosterQueryOnce()` を新設。応答をいったん**文字列で受け取ってから JSON.parse** する。
  読めなければ「JSONでない応答」として**失敗に数える**。ここが今回の本丸。
- `resolveAssignment()` を**3回まで作り直す**形に（0.8秒 → 2秒 → 4秒、散らし±400ms）。
  3回とも失敗して初めてお断り画面へ。
- お断り画面に「**もう一度試す**」ボタン。押すと問い合わせからやり直し、
  通ればそのまま同意画面へ進む。そのために起動処理を `startSession()` に切り出し、
  **何度呼んでも安全**な形にした。
- **何回目で通ったかを記録に残す**。列を増やさずに済むよう `assign_source` に書く
  （`server` / `server:retry2` / `server:retry3`）。掲載後にこの列を数えれば頻度が分かる。
- 回答の送信（`postRecord`）も**3回まで作り直す**（0.8秒 → 2.5秒 → 6秒）。
  no-cors なので分かるのは「送信そのものが失敗したか」だけ。
  最後まで送れなかった件数は `sendFailures` に貯めてコンソールに出す。

### 実物で確かめたこと（ローカルで障害を再現）
| 試したこと | 結果 |
|---|---|
| 200でHTMLを返すサーバ（実機の現象そのもの） | 3回試して約3.9秒後にお断り画面。理由は「JSONでない応答」と正しく判定 |
| そのあとサーバを復帰させて「もう一度試す」を押す | 同意画面へ到達。`assign_source=server` |
| 2回失敗→3回目に成功するサーバ | 自動で復帰し、`assign_source=server:retry2` と記録 |
| 届かない宛先への送信 | 3回試して約5.0秒で諦め、`sendFailures` が1増える |
| 生きている宛先への送信 | 1回・3msで成功 |

### 保存基盤の方針: 最終的には Firestore 一本化
`docs/FIRESTORE_TRANSFER_MIGRATION.md`（新規）に設計と手順を書いた。要点だけ。

- **段取り**: (a) 今回の下見は GAS ＋ 再試行で実施 →
  (b) Firestore 併用を有効にして書き込み経路を実データで実証 →
  (c) WISS投稿後に採番と重複チェックを Firestore で作り直して一本化。実験1はその上で。
- **理由**: GAS はリダイレクト経路が一過性で失敗して HTML を返す。Firestore は直APIで
  中継が無く安定だが、採番と重複チェックの作り直しが要り今日の日程に載らない。
- **設計の肝**（トランザクション不要にできる）:
  - 重複チェックは名簿ドキュメントの **create の成否**で判定する。読み出しを許さずに
    「作れたら初参加／作れなければ再訪」が分かる。
  - 採番は **increment 変換**で 1 増やし、**書き込みの応答に返る値**を自分の番号として使う。
    サーバ側で不可分に処理されるので同時アクセスでも衝突しない。
  - ルールは「作るだけ・読めない・消せない」。カウンタだけ更新を許すが
    **+1 の更新に限る**。`firebase/firestore.transfer.rules`（新規）に置いた。
- **既存プロジェクトの流用は勧めない**（調査済み）。Kikiwake（`kikiwake-c2e8e`）は別研究の
  本番で、ルールが「ログイン済みの本人だけ」前提。転写検証はログインしない設計なので
  ルールを緩める改修が要り、動いている研究に手を入れることになる。
  実験1用のプロジェクトは**そもそも存在しない**（`docs/FIREBASE_SETUP.md` は作る手順のみ）。
  → 転写検証用に**新規プロジェクトを1つ作る**のが最短かつ安全。**これは人の手作業**。

### 日程の判断（推奨）
**下見は GAS ＋ 再試行のまま出す。Firestore 移行は下見のあと。**
今日の失敗は再試行で塞がった／Firestore はプロジェクトすら無くキーが揃うまで試せない／
下見の目的は運用確認で基盤に依存しない／未検証の採番機構を有償募集の直前に入れるのは
割に合わない（採番と重複チェックは間違えると取り返しがつかない）。
**撤退線は満たしてある**——今回の変更は再試行の追加だけで、現行構成は壊していない。

### 追記: 本番URLで測ったら GAS の応答が約6秒だった（タイムアウトを15秒へ）

再試行を入れて本番URLで確かめたところ、**1回目の読み込みで実際に一過性の失敗が起き、
2回目で自動復帰した**（`assign_source=server:retry2`）。修正前ならこの参加者は
お断り画面になっていた。仕組みは狙いどおり働いている。

そのうえで、素の応答時間を本番URLから測ると **約5.9秒**だった。
待ち上限が8秒では少しの揺らぎで間に合わず、サーバが生きているのに
「時間切れ」として失敗に数えていた。**`roster.timeout_ms` を 8秒 → 15秒**にした。

あわせて、作り直している最中に「混み合っています。接続をやり直しています（2/3）…」と
画面に出すようにした。無言で数十秒待たせると参加者が閉じてしまうため。

**`?v=` の据え置きで修正が届いていなかった件**（同日・別項）と合わせ、版を **3.2** にした。

## 2026-08-21 追記5: 刺激を合成音声（VOICEVOX）へ切り替える — 話者選びの試聴ページまで

### 方針（ユーザー決定）
最終的な応用が合成音声を入力とするシステムなので、較正も同じ音源で行うほうが一貫する。
**自然音声の資産は破棄せず凍結保存し、比較材料として残す。**

### 厳守した条件
**完全に無加工。** VOICEVOX に text と speaker を渡して返る `audio_query` を
**1つも書き換えずに**合成へ回している。長さの統一・F0平坦化・VOT補正・字別フィルタ・
文脈切り出し、いずれも行っていない。加工の口をコードに作っていない。
唯一そろえたのは音量で、1音まるごとに一定倍率を掛けるだけ（自然音声と同じ方針）。

### 作ったもの
| パス | 中身 |
|---|---|
| `experiment/tools/build_voicevox_stimuli.py` | 全68かなを無加工で合成し、音量そろえと測定を行う（新規） |
| `experiment/tools/build_voicevox_audition_page.py` | 聴き比べページの生成器（新規） |
| `experiment/voicevox_audition/<話者ID>/<かな>.wav` | 4話者 × 68字 = 272本（約5.7MB） |
| `experiment/voicevox_audition/onsets_<ID>.json` | onset と測定値 |
| `experiment/voicevox_audition.html` | 聴き比べページ（行=かな × 列=話者） |

### 候補にした4話者（すべてノーマル系。ささやき・ヒソヒソは声帯が鳴らないため除外）
| ID | 話者 | 声の傾向 |
|---|---|---|
| 108 | 東北きりたん | 落ち着いた女性（現行の想定） |
| 13 | 青山龍星 | 低めの男性 |
| 29 | No.7 | はっきりした女性 |
| 8 | 春日部つむぎ | 高めの女性 |

### 測定について（正直に書いておく）
当初は VOT（破裂から声が出るまで）を出すつもりだったが、**自動で破裂の瞬間を取るのは
難しく、3回作り直しても当てになる値にならなかった**。そこで名前を変え、
**「音が始まってから声が出るまで」**（onset から周期が現れるまで）を出している。
無声破裂なら大きめ、有声なら0に近い、という向きは同じで、こちらは安定して測れる。
ページにも「測定は粗いので目安。判断は耳が主」と明記した。

作り直しの過程で踏んだ穴を2つ記録しておく。
1. **5ms窓では周波数分解能が200Hzしかなく**、破裂の高い成分と声の低い成分を分けられない。
   窓を25msにし、位置の細かさはずらし幅5msで確保した。
2. **無音でも「周期あり」と判定していた。** 振幅がほぼ0だと自己相関の比が数値のゆらぎで
   大きく出る。エネルギーが雑音床+10dBを超えていることを条件に足して直した。

### 次にユーザーがやること
試聴ページで**1話者を選ぶ**。選ばれた声で全68字の本刺激と打ち切りWAVを作る。
清濁（ぱ/ば・か/が・た/だ・さ/ざ）と、以前つまずいた ぽ・ぴ・ぷ を表の上部に固定してある。
どの話者も落ちるようなら報告して判断を仰ぐ。

### ライセンス表記
採用した話者について `VOICEVOX:<話者名>` を実験ページと論文に入れる。
ページ末尾に候補4名ぶんの表記を用意した。

## 2026-08-21 追記6: 話者選びをやり直した — VOT の実測と、1ファイルで完結する聴き比べページ

### 概要
追記5（合成音声への切り替え・試聴ページ）を**作り直して置き換えた**。
やり直した理由は3つ。(1) 追記5は VOT（破裂から声が出るまでの時間）の測定を断念していたが、
清濁を判断する材料としてこれがいちばん効く。(2) 候補の音（4話者×68字・5.6MB の wav）が
リポジトリに入っていた。1話者を選んだら残り3話者ぶんは捨てるものなので入れない方針にした。
(3) 試聴ページが隣のフォルダの wav を参照しており、ページだけ共有しても鳴らなかった。

### 置き換えたファイル

| 消した（追記5） | 置いた（追記6） |
|---|---|
| `experiment/tools/build_voicevox_stimuli.py` | `experiment/tools/build_tts_candidates.py` |
| `experiment/tools/build_voicevox_audition_page.py` | `experiment/tools/build_tts_voice_compare.py` |
| `experiment/voicevox_audition.html` | `experiment/tools/tts_voice_compare.html` |
| `experiment/voicevox_audition/**`（wav 272個・コミット済みだった） | `experiment/tts_candidates/<話者名>/`（`.gitignore` 済み） |
| （なし） | `project/合成音声_話者候補.md`（VOT表・所見・クレジット・再現手順） |

合成のやり方（**完全に無加工**・`audio_query` を1つも書き換えない・音量そろえのみ）と
候補4話者（東北きりたん108／青山龍星13／No.7 29／春日部つむぎ8・すべてノーマル系）は追記5のまま。

### 配信用の100Hzハイパスは掛けない（決定・記録）
自然音声では電源ハムを落とすため配信する音そのものに 100Hz ハイパスを掛けていたが、
**合成音にハムはないので掛けない**。音に触れる処理が1つ減る。
onset 検出**だけ**に使う 150Hz のハイパスは自然音声と同じく掛けている（書き出す音には残らない）。

### VOT は測れた（追記5の「測れなかった」を撤回）
追記5は「破裂の瞬間を自動で取れない」として VOT を諦めていたが、次の3点で測れるようになった。
1. **破裂を探す範囲を onset の20ms手前まで広げてはいけない。** 区間の先頭では「3ms前との差」が
   取れず、そこに偽の立ち上がりが立つ。実際に破裂が onset より20ms前に出た。手前は5msまでにした。
2. **声の周期は40ms窓で見る。** 25msだと低い男声（基本周波数90Hz前後）で周期が2つ強しか入らず、
   青山龍星がほぼ全滅した。自己相関はずらし幅ごとに重なりが短くなるので、重なる範囲の力で割り直す。
3. **声の出はじめを「低い帯域の力の立ち上がり」で詰めてはいけない。** 破裂の音にも低い成分が
   あるので破裂まで戻ってしまう（東北きりたんの「か」の VOT が 2ms になった）。
   声が鳴っている区間で周期の長さを求め、**その長さの繰り返しがあるか**を1msずつさかのぼって見る。
   さらに「力が声の1/6を下回ったら打ち切り」の歯止めを足した。

### 主要な発見（清濁の間の差＝無声のVOT − 有声のVOT・13対の中央値）

| 話者 | 中央値 | 差が5ms未満の対 |
|---|---|---|
| 青山龍星（男性・低音） | **+30ms** | 2／13 |
| No.7（女性） | **+19ms** | 2／12 |
| 東北きりたん（女性・第一候補） | **+2ms** | 7／13 |
| 春日部つむぎ（女性・高め） | **+0ms** | 10／13 |

東北きりたん・春日部つむぎは**か行では差が出る（+15〜+32ms）が、た行・ぱ行でほぼ差がない**
（た／だ ±0、ぱ／ば ±0〜+1、ぽ／ぼ −1〜±0）。以前からの「合成した1音で清濁が混同される」
（`project/相談_合成かな1音の清濁混同.md`）と符合する。当時は加工が原因かと疑っていたが、
**無加工でも同じ傾向が出た**ので、話者そのものの性質という可能性が高い。
青山龍星は が・だ・ば・ど・ぼ で「破裂の前から声が鳴っている」（VOT がマイナス）が明確に出る。

### 聴き比べページ
`experiment/tools/tts_voice_compare.html`（公開時 https://g19922ma.github.io/iFont/experiment/tools/tts_voice_compare.html ）。
音を MP3(128kbps・モノラル)に変換して**ページに埋め込んである**ので1ファイルで完結する（2.8MB）。
判断のもとの音は無加工の wav（`experiment/tts_candidates/`）で、ローカルではそちらへ自動で切り替わる。
中身: 話者カード4枚（クレジット・中央値・「この話者で全68字を通して聴く」）／清濁の対
（1セルに清と濁を並べ差を表示・「続けて」で4話者ぶん清→濁）／要注意の字 ぽ・ぴ・ぷ／全68字
（「横に聴く」で1字を4話者ぶん）／下部の帯に進捗と「止める」（Esc でも止まる）。
Chrome で再生・連続再生・進捗表示を実機確認済み。

### ライセンスで注意する点
`VOICEVOX:<話者名>` の表示は4話者とも共通だが、**青山龍星は企業が携わる場合、No.7 は商用の場合に
権利者への事前確認が要る**。大学の研究発表がこれに当たるかは判断が分かれうるので、
この2名を採用するなら問い合わせておくのが安全。東北きりたん・春日部つむぎはこの懸念がない。

### 次にユーザーがやること
聴き比べページで1話者を選ぶ。数値だけなら青山龍星＞No.7＞きりたん≒つむぎ。
きりたん・つむぎを選ぶ場合は**た行・ぱ行の清濁が耳で区別できるか**を念入りに確かめること
（数値上は区別がつかないので、紛らわしければ結果が「知覚の限界」ではなく「刺激の欠陥」で決まる）。

### 再現
```bash
curl -s http://127.0.0.1:50021/version      # VOICEVOX エンジン（2026-08-21 時点 0.25.2）
python3 experiment/tools/build_tts_candidates.py      # 合成＋測定（約2分）
python3 experiment/tools/build_tts_voice_compare.py   # ページ生成（ffmpeg 必要）
```

## 2026-08-21 追記7: 見え心地を独立実験（群C）に切り出した／聴覚の時点を字ごとの配置にした

### 概要
2つのユーザー決定を実装した。
(1) **見え心地の評価（RQ3）を群Bの末尾から切り離し、独立した実験「群C」にした。**
(2) **聴覚の打ち切り時点を、全字共通の7点から「字の音の種類ごとの配置」に変えた。**
どちらも計画書 `project/実験計画書_転写検証.md` に反映済み。

### (1) 群C（見え心地）

**なぜ切り出したか**（計画書 4.5 に記載）
- 群Bは1人あたり90問以上の識別課題をこなす。その直後に主観を聞くと、答えているのが
  表示の性質なのか、やり終えた疲れなのかを分けられない
- 独立させれば**打ち切りなしで繰り返し流す**提示にできる。字幕は本来くりかえし流れるもの
- 群Bの負担が減る
- 掲載は検証フェーズと**並行**なので日程は延びない

**新しく作ったファイル**

| ファイル | 何をするもの |
|---|---|
| `experiment/transfer_comfort.html` | 群Cの入口。薄いエントリ（CSS＋JS4本を読むだけ） |
| `experiment/transfer_comfort_config.js` | 群Cだけの設定（代表字・質問項目・繰り返しの間合い・名簿の使い方） |
| `experiment/transfer_comfort.js` | 本体。同意 → 見え方の確認 → 説明 → 8本の評価 → 4択1問 → 完了コード |
| `experiment/tools/check_comfort_render.js` | 群Cのページの一式検査（**描画器が `transfer.js` と一字一句同じか**を見る） |

**提示の設計**: 代表字2字（あ・ぱ）× 4方式 = **8本**。1本ごとに7件法3項目、
最後に4方式の見本を横に並べた4択1問。提示順は方式も字も混ぜ、何番目に出したかを記録する。
評価しているあいだアニメは自動でくりかえし流れる（現れきった姿を1.2秒 → 0.3秒消す → また流す）。
「▶ もう一度みる」の押下回数と自動再生の回数を別々に数える。
**所要はおよそ4分**（実測はまだ。掲載前チェックリスト H-4 で測る）。

**なぜ `transfer.js` を読み込まず、描画器を写したか**: `transfer.js` は読み込まれた時点で
較正／検証の手続きを自分で走らせるので、群Cのページに載せると別の実験が始まってしまう。
そこで4方式の描画器と進み方の補間だけを一字一句写した。**ずれると群Bと群Cで違う絵を
見せることになる**ので、`check_comfort_render.js` が関数ごとに突き合わせる。

**記録**: 群Bのときと同じ `transfer_wellbeing` シートに1参加者1行。`phase` 列に `comfort`、
`group` 列に `c` が入る。**GAS の保存側は無変更でよい。** 1本ごとの数値は
`wellbeing_json` の中（`order` `char` `family` `ratings` `auto_plays` `replays` `view_ms`）。

**残っている結線（掲載前に必要）**: サーバの名簿（`gas_transfer/code.gs` の `transferStatus`）が
`comfort` フェーズを知らない。いまの状態で `?prod=1` で開くと、名簿の問い合わせが3回とも
失敗して「接続できませんでした」の画面になる（実機で確認済み・意図した安全側の動作）。
足す内容は掲載前チェックリスト **H-1** に書いた。Firestore へ移したあとも同じ2つの規則を移植する。

**群Bの見え心地画面は既定で切った**。`transfer_config.js` の `wellbeing.in_group_b` を
`false` にし、`transfer.js` の `GROUPS.b.wellbeing` がそれを読むようにした（1行）。
`true` に戻せば旧方式に戻る。

### (2) 聴覚の時点を字ごとの配置に

**根拠**: Furui (1986) の日本語CV音節の打ち切り実験——破裂音・鼻音は50〜70ミリ秒、
無声摩擦音は平均120ミリ秒で識別できる。下見（`project/パイロット分析_20260821.md`）でも
あ・が=20ms、破裂系=40ms、つ=60ms、ら=110ms で天井に張り付いていた。
全字に同じ並びを当てると、早く天井に着く字では後半の点が、遅い字では前半の点が、
どちらも情報を持たない点になる。

**採用した配置**（暫定・A2で再確定。すべて onset を0とした絶対時間ミリ秒）

| 音の種類 | 字 | 時点 |
|---|---|---|
| 母音・破裂・鼻音 | あ・か・が・ぱ・ま | 10, 20, 30, 40, 55, 70, 100 |
| 破擦 | つ | 20, 40, 60, 80, 110, 150, 200 |
| 無声摩擦 | し | 30, 60, 90, 120, 150, 190, 240 |
| はじき | ら | 20, 40, 60, 80, 110, 150, 200 |
| まぎれ字（`_default`） | 上記以外の60字 | 20, 40, 60, 80, 110, 150, 220 |

**絶対時間のままにした理由**: 字をまたいだ比較（曲線どうしの距離E）も、生成の逆引きも、
どちらも絶対時間の軸で行う。「その字の全長の何%」という相対配置にすると両方が成り立たない。

**実装は最小限で済んだ**——`transfer.js` の `gatesFor()` も `build_transfer_gates.py` の
`gates_for()` も、もともと「字をキーにした表」を読めるようになっていた。
そのため `gates_ms` に字ごとの並びを書くだけで通る。`gates_ms_by_char` のような
**別のキーは作らなかった**（同じ表が2か所に分かれると必ず食い違うため）。

コード側で足したのは次の3点だけ。
1. `audio.include_full_gate`（新しい設定）… 各字の並びの最後に「全長」を1点足す。
   曲線の天井を字ごとに押さえるため。`transfer.js` の `buildAudioTrials()` に3行
2. 確認問題C（ほとんど情報のない問題）が、全字共通の最小値ではなく
   **その字の最小の時点**を使うようにした。「し」は30msから始まるので、
   共通の20msを当てるとその字に無い刺激を要求してしまう
3. `audio.pilot_extra_gates.enabled` を `false` に。これは「全字が同じ7点」だった頃の
   応急処置で、いま `true` に戻すと「し」にまで10msが足される

**刺激を作り直した**: `python3 experiment/tools/build_transfer_gates.py --src recordings_raw/adopted
--onsets recordings_raw/adopted_onsets.json` で **544本**（68かな ×（7時点＋全長））。
索引に載らなくなった古いWAV **95本を削除**した（`check_transfer_stimuli.js` が
「索引に載っていない余分なWAV」として見つけてくれる）。truncated（音源が足りない）は0本。

**問題数と所要は変わらない**: 聴覚は**108問のまま**（以前は「共通7点＋下見の10ms」で
1字8点、いまは「その字の7点＋全長」で1字8点）。所要の見込み **約11.5分**も据え置き。
「し」が最長240msまで伸びたぶん、1問あたりコンマ数秒だけ長くなる。

**まだやっていない**: 視覚（群B）の時点 `visual.gates_ms` は全字共通のまま。聴覚と同じ時点で
曲線を比べる決まりなので、**検証フェーズの掲載前に**聴覚と同じ字ごとの配置へそろえる
（計画書 11章 A2②）。

### 版を上げた
- `transfer.js` の `VERSION` を **3.2 → 3.3**、`transfer_calib.html` / `transfer_test.html` の
  `?v=` も 3.3 に（各4か所）
- `transfer_config.js` の `config_version` を **prov-2026-08-20b → prov-2026-08-21**
- 群Cのページは別系統の版番号 `c1.0`（`transfer_comfort.js` の `VERSION` と
  `transfer_comfort.html` の `?v=` 5か所）。
  ⚠ **`transfer_config.js` を直したら群Cの `?v=` も上げること**（3ページで共用しているため）

### 通した検査
```bash
node experiment/tools/check_transfer_stimuli.js   # 合格（544本・108問・時点は3通りの配置）
node experiment/tools/check_comfort_render.js     # 合格（描画器14個が transfer.js と一致）
```
群Cのページはローカルサーバで最後まで1回通し、記録の中身（提示順・7件法・再生回数・
最終選択）と完了コードが出ることを実物で確認した。`?prod=1` で開くと、名簿が
`comfort` を知らないため意図どおりお断り画面になることも確認した。

### 学び
- 「字ごとの時点表」は**もともと両方の実装が読める形になっていた**。新しい仕組みを足す前に
  既存のヘルパを読むと、この日の作業は設定を書くだけで済んだ
- 打ち切りWAVを作り直すと**古い時点のファイルが残る**。`check_transfer_stimuli.js` の
  「索引に載っていない余分なWAV」の検査が無ければ気づかず配信していた
- ブラウザ自動化でアニメの確認をすると、タブが裏に回っている間 `requestAnimationFrame` が
  止まる。「動いていない」ように見えても実装のせいとは限らない。
  ただしこれをきっかけに、再生の時計を「play() を呼んだ時刻」から
  「最初の1コマが描かれた時刻」に変えた（参加者が別タブに移って戻ったとき、
  いきなり完成形が出るのを防ぐ）

## 2026-08-21 追記8: 群Cに「5文字続けて出す」提示を2通り足した（12本・約7分）

### 概要
ユーザー決定により、群C（見え心地）の提示を **4方式 × 3通りの出し方 = 12本**に拡張した。
字幕は本来1文字ずつ続けて流れるものなので、1字だけの印象では字幕としての見え心地を
答えたことにならない、という理由である。あわせて最後の質問を2問に分けた。

### 3通りの出し方

| 出し方 | 何をするか | 画面 |
|---|---|---|
| `single` | 1字が現れて、しばらく残って、消える | キャンバス1枚 |
| `row5` | 5字を左から1字ずつ出し、**出た字はそのまま残す** | キャンバス5枚 |
| `swap5` | 5字を**同じ位置で1字ずつ差し替える** | キャンバス1枚 |

使う字は 1字条件が **あ**、5字条件が **あ・か・し・ま・ら**（事前固定）。
**意味のある語にしない**——言葉として読めると、表示の見え心地ではなく語の読みやすさを
答えられてしまうため。**字の種類は増やしていない**（比べたいのは出し方であって字ではない）。

### 字と字の間隔: 現れきってから300ミリ秒（＝開始どうしの間隔900ミリ秒）

`transfer_comfort_config.js` の `timing.inter_char_gap_ms = 300`。

- **なぜ**: ここで測りたいのは「字幕として続けて見ていられるか」だけで、
  **前後の字が互いの見え方を邪魔する現象（視覚的マスキング）は測らない**。
  先行研究では、視覚的マスキングが起きるのは2つの刺激の開始どうしの間隔（SOA）が
  0〜200ミリ秒の範囲とされ、文字識別への抑制は約60〜120ミリ秒で最大、120ミリ秒付近で消える
  （Enns & Di Lollo のレビュー、経頭蓋磁気刺激による文字識別の抑制の研究など）。
  アニメ本体が600ミリ秒なので、現れきってから300ミリ秒空けると SOA は **900ミリ秒**、
  干渉が起きる時間帯の **4.5倍**離れる。
- **読み上げの速さとの関係**（実測して確かめた）: 録音した自然音声の全長は
  ターゲット8字で **303〜419ミリ秒（平均347）**、全68かなで250〜451ミリ秒。
  900ミリ秒はその **2.6倍ほどゆっくり**。5文字続けて読む速さを土台にしつつ、
  干渉しないところまで十分に間を空けた、という位置づけになる。
  （当初の指示にあった「1字180〜280ミリ秒」は実測より短めだった。実測値のほうを採用した。）
- **横並びの字間**: 残った字と新しい字が空間的に隣り合うので、これは時間の干渉ではなく
  隣の字が邪魔をする現象（crowding）の領域。対策として字間を**1文字分**あける
  （`layout.gap_ratio = 1.0`）。

### 大きさをそろえた
3条件とも1字あたりの表示幅を同じにする（`layout` で計算）。1字条件だけ大きく出すと、
「出し方の違い」ではなく「字の大きさの違い」を答えられてしまうため。
実際の幅は記録に残す（`char_px`）。カード幅718pxで1字73px、字間73px。

### 最後の質問を2問に分けた
聞きたいことが「どの**現れ方**（方式）か」と「どの**出し方**か」の2つあり、
1問にまとめると参加者がどちらについて答えたのか分からなくなるため。

1. **現れ方**（4択）: 4方式の見本を1字の形で横に並べ、同時に流す
2. **出し方**（3択: 横に並べる／その場で入れ替える／どちらでもよい）:
   見本は**1問目で選ばれた方式**で出す。この2つは同じ方式なので**1周ずつかわりばんこに**流し、
   いま流しているほうを枠の色で示す

> **なぜかわりばんこか**: 部分表示（reveal）とぼかし解除（blur）は、画素の並びと作業用
> キャンバスを**1組しか持たない**作りになっている（`transfer.js` から写した実装）。
> 同じ方式のアニメを2つ同時に動かすと互いの状態を壊す。質問1で4つ同時に流せるのは
> 4方式がすべて違うからで、質問2は同じ方式が2つなので同時には流せない。

### 実装のキモ: 1つの再生器で3通りをまかなう（`makePlayer`）
**同時に動いている字は常に1つだけ**という性質を使っている。字と字の間隔（900ミリ秒）が
アニメの長さ（600ミリ秒）より長いので、どの瞬間も動いている字は高々1つ。
そのため `transfer.js` から写した描画器（内部状態を1組しか持たない）をそのまま使える。

- 1周 = 字数 × 900ms ＋ 残す時間 ＋ 消す時間。1字条件2.1秒／5字条件6.5秒
- 現れきった字は**描き直さない**（横並びではそのまま残る）
- 入れ替え条件は、次の字を出す前にキャンバスを消す

### 実機で見つけて直した2つの不具合
1. **5字が枠からはみ出して右端が切れた。** `screenEl.clientWidth` には左右の余白（padding）が
   含まれるので、引かずに5等分すると幅を過大に見積もる。カードの内側の幅から
   余白とキャンバスの枠線を引いて計算するよう直した（`charPx`）。
   質問2の見本は、カードではなく**その見本自身の枠**の幅で計算する（`buildStage` の `px`）。
2. **横並びで「2文字目だけ出ていない」抜けが残った。** 画面が重いときや、参加者が
   別のタブに移っているあいだは、ブラウザが描画のコマを飛ばす。飛んだぶんの字が
   一度も描かれないまま先へ進んでしまう。次の字に移るときに、まだ描き終わっていない
   手前の字を**完成した姿で描き足す**よう直した（`fillSkipped`）。

### 所要と報酬（**要ユーザー確認**）
- 試算 **6.7分**（`node experiment/tools/check_comfort_render.js` が同じ勘定で出す）。
  内訳: 前置き100秒 ＋ 慣れ15秒 ＋ 1字条件4本×(2.1+12)秒 ＋ 5字条件8本×(6.5+12)秒
  ＋ 最後の2問70秒 ＋ 完了15秒
- 掲載文のタイトルは **「約7分」**、報酬は **120円**を提案（時給約1,030円。
  計画書11章の「時給1,000円目安」を満たす。100円だと時給860円で下回る）

### 検査を足した
`check_comfort_render.js` に次を追加した。
- 字と字の開始の間隔（SOA）が400ミリ秒未満なら**不合格**にする
  （干渉が起きる時間帯に近づけたら気づけるように）
- 横並びの字間が1文字分未満なら**不合格**にする
- 質問2の選択肢を本編で1度も見せていなければ不合格
- 1周の長さと所要のめやすを表示する

### 版
`transfer_comfort.js` の `VERSION` を **c1.0 → c1.1**、`transfer_comfort.html` の `?v=` も
5か所とも c1.1 に。設定版は **comfort-prov-2026-08-21b**。

### 学び
- ブラウザ自動化で見た「動いていない」は環境のせいのことが多いが、**そのまま実装の弱点でもある**。
  今回はタブが裏に回ってコマが飛ぶ状況を再現できたおかげで、実機（重い端末・ながら見）で
  同じ抜けが出るバグを掲載前に潰せた
- `clientWidth` は padding を含む。5等分するような計算では必ず引く
