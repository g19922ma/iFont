# HANDOFF — 2026-08-25 時点

**次のセッションは、この文書を最初から最後まで読んでから作業を始めること。**
WISS 2026 投稿まで残り6日（締切 8/31 23:59）。

---

## 0. いま何が起きているか（30秒で把握する）

**聴覚刺激は完成した。掲載できない理由は、いま視覚較正（群A′）の水準が決まっていないことだけ。**

60Hzの画面は16.7ms刻みでしか書き換わらない。視覚較正のアニメは300msなので、指定した進み具合が
細かすぎると「1枚も描かれず真っ白」になる試行が出る。今日、丸山さんが本番URLで自分で通したところ、
**視覚の本命25問中11問が真っ白**だった。この問題への対処は3案（`project/相談_視覚水準とフレーム量子化.md`）
に整理してあるが、**まだどれを採るか決めていない**。

**次にやること**: この文書の §2 を読んで、丸山さんと3案（A/B/C）のどれにするか決める。
決まったら `transfer_config.js` の `visual.families.fade.progress_pct_levels` を置き直し、
下流の `fade.gamma` / `blur.max_radius_px` を再判定してから、Firestoreの掃除をして掲載申請する。

---

## 1. 聴覚刺激 — 完成済み（今日の主な成果）

### 1.1 何が起きたか（要約。詳細は `project/刺激作成_キャリアフレーズ.md` が一次記録）

丸山さんが68字を聴取し、**び・り・ろ の3字が正しく聞こえない**と判定した。あわせて、
後続モーラを「さ」に決めていたのに**544本が古い「ぱ」版のまま作り直されていなかった**取りこぼしも
見つかった。原因も対処も字ごとに違うので、まとめて手当てした。

| 何が問題だったか | 原因 | 対処 |
|---|---|---|
| 544本が「ぱ」のまま | 後続音を決めたあと作り直す手順を1回飛ばしていた | 「さ」版で544本を作り直し |
| り→「い」に聞こえる<br>ろ→「も」に聞こえる | 弾き音（/r/）が母音に埋もれる | 対象モーラの核を外す（`accent=0`）。**り・ろの2字だけ**。全字で外すと本命の「か」「つ」の切り出しが壊れるため |
| び→「い」に聞こえる | 語頭の破裂音は手前に音が無いと閉鎖が作れない | 「あ＋び＋さ」の3モーラで合成し、前後の「あ」「さ」を削って切り出す。子音部 8ms→31ms |

**り・ろ・び はいずれも本命8字（あ・か・が・ぱ・し・つ・ま・ら）ではなく偽ターゲット候補**なので、
この個別対応が本命の測定に影響しない。

### 1.2 検査結果

544本・検算0本不一致／末尾の混入0本／前置き50〜50ms／`check_transfer_stimuli.js` の一式検査 全項目合格。
ただし**この日も機械検査を2回外している**（「ん」残り0/68→実際34/68字、「り」の弾き音16ms存在→
耳では「い」）。**刺激の合否は必ず人の耳で最終判定する**方針を再確認した。

### 1.3 現在のファイル状態

| パス | 内容 |
|---|---|
| `experiment/transfer_stimuli_amitaro/` | **現行の544本（完成・配信中）**。「さ」版・り/ろ核なし・び 3モーラ方式 |
| `experiment/transfer_config.js`（`config_version: "prov-2026-08-25a"`） | 打ち切り点・偽ターゲットの選び方など、版履歴コメントに詳しい経緯あり |
| `experiment/tts_candidates_carrier2/_rejected_acc0/` | 全字 `accent=0` 版（却下・比較用に保存） |
| `experiment/tools/check_final_stimiuli.html` ほか比較ページ | すべて音声base64埋め込み（隣フォルダ参照はローカルで鳴らないため） |
| `project/刺激作成_キャリアフレーズ.md` | **一次記録。論文の刺激の節はここから書く** |

**この節はもう触らなくてよい。** 話者・後続モーラ・切り出し法は確定済み。

---

## 2. 視覚較正の水準 — 未解決（次のセッションの最優先事項）

### 2.1 問題の本体

視覚較正（群A′）は、文字アニメを 0%→100%（300ms）で再生し、指定した進み具合 `s` で
打ち切って字を当てさせる。打ち切りは**描く前に判定**するので、画面が16.7ms（60Hz）でしか
書き換わらないと、`s` の小さい水準は**1枚も描かれず真っ白**になる。

- 60Hz → 5.6%刻み、120Hz → 2.8%刻み、**30Hz → 11.1%刻み**（今日の実測でいちばん多かった）
- 今日、丸山さんが本番URL (`kana-task.web.app`) を自分で通した記録: `refresh_hz` は
  60が30件・31が25件・77が8件。**視覚の本命25問中11問が s=0（真っ白）**だった
- 方式によって s=0 の見え方が違う（うすい→濃い／点が増える／端から現れる は「何も出ない」だが、
  **ぼやけ→はっきり だけ最大ぼかしの字が1枚見えてしまう**。床が取れない）

### 2.2 なぜ今日まで気づかなかったか（教訓）

8/24に「アニメ600ms→300msへ半減」と「水準を薄い側へ寄せる」を**同じ日に両方**入れてしまった。
後者の根拠にした先生のパイロットは600ms基準だったため、見る時間が半分になった分だけ本来は
水準を**上げる**べきところを**下げて**しまっていた（先生600ms: 3%→61%正解／丸山300ms: 6.25%→0%）。
**前提の違う変更を同じ日に重ねると、片方の根拠がもう片方で無効になっていることに気づきにくい。**

いったんこの向きを直して `progress_pct_levels: [6, 12, 17, 25, 30, 50, 100]` に置き直したが
（60Hzで止まれる場所ぴったりに乗せた設計）、**これは60Hz前提であり、今日いちばん多かった
30Hz端末では低い水準がやはり真っ白になる**ことが後から判明した。

### 2.3 選択肢（`project/相談_視覚水準とフレーム量子化.md` に詳細）

| 案 | 内容 | 利点 | 欠点 |
|---|---|---|---|
| **A** | 打ち切り規則を「目標sを超えたら描かず終了」→「目標sでちょうど1枚描いて終了」に直す | 端末・負荷に関係なく指定した進み具合が必ず出る | v1（先行実験）の「提示は直さず実測で扱う」方針から外れる |
| **B** | 現状のまま。解析は実測値(`actual_s`)を使う（v1踏襲） | コードを変えない | **今日の実測で25問中11問が真っ白。0〜11%の間が測れない** |
| **C** | 水準をフレーム刻み（30Hzの11.1%刻み）の倍数のすぐ上に置く | コード変更なし。端末差がほぼ消える。v1の方針とも矛盾しない | 使える進み具合が実質 0/11.1/22.2/…に限られる。gammaで床を薄くはできる |

**まだ決まっていない。** 次のセッションはまず丸山さんとこれを決めること。

### 2.4 決まったあとにやること

1. `transfer_config.js` の `visual.families.fade.progress_pct_levels` を置き直す
2. `experiment/tools/gamma_compare.html`（新規・未コミット）と `level_chooser.html` で、
   新しい水準に対して `fade.gamma` と `blur.max_radius_px` を目で再判定する
   （**静止画で見てはいけない**。本番と同じ時間で再生して判断する）
3. `check_transfer_stimuli.js` 等の検算をやり直す
4. `config_version` を上げて、変更点を版履歴コメントに残す

### 2.5 いま未コミットの作業（消さずに引き継ぐこと）

視覚水準そのものとは別に、「いちばん薄い水準でも読めて（見えて）しまう」という床の問題に、
今日すでに手を付けている。**`git diff` で確認できる、まだコミットしていない変更**:

- `experiment/transfer_config.js`: `fade.gamma` を 1.0→2.0、`blur.max_radius_px` を 48→72
  （丸山さんが `level_chooser.html` で本番と同じ時間の再生を見て判定した値）
- `experiment/tools/build_gamma_compare.js` / `gamma_compare.html`（新規）: gamma=1.0 と 2.0 を
  同じ1フレームで見比べるための判定ツール
- `experiment/tools/build_level_chooser.js` / `level_chooser.html` / `visual_trial_list.html`（変更）

**この2つの値（gamma・blur半径）は水準の並び（案A/B/C）が変わると前提が崩れる**ので、
水準を決め直したら必ず作り直すこと（`transfer_config.js` の該当コメントに手順を明記済み）。

---

## 3. 今日の他の変更（完了・コミット済み）

### 3.1 課題前の関門（マナーモード対策）

iPhoneの着信スイッチ（マナーモード）はWeb Audioも消すため、無音のまま70問答えて確認問題で
除外される人が出る問題への対処。

- **聞き取り確認**: あみたろで合成した数字（いち・に・さん・よん）を1つ鳴らし4択。2回連続正解しないと
  進めない。外すとマナーモード解除・音量を上げる案内を出す
- **見え方確認**: 全部表示した字を4択（本命8字・decoy12字は使わない）。同じく2回連続正解が必要
- `audio_check_misses` / `vision_check_misses` を記録（除外判定には使わない）
- `transfer.js` VERSION は現在 **3.14**

### 3.2 解析の横軸を実測値に修正

`analyze_transfer.py` が `actual_s`（実際に見えた進み具合）を読み込んでいたのに、曲線の横軸には
指定値（`progress_pct`）を使っていた。v1の方針（記録済み `actual_ms` を横軸にする）と食い違って
いたため修正。`level` 列は視覚では `actual_s×100` を使い、名目は `level_nominal` に残す。

### 3.3 偽ターゲットの選び方を純ランダムに

系統的な配り方（候補を巡ごとに並べ替えて5人で使い切る）から、参加者ごとに独立な純ランダムへ変更
（丸山判断。decoyの回答は解析に使わないので均等配分の利点より説明のしやすさを優先）。
実装では `Math.random()` は使わない（途中再開で12字が変わり頻度差ゼロの設計が崩れるため）。

---

## 4. システムの現状

### 4.1 配信先

Firebase Hosting (`https://kana-task.web.app`) にデプロイ済み。**`pre_launch: true` のまま**
（`curl -s https://kana-task.web.app/build_info.json` で確認）。参加者はまだ入れない。

### 4.2 Firestore

- プロジェクト `ifont-transfer`（東京リージョン）
- **⚠ `is_test` の印が無い行が残っている。** 前回記録では103行（回答96・見え心地1・名簿6）だったが、
  **今日も本番URLで視覚水準の検証のため複数回通しているので、さらに増えている可能性が高い（要確認）**。
  掲載前に必ず削除すること:
  ```bash
  python3 experiment/tools/purge_transfer_firestore.py --yes
  ```

### 4.3 同意画面・Yahoo掲載文

前回から変更なし。同意画面は906字→190字に圧縮済み。「Yahooの掲載文へ情報を寄せてスクロール不要化する」
という丸山さんの方針転換はまだ未実装（詳細は前回HANDOFFの該当節、または `project/掲載文.md`）。

---

## 5. 確定している実験設計（変更不要・前回から継続）

- **4集団**: Acal（音声打ち切り・80人）／A′（視覚較正・150人）／B（生成アニメ検証・168人）／
  C（見え心地・30人）。計428人・謝礼総額60,403円（枠7.5万円）
- **本命8字**: あ・か・が・ぱ・ま・つ・し・ら（音声学的な層化。「ら」が弾き音の代表）
- **出題設計**: 1人70問（本命8字×3点=24問＋偽ターゲット12字×3回=36問＋確認問題10問）。
  頻出20字がすべて3回ずつ出るので、出現頻度から本命を特定できない設計
- **判定基準**: δ=6正答率ポイント、Holm法で多重比較調整、除外は確認問題Aの正答率のみ
- **群Atest は廃止**（外部専門家2名の相談で「必須ではない」の結論。詳細は
  `project/相談_Atestは必要か.md`）

詳細はすべて `project/実験計画書_転写検証.md`（正本）にある。

---

## 6. 残っている作業（優先順）

| # | 作業 | 状態 |
|---|---|---|
| 1 | **視覚水準（案A/B/C）を丸山さんと決める** | 未決定・最優先（§2） |
| 2 | 水準決定後、`fade.gamma`・`blur.max_radius_px` を再判定 | 2.5の未コミット分と合わせて |
| 3 | 上記の未コミット変更＋水準変更をまとめてコミット | 2以降 |
| 4 | Firestoreの `is_test` 無し行を削除 | 未着手・**掲載前必須** |
| 5 | 同意画面をYahooへ寄せる＋スクロール不要化 | 未着手 |
| 6 | Yahooタスク説明文4本 | 未着手 |
| 7 | 群Bの打ち切り時点を聴覚と同じ字ごとの配置に | 未着手 |
| 8 | `project/刺激作成_キャリアフレーズ.md` の仕上げ（論文転用前の見直し） | 一次記録は完成済み |
| 9 | 掲載申請 | 1〜4完了後 |
| 10 | 生成・凍結 → 検証＋群Cの掲載 | 較正データ収集後 |
| 11 | 論文の後半（結果・考察） | データ取得後 |

**⚠ 群Bの本番前に `transfer_warp.json` の生成が必要。** 現在存在せず、無いと提案手法が
等速で代用され、検証データにならない。

---

## 7. 教訓（次回への申し送り）

### 7.1 機械検査を信用しすぎない

このプロジェクトで**3回目**、機械が「問題なし」と判定したものを丸山さんが聞いて/見て問題を
見つけている：①「ん」の残り 0/68→実際34/68字、②「り」の弾き音 16ms存在→耳では「い」、
③視覚の水準 progress_pct_levels の理論値では問題なし→実機で25問中11問が真っ白。
**刺激・水準の合否は必ず人が確かめてから確定する。**

### 7.2 同じ日に前提の異なる変更を重ねない

「アニメを300msに半減」と「水準を薄い側へ寄せる」を同じ日に両方入れたため、
後者の根拠（600msのパイロット）が前者によって無効化されていることに気づけなかった。
**変更のたびに、その根拠にしたデータの前提（この場合はアニメの長さ）が今も成立しているか確認する。**

### 7.3 静止画で判定しない

視覚較正のいちばん薄い水準は1フレーム（約17ms）しか映らない。gamma・ぼかし半径などの
「見えるか見えないか」の判定は、**必ず本番と同じ再生規則・同じ時間で動かして**行うこと。
静止画やスローで見ると実際より読めてしまう。

---

## 8. ファイルとURLの一覧

### 8.1 確認・比較ページ（聴覚。すべてbase64埋め込み）

| ページ | 内容 |
|---|---|
| `experiment/tools/check_final_stimuli.html` | 本番の544本を五十音表で |
| `experiment/tools/check_sa_acc1.html` | 「さ」＋核あり の68字（切り出し直後） |
| `experiment/tools/compare_target_accent.html` | 本命8字を核あり／なしで聞き比べ |
| `experiment/tools/compare_biriro.html` | び・り・ろ の合成のさせ方10通り |

### 8.2 確認・比較ページ（視覚。未コミット分含む）

| ページ | 内容 |
|---|---|
| `experiment/tools/level_chooser.html` | 水準・gamma・ぼかし半径を本番と同じ再生で目視判定するツール |
| `experiment/tools/gamma_compare.html`（新規・未コミット） | gamma=1.0 と 2.0 を同じ1フレームで見比べる |
| `experiment/tools/visual_trial_list.html` | 視覚試行の一覧（変更あり・未コミット） |
| `experiment/tools/expected_curve.html` | 水準案から予想される正答率曲線の下描き |

### 8.3 主なスクリプト

| ファイル | 役割 |
|---|---|
| `experiment/tools/build_carrier_takes.py` | 聴覚刺激の合成（`TARGET_ACCENT_BY_KANA`・`LEAD_VOWEL_BY_KANA`） |
| `experiment/tools/crop_carrier_fricative.py` | 「さ」の摩擦で切り出す |
| `experiment/tools/normalize_takes.py` | 音量そろえ |
| `experiment/tools/build_gate_ladders.py` | 打ち切り点の配置 |
| `experiment/tools/build_transfer_gates.py` | 544本の刺激を生成 |
| `experiment/tools/check_transfer_stimuli.js` | 一式検査 |
| `experiment/tools/build_gamma_compare.js`（新規・未コミット） | gamma比較ページの生成 |
| `experiment/tools/build_hosting.sh` | Firebase配信用ファイル集め |
| `experiment/tools/purge_transfer_firestore.py` | 試し打ちを消す |
| `experiment/tools/analyze_transfer.py` | 解析（曲線・図。横軸は実測値） |

### 8.4 主な文書

| ファイル | 内容 |
|---|---|
| `project/実験計画書_転写検証.md` | **正本** |
| `project/刺激作成_キャリアフレーズ.md` | 聴覚刺激の一次記録（論文用） |
| `project/相談_視覚水準とフレーム量子化.md` | **視覚水準問題の相談文書（未解決）** |
| `project/相談_Atestは必要か.md` | Atest廃止の経緯と結論 |
| `project/相談_出題範囲の学習をどう防ぐか.md` | 学習対策の経緯 |
| `project/掲載文.md` | 掲載文（未完成） |
| `project/掲載前チェックリスト.md` | 掲載前に消し込む |
| `project/Firebase_Hosting移行手順.md` | 配信の手順・切り戻し |
| `paper/wiss2026_draft.md` | 論文の正本 |

### 8.5 音源

| パス | 内容 |
|---|---|
| `experiment/transfer_stimuli_amitaro/` | **現行の刺激544本（完成・配信中）** |
| `experiment/tts_candidates_carrier2/_rejected_acc0/` | 全字accent=0版（却下・比較用） |
| `experiment/transfer_stimuli/` | 自然音声の刺激544本（予備。丸山さんが「合成音声でやる」と決めているので使わない） |
| `recordings_raw/adopted/` | 丸山さんの録音68字（凍結保存） |

---

## 9. 日程

| 日 | やること |
|---|---|
| 8/25（火・今日） | 聴覚刺激完成／視覚水準の問題を発見・相談文書を作成（**未決定のまま日をまたぐ**） |
| 8/26（水） | **視覚水準を決定 → 較正フェーズ掲載・データ収集** |
| 8/27（木） | 生成・凍結 → 検証＋群C掲載 |
| 8/28（金） | データ収集・解析 |
| 8/29〜30 | 論文執筆（結果・考察） |
| 8/31（月） | **投稿・23:59締切** |

**律速は視覚水準の決定のみ。** 決まれば掲載自体は速い（岐阜大の実例で3時間500人集まった実績あり）。
