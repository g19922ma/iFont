# 発注前チェックリスト（Stage 1・Stage 2）

方針（丸山 8/14）: **実験に甘いところを残さない。全項目が済むまで発注しない。**
「済」には検証方法を必ず添える。担当: F=Fable(実装・検証) / M=丸山 / K=栗原先生

## Stage 1（乙・干渉判定）— 発注ゲート

### データの取りこぼし対策
- [ ] F: **Firestore二重書き込みの実機確認**（?prod=1で1セッション通し、soa_trials/sessionsに
      全行が入ることをコンソールで目視。gain: 実装済み 4bfced4eb）
- [ ] F: **同時実行の負荷試験**（30並列で42試行分をPOSTするスクリプトを書き、
      Firestore側の到達率100%を確認。GAS側は参考記録）
- [ ] F: **end-to-endリハーサル**（同意→42試行→完了コード表示→保存行と完了コードの照合まで、
      聴覚・視覚それぞれ1周）

### 測定の妥当性
- [x] F: GASのactual_*/env列挙漏れ修正（23cfce726）＋ページ側env/duration送信
- [ ] F: **解析スクリプトの事前凍結**——事前登録の判定手続き(7.1のt手続き・二段階3区分)を
      実装し、パイロットデータ(研究者2名)で数値一致を確認してからコミット。
      データが来てから解析コードを書くのは甘さの温床
- [ ] F: 刺激の凍結ハッシュ記録（audio1char/2char manifestと音声のSHA256をdocsに記録。
      PSOLA非決定性の教訓）
- [x] M: **再生機器ポリシー決定(8/14)**: スピーカー○・有線ヘッドホン○・無線(Bluetooth)×
      （無線は再生開始の頭切れと符号化で刺激が変わるため）。同意画面に3択の機器申告を実装、
      無線選択はブロック(切替後に続行可)、選択は全回答に記録(audio_device列)。
      Woods式の音響チェックは不採用(HCI分野の作法として自己申告+無線排除で足りると判断)。
      検証: 同意画面で無線選択→ボタン無効化→有線に変更→続行可、を目視確認すること
- [ ] K: 募集文面と倫理審査承認内容の整合確認（謝礼額・所要時間・データの扱い）

### 手続きの固定
- [ ] M/K: **事前登録の確定**（prereg_interference.mdを最終化し、OSF登録またはコミットハッシュ
      指定で日時を固定。発注より前であることが必須）
- [ ] M/K: Yahoo発注画面での手数料実額・完了コード照合フローの確認
- [ ] K: ブランチ→mainマージ（本番ページはmainのPagesから配信）

## Stage 2（frac・g較正）— Stage1判定後の発注ゲート

### 実装（現状は本番化未着手 = 一番大きな「甘さ」）
- [ ] F: **frac課題の本番化**（prod_common.js配管: 同意→保存→完了コード。乙と同一方式）
- [ ] F: 乙で確立した改善のfracへの移植（クリック開始・教示・出題の配り方）
- [ ] F: **visual1charの実測露出時間の記録**（rAF実測。frac=5は名目10msでサブフレーム。
      7/22監査指摘・未実装）
- [ ] F: GAS/Firestoreのfrac用スキーマ（frac_trials/frac_sessions）と保存確認

### 刺激の検品（か行パイロットの教訓を本番プールへ）
- [ ] F: **全68音×全frac水準の切り出し後実効音量を機械検品**（-35dBFS未満の断片を列挙し、
      物理的に無音の水準はmanifestにフラグ。「短い断片は検品してから使う」の適用）
- [ ] F: 視覚frac刺激の実表示検証（最短水準が実際に描画されているかをrAFログで確認）

### 設計の固定
- [ ] F/M: **Stage2の解析計画を発注前に文書化・凍結**（恒常法+階層ロジスティックの式、
      文字ごとの曲線→gの構成手順、除外基準、必要試行数の根拠。Stage1のprereg並みの粒度）
- [ ] M/K: Stage2のN・試行配分・予算の最終確認（session_design.mdの数値を再点検）
- [ ] M/K: Stage2の事前登録（Stage1と同様に日時固定）

## 運用ルール
- チェックが全部つくまで発注しない（1日遅れる方が、欠けたデータより安い）
- 検証はすべて「やった」でなく「何をどう確認したか」を本ファイルに追記
- 発注後の設計変更は禁止。気づきは解析の感度分析か論文の限界へ
